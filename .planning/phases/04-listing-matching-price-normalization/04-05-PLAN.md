---
phase: 04-listing-matching-price-normalization
plan: 05
type: execute
wave: 4
depends_on: [04-04]
files_modified:
  - scripts/ingest_worker.py
autonomous: true
requirements: [MATCH-01, MATCH-02, MATCH-03]
must_haves:
  truths:
    - "run_ingestion_once() calls run_matching_once(db, run_id, started_at) once, as a second stage AFTER the per-product CATALOG fetch loop completes — never per-listing inside the fetch loop (PITFALLS Performance Trap)"
    - "The ingestion_runs document for a successful run carries integer top-level fields listings_matched, listings_unmatched, listings_excluded (D-04), merged from run_matching_once's return dict"
    - "All existing Phase 3 test_ingest_worker.py tests remain green (no regression to INGEST-01/02/03)"
  artifacts:
    - "scripts/ingest_worker.py (run_ingestion_once extended; top-level `from scripts.matching import run_matching_once`)"
  key_links:
    - "run_matching_once's returned counts are merged into the same ingestion_runs update_doc that already sets products_queried/listings_fetched/listings_written"
    - "matching runs in-process as a second stage over the run's batch (scoped by run_id), activating MATCH-01/02/03 in the live pipeline"
---

<objective>
Wire Phase 4's matching stage into the Phase 3 ingestion worker: call `run_matching_once(db, run_id, started_at)` at the end of `run_ingestion_once()` and merge its returned counts (`listings_matched`, `listings_unmatched`, `listings_excluded`) into the `ingestion_runs` document (D-04), turning the integration test GREEN.

Purpose: This is the activation point that makes MATCH-01/02/03 run in the real pipeline — every scheduled ingestion cycle now matches, excludes, filters, and aggregates the listings it just fetched, and records per-run match-health counts alongside the existing observability metadata (D-04, ROADMAP SC-1 "match results inspectable"). Matching runs as a decoupled second stage over the run's batch, not synchronously per-listing inside the fetch loop (PITFALLS Performance Trap).

Output: `scripts/ingest_worker.py` extended with the matching call and the three new ingestion_runs count fields. Phase 3's existing contract tests stay green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-listing-matching-price-normalization/04-CONTEXT.md
@.planning/phases/04-listing-matching-price-normalization/04-RESEARCH.md
@.planning/phases/04-listing-matching-price-normalization/04-PATTERNS.md
@scripts/ingest_worker.py
@scripts/matching.py
@tests/test_ingest_worker.py
@tests/test_matching.py
</context>

## Artifacts this phase produces (this plan's slice)

- `scripts/ingest_worker.py` — new top-level import `from scripts.matching import run_matching_once`; `run_ingestion_once()` extended to call it after the fetch loop and merge counts.
- New `ingestion_runs` document fields (D-04): `listings_matched`, `listings_unmatched`, `listings_excluded` (integers, top-level/flat — never a nested sub-document, never a separate `matching_runs` collection).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Call run_matching_once from run_ingestion_once and record D-04 counts</name>
  <files>scripts/ingest_worker.py</files>
  <behavior>
    - After the `for product in CATALOG:` fetch loop finishes, run_ingestion_once calls run_matching_once(db, run_id, started_at) exactly once.
    - The final ingestion_runs update_doc gains three integer fields listings_matched / listings_unmatched / listings_excluded taken from run_matching_once's return dict.
    - test_run_ingestion_once_records_match_counts (from tests/test_matching.py) passes: a live run produces an ingestion_runs doc with those three flat integer fields.
    - All existing test_ingest_worker.py tests still pass (happy-path, partial-on-error, skip-when-locked, upsert idempotency, lock semantics) — matching adds fields and in-place updates, never changes active_listings document COUNT and never alters status/products_queried/listings_fetched/listings_written.
  </behavior>
  <read_first>
    - scripts/ingest_worker.py (the WHOLE run_ingestion_once, lines ~171-276: the top-level import block ~32-43 to extend; the `try:` block where the `for product in CATALOG:` loop lives; the `status = "success" if not errors else "partial"` line; the `finally:` block's `update_doc = {...}` and `db.ingestion_runs.update_one({"_id": run_id}, {"$set": update_doc})`; and the skip-when-locked early-return path which must NOT run matching)
    - scripts/matching.py (run_matching_once signature `run_matching_once(db, run_id, ts) -> {"listings_matched","listings_unmatched","listings_excluded"}`; confirm it defers the PRODUCT_TYPE_SEARCH_TERMS import so a top-level `from scripts.matching import run_matching_once` in ingest_worker.py does NOT create a circular import)
    - .planning/phases/04-listing-matching-price-normalization/04-PATTERNS.md (the `scripts/ingest_worker.py (extend — call run_matching_once)` section: the exact insertion point after the fetch loop, the top-level flat-field convention for ingestion_runs, and the note that no lazy import is needed here because matching.py breaks the cycle on its side)
    - .planning/phases/04-listing-matching-price-normalization/04-RESEARCH.md (the "Integration point in scripts/ingest_worker.py" paragraph at the end of Code Examples — call after the CATALOG loop, before finalizing, merge three flat int fields)
    - .planning/phases/04-listing-matching-price-normalization/04-CONTEXT.md (D-04)
    - tests/test_matching.py (test_run_ingestion_once_records_match_counts assertions)
  </read_first>
  <action>
    Edit `scripts/ingest_worker.py`:

    1. Add `from scripts.matching import run_matching_once` to the existing top-level import block (alongside the other `from scripts.* import ...` lines). This is safe (no circular import) because scripts/matching.py imports PRODUCT_TYPE_SEARCH_TERMS lazily inside `_canonical_phrase` (established in Plan 04-03) — verify both import orders still work.

    2. Inside `run_ingestion_once`'s existing `try` block, AFTER the `for product in CATALOG:` fetch loop completes (i.e. right where `status = "success" if not errors else "partial"` is set), call `match_counts = run_matching_once(db, run_id, started_at)`. Pass `started_at` (the run's start timestamp already computed at the top of the function) as the `ts` argument so each written price_points doc's `ts` aligns with the run's `active_listings.fetched_at`. This call lives on the primary (lock-acquired) path only — the `skipped_locked` early-return path must NOT call it (a skipped run fetched nothing to match). Keep `match_counts` in a scope reachable by the `finally` block (initialize `match_counts = {"listings_matched": 0, "listings_unmatched": 0, "listings_excluded": 0}` before the `try`, so if an exception aborts before matching runs, the finally still writes zeros rather than raising NameError).

    3. In the `finally` block's `update_doc` dict, add the three new flat top-level integer fields sourced from `match_counts`: `"listings_matched": match_counts["listings_matched"]`, `"listings_unmatched": match_counts["listings_unmatched"]`, `"listings_excluded": match_counts["listings_excluded"]` — alongside the existing products_queried/listings_fetched/listings_written/errors/status/finished_at fields (D-04). Do NOT create a nested sub-document and do NOT add a separate collection.

    Do not otherwise change the fetch loop, the lock logic, the token-once reuse, or the per-product error isolation.
  </action>
  <verify>
    <automated>pytest tests/test_matching.py::test_run_ingestion_once_records_match_counts tests/test_ingest_worker.py -x</automated>
  </verify>
  <acceptance_criteria>
    - test_run_ingestion_once_records_match_counts passes: the run's ingestion_runs document has integer `listings_matched`, `listings_unmatched`, `listings_excluded` at top level.
    - All existing tests in tests/test_ingest_worker.py pass unchanged (no regression): test_run_ingestion_once_happy_path still asserts status=="success", products_queried/listings_fetched/listings_written == len(CATALOG), and active_listings count == len(CATALOG).
    - `python -c "import scripts.ingest_worker, scripts.matching; print('ok')"` and `python -c "import scripts.matching, scripts.ingest_worker; print('ok')"` both succeed (no circular import in either order).
    - run_matching_once is called exactly once per run and only on the lock-acquired path (the skipped_locked branch does not reference it) — provable by test_run_ingestion_once_skips_when_locked still passing with zero eBay/matching side effects.
  </acceptance_criteria>
  <done>run_ingestion_once invokes run_matching_once as a second stage and records D-04 match counts on ingestion_runs; the integration test is GREEN and all Phase 3 tests still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ingestion worker process → matching module | run_ingestion_once now invokes matching in-process over the run's batch; a matching failure could affect the run's finalization |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05 | Denial of Service | A matching-stage exception aborting run_ingestion_once and leaving the lock/run doc dangling | medium | mitigate | run_matching_once is called inside the existing outer `try` whose `finally` always writes the run doc and calls release_lock; run_matching_once itself isolates per-listing failures (Plan 04-04, T-04-01). match_counts is pre-initialized to zeros so the finally never raises NameError |
| T-04-01 | Denial of Service | Malformed `title` in the fetched batch | medium | mitigate | Inherited — isolated per-item inside run_matching_once (Plan 04-04) |
| T-04-03 | Tampering | Manual MongoDB edits (no active_listings validator) | low | accept | Intentional per D-07 |

Security note: `security_block_on: high` — no high/critical threats open; medium threats mitigated by the existing try/finally + zero-initialized counts.
</threat_model>

<verification>
- `pytest tests/test_matching.py::test_run_ingestion_once_records_match_counts tests/test_ingest_worker.py -x` passes.
- `pytest` (full suite) is GREEN — Phase 2/3/4 tests all pass; this is the phase gate before /gsd-verify-work.
- Both import orders of ingest_worker/matching succeed (no circular import).
- Manual/CI single run `python -m scripts.ingest_worker --once` (when EBAY credentials exist) prints the run summary; the ingestion_runs doc now carries the three match-count fields. (Credential-gated / deferrable, mirroring Phase 3's live-verification precedent — see Manual-Only Verifications in 04-VALIDATION.md.)
</verification>

<success_criteria>
run_ingestion_once activates the matching stage and records D-04 match counts; the integration test and the full suite are GREEN with no Phase 3 regression; no circular import.
</success_criteria>

<output>
Create `.planning/phases/04-listing-matching-price-normalization/04-05-SUMMARY.md` when done.
</output>
