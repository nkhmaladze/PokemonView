---
phase: 07-launch-hardening-v1-active-price
plan: 05
type: execute
wave: 3
depends_on: ["07-04"]
files_modified:
  - frontend/.env.production
autonomous: false
requirements: [SC-1]
user_setup:
  - service: vercel
    why: "Static host for the Vite/React SPA build (D-01 — planner's choice among Vercel/Netlify/Cloudflare Pages)."
    dashboard_config:
      - task: "Authenticate the Vercel CLI (vercel login) and deploy the frontend build"
        location: "Vercel CLI (`vercel --prod` from frontend/)"
must_haves:
  truths:
    - "vercel (npm global CLI) is installed only after a human confirms its npm legitimacy (SUS false-positive gate)."
    - "frontend/.env.production sets VITE_API_BASE_URL to the real https://<app>.fly.dev host from plan 07-04 BEFORE the Vite build (Pitfall 4 — VITE_* is inlined at build time)."
    - "The SPA is reachable at its Vercel URL and its API calls succeed with no CORS error."
    - "The Flask API's CORS_ORIGINS is updated (via fly secrets set) to the real Vercel origin, triggering a rolling restart (Pitfall 5)."
  artifacts:
    - "frontend/.env.production :: VITE_API_BASE_URL"
    - "Deployed Vercel SPA at its public URL"
    - "Fly secret CORS_ORIGINS = the Vercel production origin"
  key_links:
    - "VITE_API_BASE_URL (build-time) must equal the deployed Fly API host, or the SPA calls the wrong origin."
    - "CORS_ORIGINS on the Fly API must include the Vercel origin, or the browser blocks API responses even though the server returns 200 (Pitfall 5)."
---

<objective>
Deploy the React SPA to Vercel (behind a package-legitimacy gate for the global CLI), wired to the live Fly API URL from plan 07-04, and close the CORS loop by pointing the API's CORS_ORIGINS at the real Vercel origin. This completes SC-1 end-to-end: a user can load the public site and it successfully reads live data from the public API.

Purpose: The final launch step — the user-facing surface goes live and talks to the deployed API without CORS errors.
Output: frontend/.env.production, a deployed Vercel SPA, and CORS_ORIGINS updated on the Fly API.
</objective>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- **New file** `frontend/.env.production` — `VITE_API_BASE_URL=https://<app>.fly.dev` (the real Fly host from plan 07-04).
- **Deployed Vercel SPA** at its assigned `*.vercel.app` origin.
- **Updated Fly secret** `CORS_ORIGINS` = the Vercel production origin.
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-launch-hardening-v1-active-price/07-RESEARCH.md
@.planning/phases/07-launch-hardening-v1-active-price/07-04-SUMMARY.md
@api/config.py
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package-legitimacy gate — vercel (npm global CLI) (T-07-SC-v)</name>
  <read_first>
    - .planning/phases/07-launch-hardening-v1-active-price/07-RESEARCH.md — "Package Legitimacy Audit": vercel flagged SUS (`too-new`, a false positive from Vercel's continuous-release cadence); 2.7M weekly downloads, official github.com/vercel/vercel org.
  </read_first>
  <action>Blocking-human legitimacy gate: halt before `npm install -g vercel` until a human verifies it against npmjs.com/package/vercel and explicitly approves. Not auto-approvable. On rejection, halt the SPA deploy path (or switch to an approved alternative static host).</action>
  <what-built>
    `vercel` is the npm global CLI used to deploy the Vite SPA. RESEARCH flagged it SUS only on a `too-new` signal (rolling releases), with 2.7M weekly downloads and the official Vercel org repo — an extremely low-risk approval matching the false-positive pattern approved repeatedly in this project.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/vercel and confirm it is the official Vercel CLI (published by the `vercel` org, links to github.com/vercel/vercel, multi-million weekly downloads).
    2. Confirm the SUS flag is the expected `too-new`/continuous-release false positive, not a typosquat or unknown publisher.
    3. This checkpoint is NOT auto-approvable — a human must explicitly approve before `npm install -g vercel`.
  </how-to-verify>
  <resume-signal>Type "approved" to allow the global vercel install, or describe the concern.</resume-signal>
  <acceptance_criteria>
    - A human has explicitly approved (or rejected) after checking npmjs.com/package/vercel.
    - On rejection, the SPA deploy path halts (or switches to an approved alternative host) — do not install an unapproved global package.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create frontend/.env.production with the live Fly API URL</name>
  <files>frontend/.env.production</files>
  <read_first>
    - .planning/phases/07-launch-hardening-v1-active-price/07-04-SUMMARY.md — the exact assigned `https://<app>.fly.dev` host recorded during plan 07-04's deploy verification.
    - .planning/phases/07-launch-hardening-v1-active-price/07-RESEARCH.md — Pitfall 4 (VITE_* values are inlined at `vite build` time, not read at runtime — this file must exist and hold the correct URL before the build runs).
    - frontend/ — confirm it has no existing `.env*` file and uses Vite's `import.meta.env.VITE_*` convention (api/client.js from Phase 6 reads VITE_API_BASE_URL).
  </read_first>
  <action>
    Create frontend/.env.production containing exactly one line: `VITE_API_BASE_URL=https://<app>.fly.dev`, substituting the real Fly host recorded in 07-04-SUMMARY (no trailing slash, HTTPS). This value is a public URL, not a secret. Do NOT put any secret (MONGODB_URI, eBay creds, Discord webhook) in this or any frontend env file — only `VITE_*` build-time public values belong here. If the Fly host from 07-04 is not yet recorded, stop and surface that this task is blocked on 07-04's deploy output rather than guessing a URL.
  </action>
  <verify>
    <automated>grep -c 'VITE_API_BASE_URL=https://' frontend/.env.production</automated>
  </verify>
  <acceptance_criteria>
    - frontend/.env.production exists with a single `VITE_API_BASE_URL=https://<app>.fly.dev` line matching the 07-04 host.
    - The URL uses HTTPS and has no trailing slash.
    - `grep -Evi 'mongodb|ebay|discord|secret|password' frontend/.env.production` still returns the VITE_API_BASE_URL line (i.e. no secret token is present in the file).
  </acceptance_criteria>
  <done>frontend/.env.production points VITE_API_BASE_URL at the live Fly API host, ready for the Vite build.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Deploy SPA to Vercel + point CORS_ORIGINS at the Vercel origin + verify no CORS error</name>
  <read_first>
    - .planning/phases/07-launch-hardening-v1-active-price/07-RESEARCH.md — "Code Examples: Vercel CLI deploy", Pitfall 5 (CORS_ORIGINS must include the Vercel origin or the browser blocks 200 responses), "Code Examples: fly secrets set" (rolling restart mechanism).
    - api/config.py — CORS_ORIGINS is comma-split (Phase 5 CR-02), so multiple origins (e.g. prod + preview) can be provided comma-separated.
    - .planning/phases/07-launch-hardening-v1-active-price/07-VALIDATION.md — Manual-Only Verifications: SPA reachable + calls API without CORS error (SC-1).
  </read_first>
  <action>Human-run SPA deploy + CORS-close runbook (Vercel auth and the CORS secret update are involved): `npm install -g vercel` (gated by Task 1), `cd frontend && vercel --prod` (auto-detects Vite, inlines VITE_API_BASE_URL, deploys dist/), note the assigned Vercel origin, then `fly secrets set CORS_ORIGINS="https://<assigned>.vercel.app" --app pokemonview` (rolling restart, no redeploy), and verify in a browser DevTools Network tab that the SPA's API calls return 200 with no CORS error. Follow the ordered steps in how-to-verify.</action>
  <what-built>
    frontend/.env.production points at the live Fly API; the Vite SPA is ready to build and deploy. This step deploys it to Vercel and updates the API's allowed origins.
  </what-built>
  <how-to-verify>
    Run this runbook (human operator — Vercel auth and the CORS secret update are involved):
    1. `npm install -g vercel` (gated by Task 1).
    2. `cd frontend && vercel --prod` — the CLI auto-detects Vite, runs `vite build` (inlining VITE_API_BASE_URL from frontend/.env.production), and deploys `dist/`. Authenticate (`vercel login`) when prompted. Note the assigned production origin, e.g. `https://pokemonview.vercel.app`.
    3. Point the API's allowed origins at that Vercel origin: `fly secrets set CORS_ORIGINS="https://<assigned>.vercel.app" --app pokemonview` (comma-separate if adding a preview origin too). This triggers Fly's automatic rolling restart — no `fly deploy` needed (same mechanism as the eBay-cred injection in D-05).
    4. Open the deployed Vercel URL in a browser, open DevTools → Network, and confirm the SPA's calls to `https://<app>.fly.dev/products` succeed (HTTP 200) with NO "blocked by CORS policy" console error.
  </how-to-verify>
  <resume-signal>Type "live" once the SPA loads and its API calls succeed with no CORS error, or paste the error to debug.</resume-signal>
  <acceptance_criteria>
    - The SPA is reachable at its `https://<assigned>.vercel.app` URL (loads over HTTPS).
    - Browser DevTools Network tab shows the SPA's `GET /products` (and product-detail) calls returning 200 from the Fly API with NO CORS console error (SC-1 end-to-end).
    - `fly secrets list --app pokemonview` now includes CORS_ORIGINS (value not shown).
    - The catalog renders live data from the deployed API (not a placeholder/localhost) — confirms VITE_API_BASE_URL was inlined correctly (Pitfall 4).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → npm | The global `vercel` CLI enters the developer toolchain. |
| browser (Vercel origin) → Fly API | Cross-origin requests from the SPA are gated by the API's CORS policy. |
| build → static bundle | VITE_API_BASE_URL is inlined into the public JS bundle at build time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-SC-v | Tampering | vercel npm global CLI install | high | mitigate | Blocking-human package-legitimacy checkpoint (Task 1) before `npm install -g vercel`; verified against npmjs.com/package/vercel (official Vercel org, 2.7M weekly downloads). Not auto-approvable. |
| T-07-11 | Information Disclosure | A secret leaking into a VITE_* build-time value (inlined into the public bundle) | high | mitigate | frontend/.env.production carries only the public VITE_API_BASE_URL; no MONGODB_URI/eBay/Discord value is ever placed in a frontend env file (acceptance criterion greps for secret tokens). |
| T-07-12 | Spoofing (CSRF-adjacent) / overly-permissive CORS | CORS_ORIGINS left as a wildcard in production | medium | mitigate | CORS_ORIGINS is set to the explicit Vercel origin (not `*`) via fly secrets set, closing the temporary permissive-CORS window from plan 07-04 (Pitfall 5, Phase 5 CR-02 comma-split). |
| T-07-09b | Spoofing / MITM | SPA served/loaded over plaintext | high | mitigate | Vercel serves the SPA over automatic HTTPS (V6, D-04); DevTools verification is against the https:// origin. |
</threat_model>

<verification>
- Vercel package-legitimacy checkpoint approved by a human.
- frontend/.env.production holds only the public VITE_API_BASE_URL (real Fly host).
- SPA loads at its Vercel URL; DevTools shows 200 API calls, no CORS error (SC-1).
- `fly secrets list` includes CORS_ORIGINS (explicit Vercel origin, not wildcard).
</verification>

<success_criteria>
The public SPA is live at its Vercel URL and successfully reads live data from the deployed Fly API with no CORS error, and the API's allowed origins are pinned to the real Vercel origin — SC-1 is satisfied end-to-end (site reachable at a public URL, talking to the public API). (SC-1)
</success_criteria>

<output>
Create `.planning/phases/07-launch-hardening-v1-active-price/07-05-SUMMARY.md` when done.
</output>
