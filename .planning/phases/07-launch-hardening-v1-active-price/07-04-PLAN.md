---
phase: 07-launch-hardening-v1-active-price
plan: 04
type: execute
wave: 2
depends_on: ["07-01", "07-02", "07-03"]
files_modified:
  - fly.toml
autonomous: false
requirements: [SC-1, SC-2]
user_setup:
  - service: fly.io
    why: "Container hosting for the Flask API (web) and ingestion worker (worker) process types (D-01/D-02)."
    env_vars:
      - name: MONGODB_URI
        source: "MongoDB Atlas — existing M0 cluster connection string (Database → Connect → Drivers)"
      - name: DISCORD_WEBHOOK_URL
        source: "Discord → Server Settings → Integrations → Webhooks → New Webhook → Copy Webhook URL (D-07/D-08)"
    dashboard_config:
      - task: "Set Network Access to Allow Access from Anywhere (0.0.0.0/0)"
        location: "MongoDB Atlas → Security → Network Access → Add IP Address"
  - service: mongodb-atlas
    why: "Fly Machines have no stable outbound IP; Atlas denies all connections until allow-listed (Pitfall 3)."
must_haves:
  truths:
    - "The Fly app is deployed from the authored fly.toml with its always-on overrides intact (not clobbered by fly launch defaults)."
    - "The Flask API answers over HTTPS at its https://<app>.fly.dev URL."
    - "Both the web and worker machines show `started` (never `stopped`) — no scale-to-zero (SC-2)."
    - "MONGODB_URI and DISCORD_WEBHOOK_URL are injected via `fly secrets set`, never baked into the image or fly.toml."
    - "eBay credentials are deliberately NOT set yet (D-05); the worker runs and fails at get_app_token, recording status=failed and firing the staleness alert (D-06)."
  artifacts:
    - "Deployed Fly app `pokemonview` with web + worker machines"
    - "Fly secrets: MONGODB_URI, DISCORD_WEBHOOK_URL"
    - "MongoDB Atlas Network Access entry permitting the Fly app"
  key_links:
    - "fly secrets set triggers an automatic rolling machine restart — the mechanism D-05 depends on for adding eBay creds later with no redeploy."
    - "worker process runs scripts.ingest_worker scheduled path → writes ingestion_runs → check_and_alert_staleness (plan 07-01) fires the Discord alert."
---

<objective>
Deploy the Fly.io app (API web process + ingestion worker process) from the authored Dockerfile/fly.toml, inject the runtime secrets via `fly secrets set`, open MongoDB Atlas Network Access to the Fly app, and verify the public API is reachable over HTTPS with both machines always-on. eBay credentials are intentionally left unset per D-05 — the worker genuinely fails and the staleness alert fires (D-06), which is the expected launch condition, not a blocker.

Purpose: SC-1 (public API reachable) and SC-2 (worker runs unattended on schedule) go live.
Output: A running Fly app with two always-on machines, secrets injected, Atlas reachable from production.
</objective>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- **Deployed Fly app** `pokemonview` — web (gunicorn) + worker (APScheduler) machines.
- **Fly secrets store entries** — MONGODB_URI, DISCORD_WEBHOOK_URL (CORS_ORIGINS set later in plan 07-05; eBay creds deferred per D-05).
- **MongoDB Atlas Network Access** entry permitting the Fly app to connect.
- **Possibly-updated** `fly.toml` — app name confirmed by fly launch/apps create (always-on overrides preserved).
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-launch-hardening-v1-active-price/07-RESEARCH.md
@.planning/phases/07-launch-hardening-v1-active-price/07-CONTEXT.md
@fly.toml
@Dockerfile
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Provision + deploy the Fly app (install flyctl, launch, secrets, Atlas)</name>
  <read_first>
    - .planning/phases/07-launch-hardening-v1-active-price/07-RESEARCH.md — "Environment Availability" (flyctl install), "Code Examples: fly secrets set", Pitfall 1 (verify always-on survived), Pitfall 3 (Atlas Network Access), Assumption A4 (confirm `fly secrets set` triggers the rolling restart at execution time).
    - fly.toml — the authored always-on config that must survive `fly launch` (do NOT let fly launch overwrite auto_stop_machines/min_machines_running).
    - .planning/phases/07-launch-hardening-v1-active-price/07-CONTEXT.md — D-05 (eBay creds absent, do NOT set them), D-03 (always-on), D-07/D-08 (Discord webhook).
  </read_first>
  <action>Human-run deploy runbook (account creation, browser auth, real secret values, and Atlas UI are human-only): install flyctl, `fly auth login`, create the app preserving the authored fly.toml (`fly launch --no-deploy --copy-config --name pokemonview` or `fly apps create` + keep fly.toml), verify the always-on overrides survived, set MongoDB Atlas Network Access to 0.0.0.0/0, `fly secrets set MONGODB_URI DISCORD_WEBHOOK_URL` (NOT eBay creds per D-05, NOT CORS_ORIGINS yet), then `fly deploy`. Operator must not paste real secret values into the agent transcript. Follow the ordered steps in how-to-verify.</action>
  <what-built>
    The Dockerfile, fly.toml, wsgi.py, requirements.txt (with gunicorn), and the staleness-alert code are all committed. This step deploys them to Fly.io as one image / two process types.
  </what-built>
  <how-to-verify>
    Run this runbook (human, one operator — involves account creation, browser auth, real secret values, and Atlas UI, none of which run unattended):
    1. Install flyctl if absent: `brew install flyctl` (macOS) or `curl -L https://fly.io/install.sh | sh`; then `fly auth login` (or `fly auth signup` for a new account) — completes in a browser.
    2. Create the app WITHOUT clobbering the authored fly.toml: `fly launch --no-deploy --copy-config --name pokemonview` (or `fly apps create pokemonview` then keep the existing fly.toml). After it runs, re-open fly.toml and confirm `auto_stop_machines = "off"`, `auto_start_machines = false`, `min_machines_running = 1` are STILL present (Pitfall 1 — restore them if fly launch rewrote them). Confirm the app is on a paid always-on `shared-cpu-1x` machine size, not the free/scale-to-zero shape (D-03).
    3. MongoDB Atlas → Security → Network Access → Add IP Address → Allow Access from Anywhere (0.0.0.0/0). This is the documented v1 pragmatic tradeoff (Pitfall 3 / T-07-03), accepted because Atlas username/password auth remains the real access-control layer.
    4. Inject runtime secrets (YOU run these — do not paste real secret values into the agent transcript): `fly secrets set MONGODB_URI="<atlas-connection-string>" DISCORD_WEBHOOK_URL="<discord-webhook-url>" --app pokemonview`. Do NOT set EBAY_CLIENT_ID/EBAY_CLIENT_SECRET now (D-05 — you do not have them yet; ingestion will fail-loud until they exist, which is expected). Do NOT set CORS_ORIGINS yet (plan 07-05 sets it to the real Vercel origin).
    5. Deploy: `fly deploy --app pokemonview`. Wait for both machines to reach a healthy/started state.
  </how-to-verify>
  <resume-signal>Type "deployed" once `fly deploy` succeeds and both machines are up, or paste the error to debug.</resume-signal>
  <acceptance_criteria>
    - `fly status --app pokemonview` shows the app deployed with both `web` and `worker` process groups present.
    - fly.toml still contains `auto_stop_machines = "off"` and `min_machines_running = 1` after fly launch (always-on preserved).
    - `fly secrets list --app pokemonview` lists MONGODB_URI and DISCORD_WEBHOOK_URL (values not shown — only names/digests).
    - EBAY_CLIENT_ID / EBAY_CLIENT_SECRET are NOT in `fly secrets list` (deliberately deferred, D-05).
    - No real secret value was pasted into the agent transcript.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify public API reachable + both machines always-on + worker scheduling</name>
  <read_first>
    - .planning/phases/07-launch-hardening-v1-active-price/07-VALIDATION.md — "Manual-Only Verifications" table (SC-1 API reachable, SC-2 machines always-on / worker fires).
    - .planning/phases/07-launch-hardening-v1-active-price/07-RESEARCH.md — Pitfall 1 warning signs (machine in `stopped` state), Pitfall 2 (worker does NOT crash-loop on missing eBay creds — it records status=failed and keeps scheduling).
  </read_first>
  <action>Human verification of SC-1/SC-2 against the live deploy: curl the public API over HTTPS (expect 200), confirm both machines are `started` (never `stopped`) via `fly machine list`, confirm the worker is scheduled and not crash-looping via `fly logs`, and confirm the expected credentials-absent Discord staleness alert arrived carrying no secret values. Record the exact `.fly.dev` host for plan 07-05. Follow the ordered steps in how-to-verify.</action>
  <what-built>
    The deployed Fly app with web (gunicorn API) and worker (APScheduler) process types, secrets injected, Atlas reachable.
  </what-built>
  <how-to-verify>
    1. Public API over HTTPS: `curl -I https://pokemonview.fly.dev/products` (substitute the real assigned .fly.dev host) → expect `HTTP/2 200`. Note the exact host for plan 07-05's VITE_API_BASE_URL.
    2. Always-on: `fly machine list --app pokemonview` → both the web and worker machines show state `started`, NOT `stopped` (SC-2, D-03). Re-check after a few minutes to confirm neither auto-stopped.
    3. Worker is scheduled and does NOT crash-loop: `fly logs --app pokemonview` shows the worker printed its "ingestion worker started — interval_hours=4" line and is running; with eBay creds absent, an ingestion attempt records status=failed (Pitfall 2) but the worker process stays `started` (no restart loop). Do NOT expect successful eBay data yet (D-05).
    4. Staleness alert (D-06): because no successful run exists yet, the Discord channel receives a staleness alert on the first scheduled attempt — confirm the alert message contains only counts/timestamps and NO secret values (webhook URL, MONGODB_URI, eBay creds) (V7 / T-07-01).
  </how-to-verify>
  <resume-signal>Type "verified" once the API returns 200, both machines are `started`, and the expected staleness alert arrived; otherwise describe what failed.</resume-signal>
  <acceptance_criteria>
    - `curl -I https://<app>.fly.dev/products` returns an HTTP 200 over HTTPS (SC-1).
    - `fly machine list --app pokemonview` shows both machines `started`, none `stopped` (SC-2).
    - `fly logs` shows the worker running (started message) and not crash-looping despite absent eBay creds (Pitfall 2).
    - A Discord staleness alert was received (D-06) and its content contains no secret values (V7).
    - The exact `<app>.fly.dev` host is recorded for plan 07-05.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fly Machine → MongoDB Atlas | Production process (not a developer laptop) now connects to Atlas over the public internet. |
| operator → Fly secrets store | Real MONGODB_URI / DISCORD_WEBHOOK_URL values are entered and stored encrypted. |
| internet → Fly web machine | Public HTTPS traffic reaches the API. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-04 | Information Disclosure | Secrets baked into image/fly.toml instead of the secrets store | high | mitigate | All runtime secrets injected via `fly secrets set` (encrypted at rest, env-injected at boot); fly.toml/Dockerfile carry zero secret values (verified in plan 07-03). Operator instructed not to paste secret values into the transcript. |
| T-07-09 | Spoofing / MITM | Public API served over plaintext | high | mitigate | `force_https = true` in fly.toml [http_service] (V6); Fly issues/terminates TLS automatically — verified via `curl -I https://...` returning HTTP/2 (D-04). |
| T-07-03 | Elevation of Privilege / Information Disclosure | MongoDB Atlas Network Access set to 0.0.0.0/0 | medium | accept | Documented, deliberate v1 tradeoff (RESEARCH Pitfall 3): Atlas username/password remains the real access-control layer; Fly Machines lack a stable outbound IP. Follow-up hardening (Fly static egress IP + tight allowlist) recorded as a post-launch item, not a v1 blocker. |
| T-07-10 | Denial of Service | Worker crash-loop misread as normal (missing eBay creds) | low | accept | Pitfall 2: get_app_token failure is caught inside run_ingestion_once and recorded status=failed; the scheduler keeps running. Expected launch condition per D-05/D-06, surfaced by the staleness alert, not a silent failure. |
</threat_model>

<verification>
- `curl -I https://<app>.fly.dev/products` → HTTP 200 (SC-1).
- `fly machine list` → both machines `started` (SC-2).
- `fly secrets list` → MONGODB_URI, DISCORD_WEBHOOK_URL present; no eBay creds (D-05).
- Discord staleness alert received with no secret values (D-06, V7).
</verification>

<success_criteria>
The Flask API is publicly reachable over HTTPS at its .fly.dev URL and both process machines run continuously without scaling to zero, with the worker attempting its schedule and the staleness alert firing as expected in the credentials-absent state. (SC-1, SC-2)
</success_criteria>

<output>
Create `.planning/phases/07-launch-hardening-v1-active-price/07-04-SUMMARY.md` when done.
</output>
