---
phase: 01-ebay-api-feasibility-gate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-ebay-api-feasibility-gate/MANUAL-STEPS.md
  - .planning/phases/01-ebay-api-feasibility-gate/GROWTH-CHECK-NARRATIVE.md
  - .planning/phases/01-ebay-api-feasibility-gate/FALLBACK-DECISION.md
autonomous: true
requirements: [SC-2, SC-3]
must_haves:
  truths:
    - "A standalone guide lets the user create an eBay dev account + keysets, populate .env, and submit the Growth Check independently of the execution flow (D-01, D-04)"
    - "A Claude-drafted Growth Check justification exists, framed as price-transparency/resale-analytics under the user's existing business entity (D-03, D-05)"
    - "A submission-tracking record captures the submitted date and a self-imposed decision-by date (SC-2)"
    - "A documented denied-case fallback decision exists: ship active-only v1, revisit sold-price later (SC-3)"
  artifacts:
    - path: ".planning/phases/01-ebay-api-feasibility-gate/MANUAL-STEPS.md"
      provides: "Ordered portal checklist: account signup, keyset creation, .env population, Growth Check submission, plus a submission-tracking table"
      contains: "Growth Check"
    - path: ".planning/phases/01-ebay-api-feasibility-gate/GROWTH-CHECK-NARRATIVE.md"
      provides: "Claude-drafted justification narrative the user pastes into the portal"
      contains: "business entity"
    - path: ".planning/phases/01-ebay-api-feasibility-gate/FALLBACK-DECISION.md"
      provides: "Committed record of the active-only-v1 fallback if MI access is denied"
      contains: "active-only"
  key_links:
    - from: ".planning/phases/01-ebay-api-feasibility-gate/MANUAL-STEPS.md"
      to: ".planning/phases/01-ebay-api-feasibility-gate/GROWTH-CHECK-NARRATIVE.md"
      via: "the Growth Check step in MANUAL-STEPS.md instructs the user to paste the text from GROWTH-CHECK-NARRATIVE.md"
      pattern: "GROWTH-CHECK-NARRATIVE"
---

<objective>
Produce the three Marketplace Insights (MI) access-gate documents that satisfy the phase's manual-process success criteria. Claude authors all three; the user performs the portal actions out-of-band.

Purpose: Resolve the "will sold-price data ever be available?" unknown as a go/no-go gate. Per D-01/D-04 the user needs a standalone `MANUAL-STEPS.md` to create the eBay account, keysets, and submit the Growth Check; per D-03/D-05 Claude drafts the justification narrative (which the user pastes and submits — Claude never submits on their behalf); per SC-3 a committed fallback decision must exist for the denied case.
Output: `MANUAL-STEPS.md`, `GROWTH-CHECK-NARRATIVE.md`, `FALLBACK-DECISION.md` in the phase directory.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ebay-api-feasibility-gate/01-CONTEXT.md
@.planning/phases/01-ebay-api-feasibility-gate/01-RESEARCH.md
@.planning/research/PITFALLS.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write MANUAL-STEPS.md (portal checklist + submission tracking)</name>
  <files>.planning/phases/01-ebay-api-feasibility-gate/MANUAL-STEPS.md</files>
  <read_first>
    - .planning/phases/01-ebay-api-feasibility-gate/01-CONTEXT.md (D-01 account creation is a manual user step done before OAuth code; D-04 standalone guide; D-02 user creates `.env` and pastes credentials themselves; D-06 submission not blocked on anything)
    - .planning/phases/01-ebay-api-feasibility-gate/01-RESEARCH.md ("Growth Check Submission Process" ordered steps; the portal URL `https://developer.ebay.com/my/support/tickets?tab=app-check`; the "read each field's live tooltip" guidance from Assumption A1/A3 and Open Question 1)
    - .planning/research/STACK.md ("Critical Finding First" — Browse API needs no approval; MI API is Limited Release behind the Application Growth Check)
  </read_first>
  <action>
    Write `MANUAL-STEPS.md` as an ordered, self-contained checklist the user works through in eBay's Developer Portal independently of the execution flow (D-04). Cover these sections in order:

    1. Create eBay Developer Program account (D-01) — sign up at developer.ebay.com; note this is a prerequisite for all other Phase 1 work and blocks the verification script's live run.
    2. Create Sandbox + Production keysets — instruct the user to obtain the Production keyset's Client ID and Client Secret (Browse API needs no special approval; Production keyset is required because Sandbox catalog data is sparse/synthetic and cannot satisfy SC-1/SC-4 — cite the Sandbox pitfall).
    3. Populate `.env` (D-02) — user copies `.env.example` to `.env` and pastes their real Client ID/Secret and `EBAY_ENV=production` themselves. State explicitly: never paste these values into chat, never commit `.env`.
    4. Submit the Marketplace Insights Application Growth Check (D-03) — navigate to `https://developer.ebay.com/my/support/tickets?tab=app-check`; fill every red-asterisk field, reading each field's live info-tooltip (do NOT hard-code an assumed field list — the exact fields were not confirmable this session, per Assumption A1/A3); use "Save as Draft" to paste in the text from `GROWTH-CHECK-NARRATIVE.md`; then click Submit. Note there is no published eBay SLA (D-06: submission itself satisfies SC-2; do not wait on approval).
    5. Have the existing business entity's name/details on hand (D-05) for the entity fields.

    Include a "Submission Tracking" table (SC-2) with fillable rows: `Submitted date`, `Ticket / reference ID`, `Self-imposed decision-by date` (guidance: ~14 days after submission since eBay publishes no SLA), and `Outcome (pending / approved / denied)`. Use obvious placeholders anywhere a credential could appear (threat T-01-03) — never real-looking secret strings.
  </action>
  <acceptance_criteria>
    - File exists and contains sections for account creation, keyset creation, `.env` population, and Growth Check submission
    - Contains the portal URL `developer.ebay.com/my/support/tickets`
    - Contains a Submission Tracking area with a self-imposed decision-by date field (SC-2)
    - States that `.env`/credentials are never pasted into chat and `.env` is never committed (D-02)
    - No real-looking credential strings appear (grep-review)
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/01-ebay-api-feasibility-gate/MANUAL-STEPS.md; test -f "$F" && grep -qi 'Growth Check' "$F" && grep -qi 'developer.ebay.com/my/support/tickets' "$F" && grep -qi 'decision-by' "$F" && grep -qi 'keyset' "$F"</automated>
  </verify>
  <done>MANUAL-STEPS.md is a standalone checklist covering account signup, keyset creation, `.env` population, and Growth Check submission, with a submission-tracking table that records the submitted date and a self-imposed decision-by date.</done>
</task>

<task type="auto">
  <name>Task 2: Draft GROWTH-CHECK-NARRATIVE.md (justification the user submits)</name>
  <files>.planning/phases/01-ebay-api-feasibility-gate/GROWTH-CHECK-NARRATIVE.md</files>
  <read_first>
    - .planning/phases/01-ebay-api-feasibility-gate/01-CONTEXT.md (D-03 Claude drafts, user submits; D-05 submit under existing business entity; D-06 don't block; the <specifics> price-transparency/resale-analytics framing requirement)
    - .planning/research/PITFALLS.md (Pitfall 1 — "personal project" framing correlates with denial; foreground business/commercial use case and existing entity)
    - .planning/phases/01-ebay-api-feasibility-gate/01-RESEARCH.md ("Growth Check Submission Process" recommendation: include price-transparency framing, existing business entity, realistic call volume ~10-30 products every few hours within the ~5,000/day default tier, read-only sold-listing scope only)
  </read_first>
  <action>
    Draft `GROWTH-CHECK-NARRATIVE.md` as ready-to-paste prose the user reviews and submits themselves (D-03 — Claude does not submit). The narrative MUST: (a) frame the product explicitly as a price-transparency / resale-analytics tool for the sealed Pokemon TCG market (NOT a "hobby"/"personal project" — that framing correlates with denial per PITFALLS.md Pitfall 1); (b) reference the applicant as the user's existing registered business entity (D-05), leaving a clearly-marked placeholder like `[YOUR BUSINESS ENTITY NAME]` for the user to fill; (c) state a realistic estimated call volume — a periodic worker polling ~10-30 catalog products every few hours, comfortably inside eBay's ~5,000 calls/day default tier; (d) confirm the app needs only read-only sold-listing data (no selling/user-token scope). Add a short lead-in note reminding the user this is a draft to paste into the Growth Check form (via "Save as Draft"), that they submit it (D-03), and that submission is not blocked on anything (D-06). Do not include any real credential values anywhere (threat T-01-03).
  </action>
  <acceptance_criteria>
    - File exists and frames the use case as price-transparency / resale-analytics (not "personal project")
    - References an existing business entity applicant with a fillable placeholder (D-05)
    - States a realistic call volume within the ~5,000/day default tier
    - States read-only sold-listing scope only
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/01-ebay-api-feasibility-gate/GROWTH-CHECK-NARRATIVE.md; test -f "$F" && grep -qi 'price-transparency\|resale' "$F" && grep -qi 'business entity' "$F" && grep -qi 'read-only' "$F"</automated>
  </verify>
  <done>A ready-to-paste justification narrative exists with price-transparency/resale-analytics framing, an existing-business-entity applicant placeholder, a realistic call-volume estimate, and read-only scope — for the user to submit (D-03).</done>
</task>

<task type="auto">
  <name>Task 3: Record FALLBACK-DECISION.md (denied-case decision)</name>
  <files>.planning/phases/01-ebay-api-feasibility-gate/FALLBACK-DECISION.md</files>
  <read_first>
    - .planning/phases/01-ebay-api-feasibility-gate/01-RESEARCH.md (SC-3 says this is already resolved at project level — formalize it as a phase artifact, do not re-derive)
    - .planning/research/STACK.md ("If Marketplace Insights is denied — fallback options (ranked)"; "Stack Patterns by Variant" — active-only v1, PriceCharting as an explicit scope conversation, source-agnostic matching design)
    - .planning/research/PITFALLS.md ("Recovery Strategies" — MI access denied/delayed: ship active-listing-only v1, revisit later)
    - .planning/ROADMAP.md (Phase 8 is the contingent sold-price phase; Phases 1-7 ship a complete active-price v1 independently)
  </read_first>
  <action>
    Write `FALLBACK-DECISION.md` formalizing the committed denied-case decision (SC-3): if the Marketplace Insights Growth Check is denied or indefinitely delayed, ship v1 as an active-listing-only product (Browse API current price + asking-price trend from repeated active snapshots) and revisit sold-price in a later phase/milestone (roadmap Phase 8, contingent). State the ranked fallbacks from STACK.md: (1) PriceCharting licensed paid API — flag that this changes the "official eBay APIs only" constraint and requires an explicit scope conversation with the user, not a silent substitution; (2) do-not-launch-sold-price-until-approved (the chosen default). Note the architectural implication carried into later phases: design the matching/normalization service source-agnostic so a future sold-data source plugs in without a rewrite. Keep it a decision record (what/why/implication), not a re-derivation of the research.
  </action>
  <acceptance_criteria>
    - File exists and states the active-only-v1 fallback with sold-price deferred to a later phase/milestone
    - Mentions PriceCharting as a licensed fallback requiring an explicit scope conversation
    - Notes the source-agnostic matching-design implication for later phases
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/01-ebay-api-feasibility-gate/FALLBACK-DECISION.md; test -f "$F" && grep -qi 'active-only\|active-listing' "$F" && grep -qi 'sold-price' "$F" && grep -qi 'PriceCharting' "$F"</automated>
  </verify>
  <done>FALLBACK-DECISION.md is a committed record stating the active-only-v1 fallback, the deferral of sold-price to a later phase, the PriceCharting scope-conversation caveat, and the source-agnostic design implication.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → `.env` | User pastes real OAuth Client ID/Secret into a local `.env`; must never enter git or chat (D-02) |
| Claude-authored docs → git | MANUAL-STEPS.md / GROWTH-CHECK-NARRATIVE.md are committed; must contain no real credential values |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03 | Information Disclosure | MANUAL-STEPS.md / GROWTH-CHECK-NARRATIVE.md | medium | mitigate | Any example credential uses obvious placeholders (`your-ebay-client-id-here`, `[YOUR BUSINESS ENTITY NAME]`); no real-looking secret strings; docs instruct user never to paste credentials into chat (D-02) |
| T-01-01 | Information Disclosure | `.env` handling instructions | high | mitigate | MANUAL-STEPS.md directs the user to copy `.env.example` → `.env` and never commit `.env` (relies on Plan 01 `.gitignore`) |
</threat_model>

<verification>
- All three files exist in the phase directory.
- MANUAL-STEPS.md covers account/keyset/`.env`/Growth Check and has a submission-tracking table with a decision-by date (SC-2).
- GROWTH-CHECK-NARRATIVE.md uses price-transparency/business-entity framing (D-03, D-05).
- FALLBACK-DECISION.md records the active-only-v1 fallback (SC-3).
- No real credential values appear in any file.
</verification>

<success_criteria>
SC-2 (Growth Check submission kit + tracking) and SC-3 (documented fallback) are satisfied as committed artifacts. D-01, D-03, D-04, D-05, D-06 are each reflected in the docs.
</success_criteria>

## Artifacts this phase produces

New symbols/files created across Phase 1 (for source-grounding / drift exclusion):
- Files: `.gitignore`, `.env.example`, `requirements.txt`, `scripts/ebay_client.py`, `scripts/verify_ebay_access.py`, `fixtures/ebay_listing_titles.json`, `.planning/phases/01-ebay-api-feasibility-gate/MANUAL-STEPS.md`, `.planning/phases/01-ebay-api-feasibility-gate/GROWTH-CHECK-NARRATIVE.md`, `.planning/phases/01-ebay-api-feasibility-gate/FALLBACK-DECISION.md`
- Env var names: `EBAY_CLIENT_ID`, `EBAY_CLIENT_SECRET`, `EBAY_ENV`
- Functions (Plan 03): `get_app_token`, `search_sealed_listings`, `total_cost`, `main`

**Created by THIS plan:** `MANUAL-STEPS.md`, `GROWTH-CHECK-NARRATIVE.md`, `FALLBACK-DECISION.md`.

<output>
Create `.planning/phases/01-ebay-api-feasibility-gate/01-02-SUMMARY.md` when done.
</output>
