---
phase: 01-ebay-api-feasibility-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - .env.example
  - requirements.txt
autonomous: true
requirements: [SC-1]
must_haves:
  truths:
    - "Real eBay OAuth credentials can be supplied via a gitignored .env that is never committed (D-02)"
    - "The project declares pinned Python dependencies (requests, python-dotenv) so the verification script has a reproducible runtime"
  artifacts:
    - path: ".gitignore"
      provides: "Prevents .env (and Python build cruft) from ever entering git history"
      contains: ".env"
    - path: ".env.example"
      provides: "Documents required env var names with placeholder values only"
      contains: "EBAY_CLIENT_ID"
    - path: "requirements.txt"
      provides: "Pinned dependency manifest for the verification script"
      contains: "requests==2.34.2"
  key_links:
    - from: ".env.example"
      to: "scripts/verify_ebay_access.py"
      via: "env var names (EBAY_CLIENT_ID / EBAY_CLIENT_SECRET / EBAY_ENV) the script reads via load_dotenv + os.environ"
      pattern: "EBAY_CLIENT_ID"
---

<objective>
Establish the greenfield Python project scaffolding and the secret-hygiene boundary that every other Phase 1 plan depends on: a `.gitignore` that keeps real credentials out of git, a `.env.example` that documents the required variable names without any real values, and a pinned `requirements.txt`.

Purpose: This is the foundation for D-02 (the user creates and owns `.env`; Claude never sees raw secret values) and for security control V6 (secrets handling). The `.env.example` variable names are the interface contract the verification script (Plan 03) and its live run (Plan 04) consume.
Output: `.gitignore`, `.env.example`, `requirements.txt` at the repo root.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ebay-api-feasibility-gate/01-CONTEXT.md
@.planning/phases/01-ebay-api-feasibility-gate/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .gitignore and .env.example (secret-hygiene boundary)</name>
  <files>.gitignore, .env.example</files>
  <read_first>
    - .planning/phases/01-ebay-api-feasibility-gate/01-RESEARCH.md (see "Recommended Project Structure", "Security Domain" V6, and the Known Threat Patterns table — the `.gitignore must include .env before any code is written` rule)
    - .planning/phases/01-ebay-api-feasibility-gate/01-CONTEXT.md (D-02: user creates `.env` themselves; credentials never pasted into chat)
  </read_first>
  <action>
    Create `.gitignore` at the repo root (currently none exists) that ignores at least: `.env`, `__pycache__/`, `*.pyc`, `venv/`, `.venv/`, and `*.egg-info/`. Do NOT ignore `fixtures/` or `.env.example` — the fixtures file is a committed deliverable for Phase 4, and `.env.example` is a committed template. This satisfies threat T-01-01 (D-02).

    Create `.env.example` at the repo root documenting exactly three variables the verification script reads: `EBAY_CLIENT_ID`, `EBAY_CLIENT_SECRET`, and `EBAY_ENV`. Every value MUST be an obvious placeholder (for example `your-ebay-client-id-here`, `your-ebay-client-secret-here`, and `production` for `EBAY_ENV`) — never a real-looking secret string (threat T-01-03). Add a short comment line noting `EBAY_ENV` accepts `production` or `sandbox`, and that the real values are pasted by the user into a local `.env` (never committed, never shared in chat, per D-02). These three names are the interface contract that Plan 03's `scripts/verify_ebay_access.py` consumes via `load_dotenv()` + `os.environ`.
  </action>
  <acceptance_criteria>
    - `git check-ignore .env` prints `.env` (rule present even though `.env` does not exist yet)
    - `.gitignore` does NOT contain a `fixtures` ignore line
    - `.env.example` exists and contains the literal names `EBAY_CLIENT_ID`, `EBAY_CLIENT_SECRET`, `EBAY_ENV`
    - `.env.example` values are placeholders — grep-review confirms no real-looking credential strings
  </acceptance_criteria>
  <verify>
    <automated>git check-ignore .env && ! grep -q 'fixtures' .gitignore && grep -q 'EBAY_CLIENT_ID' .env.example && grep -q 'EBAY_CLIENT_SECRET' .env.example && grep -q 'EBAY_ENV' .env.example</automated>
  </verify>
  <done>`.env` is git-ignored, `fixtures/` is not ignored, and `.env.example` documents all three EBAY_* variable names with placeholder-only values.</done>
</task>

<task type="auto">
  <name>Task 2: Create pinned requirements.txt</name>
  <files>requirements.txt</files>
  <read_first>
    - .planning/phases/01-ebay-api-feasibility-gate/01-RESEARCH.md (see "Package Legitimacy Audit" — current latest pins are `requests==2.34.2` and `python-dotenv==1.2.2`; the STACK.md pins 2.32.3 / 1.0.1 are stale training-data drift and must NOT be used)
  </read_first>
  <action>
    Create `requirements.txt` at the repo root pinning exactly two dependencies: `requests==2.34.2` and `python-dotenv==1.2.2`. Do not add Flask, PyMongo, APScheduler, or any other stack library — Phase 1 is a feasibility spike whose only code path is the OAuth + Browse API smoke script; those belong to later phases. Do not run `pip install` here — installation is gated behind the package-legitimacy checkpoint in Plan 04 (threat T-01-SC).
  </action>
  <acceptance_criteria>
    - `requirements.txt` contains the exact line `requests==2.34.2`
    - `requirements.txt` contains the exact line `python-dotenv==1.2.2`
    - No other non-comment dependency lines are present
  </acceptance_criteria>
  <verify>
    <automated>grep -q '^requests==2.34.2$' requirements.txt && grep -q '^python-dotenv==1.2.2$' requirements.txt && [ "$(grep -vcE '^\s*(#|$)' requirements.txt)" -eq 2 ]</automated>
  </verify>
  <done>`requirements.txt` pins `requests==2.34.2` and `python-dotenv==1.2.2` and nothing else.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → `.env` | User pastes real OAuth Client ID/Secret into a local `.env`; these must never enter git or chat (D-02) |
| developer machine → PyPI | Python packages are downloaded from PyPI (supply-chain surface; install gated in Plan 04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `.gitignore` / `.env` | high | mitigate | `.gitignore` ignores `.env` before any code is written; only `.env.example` (placeholders) is committed (D-02, ASVS V6) |
| T-01-03 | Information Disclosure | `.env.example` | medium | mitigate | Only placeholder values (`your-ebay-client-id-here`) appear in `.env.example`; no real-looking secret strings |
| T-01-SC | Tampering | pip install (requests, python-dotenv) | high | mitigate | requirements.txt only declares pins here; the actual install is gated behind a blocking-human package-legitimacy checkpoint in Plan 04 |
</threat_model>

<verification>
- `git check-ignore .env` confirms `.env` is ignored.
- `.env.example` documents `EBAY_CLIENT_ID`, `EBAY_CLIENT_SECRET`, `EBAY_ENV` with placeholder values only.
- `requirements.txt` pins `requests==2.34.2` and `python-dotenv==1.2.2`.
- No real credential values appear in any committed file.
</verification>

<success_criteria>
Secret-hygiene boundary and dependency manifest exist so that the verification script (Plan 03) can be authored against a stable env-var contract and installed safely (Plan 04). Directly enables SC-1 (a live OAuth + Browse API call needs credentials supplied without leaking them).
</success_criteria>

## Artifacts this phase produces

New symbols/files created across Phase 1 (for source-grounding / drift exclusion):
- Files: `.gitignore`, `.env.example`, `requirements.txt`, `scripts/ebay_client.py`, `scripts/verify_ebay_access.py`, `fixtures/ebay_listing_titles.json`, `.planning/phases/01-ebay-api-feasibility-gate/MANUAL-STEPS.md`, `.planning/phases/01-ebay-api-feasibility-gate/GROWTH-CHECK-NARRATIVE.md`, `.planning/phases/01-ebay-api-feasibility-gate/FALLBACK-DECISION.md`
- Env var names: `EBAY_CLIENT_ID`, `EBAY_CLIENT_SECRET`, `EBAY_ENV`
- Functions (Plan 03): `get_app_token`, `search_sealed_listings`, `total_cost`, `main`
- Module constants (Plan 03): `CATALOG_QUERIES`, `FIXTURE_TARGET`, `FIXTURE_PATH`

**Created by THIS plan:** `.gitignore`, `.env.example`, `requirements.txt`; env var names `EBAY_CLIENT_ID`, `EBAY_CLIENT_SECRET`, `EBAY_ENV`.

<output>
Create `.planning/phases/01-ebay-api-feasibility-gate/01-01-SUMMARY.md` when done.
</output>
