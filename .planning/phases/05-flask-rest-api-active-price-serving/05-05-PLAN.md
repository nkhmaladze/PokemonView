---
phase: 05-flask-rest-api-active-price-serving
plan: 05
type: execute
wave: 4
depends_on: [05-04]
files_modified:
  - api/services/catalog_service.py
autonomous: true
requirements: [SEARCH-01, SEARCH-02, PRICE-01]
must_haves:
  truths:
    - "list_products(db, filters) filters the ≤16-product catalog IN PROCESS (never a Mongo query built from raw user text) supporting combinable ?set / ?product_type / free-text ?q against display_name/set_name (D-08)"
    - "list_products raises ValueError on a non-None product_type outside the fixed enum {booster_pack, booster_box, etb, booster_bundle} (V5 input validation)"
    - "Default (no-sort-param) order is grouped by set in the hardcoded SET_ORDER (Pitch Black → Chaos Rising → Perfect Order → Ascended Heroes), NOT a raw release_date sort (D-10)"
    - "verified:false products (Pitch Black) appear in browse/search identically to verified products — no exclusion, no extra badge field (D-09)"
    - "Each listed product carries current_price {total_price headline, item_price secondary, as_of, listing_count} or price_status 'no_data_yet' with current_price null (D-01/D-04/D-12)"
    - "get_product_detail(db, id) assembles catalog metadata + current_price + trend_7d/trend_30d only — NO raw price_points array/series (D-11) — and returns None when the id is unknown (→404)"
    - "Trend objects are {pct_change, status} where status is 'ok' with a number or 'insufficient_data' with null (D-06), computed on total_price only (D-07)"
  prohibitions:
    - statement: "list_products / get_product_detail must NOT build a MongoDB query operator from the raw q / set / product_type user input — filtering is in-process Python only"
      status: resolved
      verification: "list_products fetches db.products.find({}) and filters in Python (positive source check); test_list_free_text_search + test_product_type_enum_validation pass; injection surface is closed by construction"
    - statement: "get_product_detail must NOT include a raw price_points series/array in its response (D-11)"
      status: resolved
      verification: "test_detail_omits_raw_series asserts no value in the detail dict is a list of point-shaped dicts"
  key_links:
    - "catalog_service composes price_service.get_current_price + get_trend_baseline + compute_pct_change (Plan 05-04) into the list/detail response shape"
    - "the returned dicts are jsonify-ready primitives (ISO date strings) consumed directly by the products blueprint (Plan 05-06)"
---

<objective>
Implement `api/services/catalog_service.py` — `list_products` (browse/search/filter) and `get_product_detail` (single-product assembly) — turning the RED `tests/test_catalog_service.py` (Plan 05-03) GREEN.

Purpose: This is the catalog-facing serving logic enabling SEARCH-01 (browse/search by name and set), SEARCH-02 (detail page data), and PRICE-01 (total-led current price). It composes Plan 05-04's price functions into the locked list/detail response shape and enforces the phase's V5 input-validation control (product_type enum whitelist) plus the in-process-filtering mitigation that closes the regex/NoSQL-injection surface (T-05-01). Follows the "pure, db-first service functions, no top-level side effects" pattern from scripts/matching.py.

Output: `api/services/catalog_service.py` with `list_products`, `get_product_detail`, and the `SET_ORDER` / `VALID_PRODUCT_TYPES` / `TREND_WINDOWS` constants; `tests/test_catalog_service.py` GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-flask-rest-api-active-price-serving/05-CONTEXT.md
@.planning/phases/05-flask-rest-api-active-price-serving/05-RESEARCH.md
@.planning/phases/05-flask-rest-api-active-price-serving/05-PATTERNS.md
@scripts/matching.py
@scripts/catalog_data.py
@db/init_collections.py
@api/services/price_service.py
@tests/test_catalog_service.py
</context>

## Artifacts this phase produces (this plan's slice)

- `api/services/catalog_service.py` functions: `list_products(db, filters) -> list[dict]`, `get_product_detail(db, product_id) -> dict | None`, plus an internal serialization helper.
- Module constants: `SET_ORDER = ["Pitch Black", "Chaos Rising", "Perfect Order", "Ascended Heroes"]` (D-10), `VALID_PRODUCT_TYPES = {"booster_pack", "booster_box", "etb", "booster_bundle"}` (V5), `TREND_WINDOWS = (7, 30)` (D-05).
- Response field names (Claude's discretion, locked by Plan 05-03's contract): `id`, `set_name`, `product_type`, `display_name`, `release_date`, `msrp`, `image_url`, `verified`, `price_status`, `current_price{total_price,item_price,as_of,listing_count}`, `trend_7d{pct_change,status}`, `trend_30d{pct_change,status}`.

## Documented Claude's-discretion decisions (D-10 leaves in-set order unspecified)

- **In-set secondary sort key:** `msrp` descending (None last) — higher-value products lead within a set, matching the poe.ninja-style reference (PROJECT.md) and 05-RESEARCH.md Open Question 2's "either is reasonable; pick one and document it." This is NOT a silent choice; it is recorded here.
- **Structured `?set` / `?product_type` filters:** exact match on the stored `set_name` / `product_type` value. Free-text `?q` is case-insensitive substring against `display_name` and `set_name`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: constants + list_products (D-08 filters, V5 enum, D-10 sort, D-09, D-04/D-12 price)</name>
  <files>api/services/catalog_service.py</files>
  <behavior>
    - list_products(db, {"set_name": "Perfect Order"}) → only Perfect Order products; {"product_type": "etb"} → only ETBs; {"q": "chaos"} → only Chaos Rising products (case-insensitive); combined set+type → the AND intersection (D-08).
    - list_products(db, {"product_type": "not_a_type"}) → raises ValueError (V5); each valid enum value does not raise.
    - list_products(db, {}) → all 16 products grouped by set in SET_ORDER order (Pitch Black first, Ascended Heroes last), in-set by msrp descending (D-10).
    - Pitch Black (verified:false) products present in the unfiltered list with identical field shape to verified products (D-09).
    - A product with a price_point carries current_price{total_price, item_price, as_of, listing_count} and price_status "ok"; a product with no points carries current_price null and price_status "no_data_yet" (D-01/D-04/D-12).
  </behavior>
  <read_first>
    - tests/test_catalog_service.py (the RED assertions this task turns green: test_list_products_filters, test_default_sort_order, test_unverified_products_shown_identically, test_list_includes_price_and_freshness, test_product_type_enum_validation)
    - api/services/price_service.py (get_current_price signature + returned doc shape — reused to build each product's current_price summary)
    - scripts/catalog_data.py (the 16 products: exact set_name strings, product_type values, msrp values, display_name text used for q-search assertions; confirms all four sets and the booster_bundle distinct type)
    - scripts/seed_catalog.py (the `_id` slug convention `f"{set_name}_{product_type}".lower().replace(" ", "-")` → this is the `id` field returned; release_date stored as a datetime)
    - .planning/phases/05-flask-rest-api-active-price-serving/05-RESEARCH.md (Don't-Hand-Roll: in-process Python filtering at ≤16 products, never a $regex from raw q; Pitfall 1: hardcoded SET_ORDER, never a release_date sort; Anti-Patterns section; Architectural Responsibility Map — filter logic is API/service tier)
    - .planning/phases/05-flask-rest-api-active-price-serving/05-CONTEXT.md (D-04, D-08, D-09, D-10, D-12)
    - .planning/phases/05-flask-rest-api-active-price-serving/05-PATTERNS.md (Decision-ID-cited constants; db-first pure-function style; guard-clause/explicit-state-over-null)
  </read_first>
  <action>
    Create `api/services/catalog_service.py` with a module docstring stating it is a pure, DB-free-on-import read-only service (no MongoClient, no os.environ, no Flask import at module level — mirrors scripts/matching.py). Import `get_current_price` from `api.services.price_service`.

    Define constants with decision-ID comments: `SET_ORDER = ["Pitch Black", "Chaos Rising", "Perfect Order", "Ascended Heroes"]  # D-10`, `VALID_PRODUCT_TYPES = {"booster_pack", "booster_box", "etb", "booster_bundle"}  # V5 enum`, `TREND_WINDOWS = (7, 30)  # D-05`.

    Add an internal helper `_product_summary(db, product) -> dict` that builds the jsonify-ready base dict: `id` = product["_id"]; pass through set_name, product_type, display_name, msrp, image_url, verified; `release_date` = `product["release_date"].date().isoformat()` when not None else None; then call `get_current_price(db, product["_id"])` and, if a point exists, set `price_status` = "ok" and `current_price` = `{"total_price": doc["total_price"], "item_price": doc["item_price"], "as_of": doc["ts"].isoformat(), "listing_count": doc.get("listing_count")}` (D-04/D-12/D-03; listing_count is None for pre-Plan-05-02 points); else `price_status` = "no_data_yet" and `current_price` = None (D-01). verified:false products get NO special handling here (D-09).

    Implement `list_products(db, filters) -> list[dict]`:
      1. V5 validation: if `filters.get("product_type")` is not None and not in `VALID_PRODUCT_TYPES`, raise `ValueError` (the blueprint maps this to HTTP 400). Do this BEFORE any query.
      2. Fetch the whole small catalog once: `products = list(db.products.find({}))` (≤16 docs — no filter operators built from user text).
      3. In-process filter (combinable, AND — D-08): keep a product when — set_name filter absent or `product["set_name"] == filters["set_name"]` (exact); AND product_type filter absent or `product["product_type"] == filters["product_type"]` (exact); AND q absent or the lowercased q is a substring of `display_name.lower()` OR `set_name.lower()` (case-insensitive free text). Never pass `q`/`set`/`product_type` into a MongoDB query operator (no regular-expression or where-clause matching built from raw user text) — this in-process Python approach is the T-05-01 mitigation.
      4. Sort (D-10): primary key `SET_ORDER.index(product["set_name"])`; secondary key `msrp` descending with None treated as lowest (e.g. sort by `(SET_ORDER.index(set_name), -(msrp if msrp is not None else -1))` or an equivalent stable two-pass sort). Do NOT sort by release_date (Pitfall 1 — staggered per-product dates would interleave sets).
      5. Return `[_product_summary(db, p) for p in sorted_products]`.
  </action>
  <verify>
    <automated>pytest tests/test_catalog_service.py::test_list_products_filters tests/test_catalog_service.py::test_default_sort_order tests/test_catalog_service.py::test_unverified_products_shown_identically tests/test_catalog_service.py::test_list_includes_price_and_freshness tests/test_catalog_service.py::test_product_type_enum_validation -x</automated>
  </verify>
  <acceptance_criteria>
    - The five named tests pass (exit 0), including test_list_free_text_search and test_product_type_enum_validation (the T-05-01 mitigation proven behaviorally).
    - `python -c "import api.services.catalog_service as c; print(c.SET_ORDER)"` prints `['Pitch Black', 'Chaos Rising', 'Perfect Order', 'Ascended Heroes']`.
    - `python -c "import inspect, api.services.catalog_service as c; src=inspect.getsource(c.list_products); assert 'find({})' in src; print('ok')"` prints `ok` (the whole ≤16-product catalog is fetched once and filtered in Python — no query operator built from user text; T-05-01 mitigation by construction).
    - `python -c "import api.services.catalog_service as c" ` performs no MongoDB connection at import (`grep -nE 'MongoClient|os\\.environ|from flask' api/services/catalog_service.py` returns nothing).
    - A ValueError is raised for an invalid product_type: `pytest tests/test_catalog_service.py::test_product_type_enum_validation -x` passes.
  </acceptance_criteria>
  <done>list_products filters in-process (D-08), validates the product_type enum (V5), sorts by hardcoded SET_ORDER then msrp desc (D-10), shows verified:false identically (D-09), and attaches a total-led current_price or no_data_yet status per product (D-01/D-04/D-12); the five named tests are GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: get_product_detail — current price + 7d/30d trend, no raw series (D-01/D-05/D-06/D-07/D-11)</name>
  <files>api/services/catalog_service.py</files>
  <behavior>
    - get_product_detail(db, "perfect-order_booster_box") for a priced product → the _product_summary fields PLUS trend_7d and trend_30d objects; current_price total-led (D-12).
    - Each trend is {"pct_change": <float>, "status": "ok"} when a baseline exists in tolerance, else {"pct_change": None, "status": "insufficient_data"} (D-06); trend uses total_price only (D-07).
    - get_product_detail for a seeded product with zero points → price_status "no_data_yet", current_price None, both trends insufficient_data — NOT None-the-whole-response, NOT a 404 (D-01).
    - get_product_detail(db, "does-not-exist") → None (blueprint returns 404).
    - The returned dict contains NO array/list of price_points (D-11).
  </behavior>
  <read_first>
    - api/services/catalog_service.py (current state after Task 1 — reuse `_product_summary`; append get_product_detail)
    - api/services/price_service.py (get_current_price, get_trend_baseline, compute_pct_change signatures and semantics from Plan 05-04)
    - tests/test_catalog_service.py (test_detail_current_price_total_led, test_detail_no_data_yet, test_detail_unknown_id_returns_none, test_detail_omits_raw_series)
    - .planning/phases/05-flask-rest-api-active-price-serving/05-CONTEXT.md (D-01 no_data_yet, D-05 baseline vs current-price ts, D-06 insufficient-data explicit, D-07 total_price only, D-11 no raw series)
    - .planning/phases/05-flask-rest-api-active-price-serving/05-PATTERNS.md (the orchestration-function pattern: get_product_detail assembles a products lookup + get_current_price + two get_trend_baseline calls + compute_pct_change into one dict, mirroring run_matching_once's assembly)
    - the "## API Response Contract" section of Plan 05-03 (the exact detail shape)
  </read_first>
  <action>
    Append `get_product_detail(db, product_id) -> dict | None` to `api/services/catalog_service.py`:
      1. `product = db.products.find_one({"_id": product_id})`; if `product is None` return `None` (blueprint → 404).
      2. Build the base via `detail = _product_summary(db, product)` (reuses Task 1's helper, giving the catalog fields + current_price + price_status).
      3. Compute trend for each window in `TREND_WINDOWS`: if `detail["current_price"]` is None (no data — D-01), both trends are `{"pct_change": None, "status": "insufficient_data"}`. Otherwise, re-read the current point once via `get_current_price(db, product_id)` (or reuse the value already fetched) to get its real `ts` and `total_price`; for each `days` in (7, 30): `baseline = get_trend_baseline(db, product_id, current_ts, days)`; if `baseline` is not None, `pct = compute_pct_change(current_total, baseline["total_price"])`; when `pct is not None` set `{"pct_change": pct, "status": "ok"}` else `{"pct_change": None, "status": "insufficient_data"}` (D-06/covers baseline total 0); when `baseline` is None set `{"pct_change": None, "status": "insufficient_data"}` (D-06). Trend uses total_price only (D-07). The baseline target is measured from the CURRENT price point's ts (D-05).
      4. Attach `detail["trend_7d"]` and `detail["trend_30d"]` (map the 7/30 windows to these keys). Do NOT attach any raw price_points list/series (D-11) — only the single current_price object and the two trend objects.
      5. Return `detail`.
  </action>
  <verify>
    <automated>pytest tests/test_catalog_service.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_catalog_service.py -x` passes fully GREEN (all catalog_service unit tests including the four detail tests).
    - `python -c "import api.services.catalog_service as c; print(hasattr(c,'get_product_detail'))"` prints `True`.
    - test_detail_omits_raw_series passes — the detail dict contains no value that is a list of price_points documents (D-11).
    - test_detail_no_data_yet passes — a seeded product with zero points returns price_status "no_data_yet" + current_price None + both trends insufficient_data (not None, not 404).
    - test_detail_unknown_id_returns_none passes — get_product_detail returns None for an unknown id.
  </acceptance_criteria>
  <done>get_product_detail assembles catalog metadata + current price + 7d/30d trend (total-price-only, insufficient-data-explicit) with no raw series (D-11), returns no_data_yet for unpriced products (D-01) and None for unknown ids; the full catalog_service unit-test file is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API (q/set/product_type reaching catalog_service via the blueprint) | Untrusted user input is filtered here; the mitigation is in-process filtering + enum validation, never a Mongo query operator from raw text |
| catalog_service → products / price_points | Read-only queries; find({}) fetches the whole small catalog, find_one by internal slug _id |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | q / set / product_type query params flowing into a query | medium | mitigate | In-process Python filtering over `products.find({})` — no `$regex`/`$where` ever built from raw user text (Don't-Hand-Roll); product_type validated against `VALID_PRODUCT_TYPES` whitelist before use (V5). Verified by grep (no $regex) + test_product_type_enum_validation + test_list_free_text_search |
| T-05-05 | Information Disclosure | detail response leaking raw price history | low | mitigate | get_product_detail returns only current_price + trend numbers, never a raw price_points series (D-11); test_detail_omits_raw_series enforces it |

Security note: `security_block_on: high` — no high/critical threats; the medium input-validation threat T-05-01 is mitigated by construction (in-process filtering) and by the enum whitelist. The request-boundary 400/404/500 translation and the global error handler are delivered in Plan 05-06.
</threat_model>

<verification>
- `pytest tests/test_catalog_service.py -x` fully GREEN.
- `pytest tests/test_price_service.py -x` still GREEN (no regression).
- In-process filtering is proven by `list_products` calling `db.products.find({})` (positive check above) plus the passing free-text/enum security tests — no Mongo query operator is built from user text.
- `pytest tests/test_api_products.py -x` still RED (routes/app not built yet — Plan 05-06).
</verification>

<success_criteria>
catalog_service.py implements in-process browse/search/filter with enum validation and SET_ORDER sorting (SEARCH-01/D-08/D-09/D-10), and detail assembly with total-led current price + 7d/30d trend and no raw series (SEARCH-02/PRICE-01/D-01/D-05/D-06/D-07/D-11/D-12); the catalog_service unit-test file is GREEN.
</success_criteria>

<output>
Create `.planning/phases/05-flask-rest-api-active-price-serving/05-05-SUMMARY.md` when done.
</output>
