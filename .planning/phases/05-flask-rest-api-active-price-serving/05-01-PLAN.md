---
phase: 05-flask-rest-api-active-price-serving
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
autonomous: false
requirements: [SEARCH-01, SEARCH-02, PRICE-01, PRICE-02, PRICE-03]
must_haves:
  truths:
    - "`import flask` succeeds and reports version 3.1.3; `import flask_cors` succeeds and reports version 6.0.5"
    - "requirements.txt pins flask==3.1.3 and flask-cors==6.0.5 using the project's exact-pin (`==`) convention, appended after rapidfuzz==3.14.5"
    - "The [SUS]/unknown-downloads verdict on flask and flask-cors was reviewed and approved by a human before install (T-05-SC)"
    - "pytest-flask is deliberately NOT installed (the hand-rolled 2-fixture pattern is used instead — 05-RESEARCH.md Alternatives Considered)"
    - "gunicorn is deliberately NOT installed this phase (it is a Phase 7 launch/hardening production-WSGI concern — 05-RESEARCH.md)"
  artifacts:
    - "requirements.txt (flask==3.1.3 and flask-cors==6.0.5 appended after rapidfuzz==3.14.5)"
  key_links:
    - "flask + flask_cors become importable by api/app.py's create_app() (Plan 05-06) and by the app/client test fixtures (Plan 05-03)"
---

<objective>
Install `flask==3.1.3` and `flask-cors==6.0.5` — the REST-API framework and CORS middleware 05-RESEARCH.md's Standard Stack pins for this phase — gated behind a blocking human legitimacy checkpoint, exactly as `rapidfuzz`/`apscheduler`/`pymongo` were handled in prior phases.

Purpose: Phase 5 owns no requirement IDs directly; it is the enabling serving layer that makes SEARCH-01, SEARCH-02, PRICE-01, PRICE-02, and PRICE-03 servable (they become user-observable in Phase 6). flask and flask-cors are hard dependencies of every later plan in this phase — `create_app()`, the blueprints, and the Flask test-client fixtures cannot exist without them. Both carry a `[SUS]` legitimacy verdict caused solely by the legitimacy tool failing to resolve a download-count telemetry signal in this environment — the same known false-positive class already reviewed and approved for `rapidfuzz`, `apscheduler`, and `pymongo`. Protocol still requires a blocking human verification before install.

Output: `flask==3.1.3` and `flask-cors==6.0.5` pinned in `requirements.txt` and installed/importable.

Note on version discrepancy: CLAUDE.md's STACK.md table lists Flask-CORS 5.x, but 05-RESEARCH.md registry-verified the current latest as 6.0.5 via `pip index versions flask-cors`. Per this project's established precedent (STATE.md: "Pinned requests==2.34.2 / python-dotenv==1.2.2 per research findings, not stale STACK.md versions") the registry-verified 6.0.5 is used, and this discrepancy is noted here rather than silently defaulting to the stale figure.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-flask-rest-api-active-price-serving/05-RESEARCH.md
@requirements.txt
</context>

## Artifacts this phase produces (this plan's slice)

- `requirements.txt` — two new pinned dependency lines: `flask==3.1.3` and `flask-cors==6.0.5`.
- No new Python symbols; this plan only makes the `flask` and `flask_cors` packages importable for later plans.

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate — flask==3.1.3 and flask-cors==6.0.5</name>
  <read_first>
    - .planning/phases/05-flask-rest-api-active-price-serving/05-RESEARCH.md (the `## Package Legitimacy Audit` section — the flask and flask-cors rows, both `[SUS]`/`unknown-downloads`, both "Approved with the same reasoning" as prior phases; and the note that the postinstall-script check is Node.js-specific and N/A for this Python project)
    - .planning/STATE.md (Accumulated Context — the prior [Phase 02]/[Phase 03]/[Phase 04] entries approving pymongo, apscheduler, and rapidfuzz after human review confirmed the identical [SUS] unknown-downloads false positive)
  </read_first>
  <action>
    Do NOT install anything yet. Present the `flask==3.1.3` and `flask-cors==6.0.5` legitimacy assessment to the human and BLOCK until an explicit approve/reject decision is given (T-05-SC). Per the Package Legitimacy Audit: registry = PyPI; canonical source repos = `github.com/pallets/flask` (Pallets' flagship framework, explicitly locked by PROJECT.md) and `github.com/corydolphin/flask-cors` (actively maintained); verdict = `[SUS]` for reason `unknown-downloads` ONLY (the legitimacy tool could not retrieve download stats in this environment — a telemetry gap, not a supply-chain signal); Python wheels ship prebuilt with no npm-style postinstall hook / no arbitrary install-time code execution. This gate is never auto-approvable — `workflow.auto_advance` is ignored for package-legitimacy checkpoints. Record the approve/reject decision in the plan SUMMARY (mirroring the rapidfuzz/apscheduler/pymongo approval notes in STATE.md). Only on approval does Task 2 proceed.
  </action>
  <what-built>
    Nothing is installed yet. This checkpoint presents the `flask==3.1.3` and `flask-cors==6.0.5` legitimacy assessment for human sign-off before any `pip install` runs.
  </what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/Flask/3.1.3/ and confirm the release exists, is published by the Pallets maintainers, and links to `github.com/pallets/flask`.
    2. Open https://pypi.org/project/Flask-Cors/6.0.5/ and confirm the release exists and links to `github.com/corydolphin/flask-cors`.
    3. Confirm this is the same `[SUS]`/`unknown-downloads` false-positive class already approved in this project for `rapidfuzz`, `apscheduler`, and `pymongo` (see STATE.md entries in the read_first).
    4. Approve to proceed with `pip install flask==3.1.3 flask-cors==6.0.5`, or reject and stop the phase.
  </how-to-verify>
  <acceptance_criteria>
    - A human has explicitly approved the flask + flask-cors install (or rejected it). This checkpoint is NOT auto-approvable — `workflow.auto_advance` is ignored for package-legitimacy gates.
    - The approval decision is recorded in the plan SUMMARY (mirroring the rapidfuzz/apscheduler/pymongo approval notes in STATE.md).
  </acceptance_criteria>
  <verify>Human types "approved" (or rejects); the decision is recorded in 05-01-SUMMARY.md before Task 2 runs.</verify>
  <resume-signal>Type "approved" to proceed with the install, or describe the concern to halt.</resume-signal>
  <done>A human has explicitly approved (or rejected) the flask==3.1.3 and flask-cors==6.0.5 install, and the decision is recorded in the SUMMARY. No package was installed during this task.</done>
</task>

<task type="auto">
  <name>Task 2: Pin and install flask==3.1.3 and flask-cors==6.0.5</name>
  <files>requirements.txt</files>
  <read_first>
    - requirements.txt (current 6 pinned lines — append, do not reorder or re-pin existing entries)
    - .planning/phases/05-flask-rest-api-active-price-serving/05-RESEARCH.md (`## Standard Stack` → Installation block confirming `flask==3.1.3` and `flask-cors==6.0.5` are the registry-latest as of 2026-07-15; the "skip pytest-flask" and "gunicorn is out of scope for Phase 5" notes)
  </read_first>
  <action>
    Append the two exact lines `flask==3.1.3` and `flask-cors==6.0.5` to `requirements.txt` as new final lines, using the same exact-version-pin style (`==`) as the existing six lines (`requests==2.34.2`, `python-dotenv==1.2.2`, `pymongo==4.17.0`, `pytest==8.4.2`, `apscheduler==3.11.3`, `rapidfuzz==3.14.5`). Do not modify or reorder any existing pin. Do NOT add `pytest-flask` (05-RESEARCH.md recommends the hand-rolled 2-fixture pattern over the plugin) and do NOT add `gunicorn` (a Phase 7 production-WSGI concern, not needed for `flask run`/`app.test_client()`). Then run `pip install flask==3.1.3 flask-cors==6.0.5` (equivalently `pip install -r requirements.txt`) in the same interpreter environment the existing dependencies are installed into (the one `pytest` runs under). This proceeds ONLY after Task 1's human approval of the T-05-SC supply-chain gate.
  </action>
  <verify>
    <automated>python -c "import flask, flask_cors; print(flask.__version__, flask_cors.__version__)"</automated>
  </verify>
  <acceptance_criteria>
    - `requirements.txt` contains a line reading exactly `flask==3.1.3` and a line reading exactly `flask-cors==6.0.5`.
    - `python -c "import flask, flask_cors; print(flask.__version__, flask_cors.__version__)"` prints `3.1.3 6.0.5` and exits 0.
    - `python -c "from flask import Flask, Blueprint, jsonify, request, current_app; from flask_cors import CORS; print('ok')"` prints `ok` and exits 0 (the exact symbols Plans 05-04/05/06 depend on).
    - `python -c "import importlib.util; print(importlib.util.find_spec('pytest_flask') is None)"` prints `True` (pytest-flask was NOT installed).
  </acceptance_criteria>
  <done>flask==3.1.3 and flask-cors==6.0.5 are pinned in requirements.txt and importable, exposing Flask/Blueprint/jsonify/request/current_app and CORS; pytest-flask and gunicorn are absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI registry → local environment | External packages (flask, flask-cors) are downloaded and installed into the environment that will run the API |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-SC | Tampering | `pip install flask==3.1.3 flask-cors==6.0.5` | high | mitigate | Blocking human legitimacy checkpoint (Task 1, `gate="blocking-human"`, never auto-approvable) before install; verified against pypi.org/project pages + github.com/pallets/flask and github.com/corydolphin/flask-cors; prebuilt Python wheels have no postinstall code-execution hook |

Security note: `security_block_on: high` — T-05-SC is high and is mitigated by the blocking checkpoint, so this plan clears the gate only after human approval.
</threat_model>

<verification>
- `requirements.txt` diff adds exactly two lines (`flask==3.1.3`, `flask-cors==6.0.5`), no other lines changed.
- `python -c "import flask, flask_cors; print(flask.__version__, flask_cors.__version__)"` → `3.1.3 6.0.5`.
- Task 1 human approval recorded in SUMMARY.
</verification>

<success_criteria>
flask==3.1.3 and flask-cors==6.0.5 are pinned and importable; the [SUS] verdict was human-reviewed and approved before install; pytest-flask and gunicorn were intentionally not installed.
</success_criteria>

<output>
Create `.planning/phases/05-flask-rest-api-active-price-serving/05-01-SUMMARY.md` when done.
</output>
