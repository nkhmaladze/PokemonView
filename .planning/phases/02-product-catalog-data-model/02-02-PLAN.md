---
phase: 02-product-catalog-data-model
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
autonomous: false
requirements: [CATALOG-01, CATALOG-02]
user_setup:
  - service: mongodb
    why: "Shared data store for the products catalog and reserved price_points time-series collection; 02-RESEARCH.md Environment Availability found no MongoDB instance (mongod/mongosh/Docker) on this machine"
    env_vars:
      - name: MONGODB_URI
        source: "MongoDB Atlas Dashboard -> Cluster -> Connect -> Drivers (Python) connection string, OR a local mongodb://localhost:27017 URI if provisioning via Homebrew"

must_haves:
  truths:
    - "A MongoDB instance (Atlas M0 or local) is reachable via the MONGODB_URI connection string"
    - "MONGODB_URI is stored only in .env (gitignored), never committed to source"
    - "The intended MONGODB_URI variable is documented in .env.example for future contributors"
  artifacts:
    - ".env (contains MONGODB_URI; not committed)"
    - ".env.example (documents the MONGODB_URI placeholder line)"
  key_links:
    - "MONGODB_URI is consumed by db/init_collections.py, scripts/seed_catalog.py, and tests/conftest.py in later Phase 2 plans"
---

<objective>
Provision the MongoDB instance this phase persists the catalog into, and wire its connection string into the project's established `.env` + python-dotenv convention (the same pattern Phase 1 used for eBay credentials).

Purpose: 02-RESEARCH.md Environment Availability confirmed there is NO MongoDB on this machine (no `mongod`, `mongosh`, or Docker daemon). Every downstream Phase 2 plan (collection creation, seeding, the integration test suite) needs a live MongoDB reachable via `MONGODB_URI`. This plan makes that instance exist and documents the variable without ever committing the real connection string.
Output: A reachable MongoDB instance, a `MONGODB_URI` in `.env`, and a documented placeholder in `.env.example`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-product-catalog-data-model/02-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Provision a MongoDB instance and set MONGODB_URI in .env</name>
  <read_first>
    - .planning/phases/02-product-catalog-data-model/02-RESEARCH.md (Environment Availability section — Atlas M0 recommended path and local Homebrew fallback; Homebrew v6.0.6 is available on this machine)
    - .env.example (existing Phase 1 secret-hygiene scaffold — the file the placeholder line is added to; the real .env mirrors it with actual values)
  </read_first>
  <action>
    STOP and provision a MongoDB instance before any later Phase 2 plan can create collections or seed. Choose one of the two provisioning paths below, complete it, set `MONGODB_URI` in the gitignored `.env` (never `.env.example`), and confirm the instance is reachable. Do NOT commit the real connection string. This is a human step because Atlas provisioning requires a dashboard signup and the local path requires a deliberate install decision.
  </action>
  <what-built>
    Nothing is auto-provisioned — a MongoDB instance requires either a human dashboard signup (Atlas) or a deliberate local-install decision. This gate presents both provisioning paths and confirms a reachable instance plus a `MONGODB_URI` in `.env`.
  </what-built>
  <how-to-verify>
    Choose ONE path and complete it:

    Path A — MongoDB Atlas M0 (recommended; matches .claude/CLAUDE.md's stated stack choice, no local infra):
    1. Create a free Atlas account and an M0 (free-tier) cluster at https://www.mongodb.com/cloud/atlas .
    2. Under Cluster -> Connect -> Drivers, select Python and copy the `mongodb+srv://...` connection string; replace `<password>` with the real database-user password and append the database name `pokemonview` before the query string.
    3. Add a line to the project's `.env` (NOT `.env.example`): `MONGODB_URI=mongodb+srv://...pokemonview...` .
    4. Under Network Access, allow the current IP so the seed script and tests can connect.

    Path B — Local MongoDB via Homebrew (fully local dev; Homebrew v6.0.6 confirmed available):
    1. `brew tap mongodb/brew && brew install mongodb-community` then `brew services start mongodb-community`.
    2. Add to `.env`: `MONGODB_URI=mongodb://localhost:27017/pokemonview` .

    Then, regardless of path, confirm the URI is present in `.env` and the instance is reachable.
  </how-to-verify>
  <acceptance_criteria>
    - `.env` contains a `MONGODB_URI=` line with a real connection string ending in / referencing the `pokemonview` database
    - The connection string is present in `.env` ONLY — it does NOT appear in `.env.example` or any committed file (verified: `grep -r "mongodb+srv://" --include="*.py" . ` and a check of git-tracked files return no real credential)
    - `.env` remains gitignored (Phase 1 `.gitignore` already excludes it — confirm `git check-ignore .env` prints `.env`)
    - The instance is reachable (Atlas Network Access allows the current IP, or the local `mongodb-community` service is running)
  </acceptance_criteria>
  <verify><human-check>Confirm `git check-ignore .env` prints `.env`, that `.env` contains a real `MONGODB_URI` for the `pokemonview` database, and that the instance is reachable. This connection is exercised end-to-end by Plan 04's live smoke check and Plan 06's pytest suite.</human-check></verify>
  <done>A MongoDB instance is provisioned and reachable via MONGODB_URI stored only in the gitignored .env.</done>
  <resume-signal>Type "approved" once MONGODB_URI is set in .env and the instance is reachable, or describe the blocker</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Document MONGODB_URI in .env.example</name>
  <files>.env.example</files>
  <read_first>
    - .env.example (current Phase 1 content — add to it without disturbing existing eBay credential placeholders)
    - .planning/phases/02-product-catalog-data-model/02-RESEARCH.md (Security Domain — Hardcoded credentials / Information Disclosure: never commit the real MONGODB_URI; only a placeholder belongs in .env.example)
  </read_first>
  <action>
    Add a `MONGODB_URI=` line to `.env.example` with a non-secret placeholder value (for example `MONGODB_URI=mongodb+srv://<user>:<password>@<cluster-host>/pokemonview` or `mongodb://localhost:27017/pokemonview`), following the exact placeholder style already used for the eBay credential entries in that file. The placeholder MUST contain no real host, username, or password — only angle-bracket tokens or localhost. Preserve every existing line in `.env.example` unchanged. Do NOT create or modify the real `.env` in this task (that was Task 1's human step).
  </action>
  <verify>
    <automated>grep -q '^MONGODB_URI=' .env.example && ! grep -qE 'mongodb\+srv://[^<]*:[^<]*@[a-z0-9]+\.[a-z0-9]+\.mongodb\.net' .env.example && echo "placeholder ok, no real creds"</automated>
  </verify>
  <acceptance_criteria>
    - `.env.example` contains a `MONGODB_URI=` line
    - The value is a placeholder only (angle-bracket tokens or `localhost`) — no real Atlas host, username, or password
    - All pre-existing `.env.example` lines are preserved unchanged
  </acceptance_criteria>
  <done>.env.example documents the MONGODB_URI variable with a safe placeholder; the real connection string lives only in the gitignored .env.</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Type | Notes |
|----------|------|-------|
| MongoDB instance (Atlas M0 or local) | infra | Reachable via MONGODB_URI; hosts the `pokemonview` database |
| `MONGODB_URI` env var (in `.env`) | secret/config | Connection string; gitignored, never committed |
| `.env.example` (updated) | config | Adds a safe `MONGODB_URI=` placeholder line |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.env` (secret) → source control / `.env.example` | The MongoDB connection string may embed an Atlas username/password; committing it would leak database credentials |
| Network → MongoDB instance | Atlas Network Access / local bind controls who can reach the database |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02 | Information Disclosure | MONGODB_URI in `.env` | high | mitigate | Store MONGODB_URI only in the gitignored `.env` (reusing Phase 1's `.env` + python-dotenv pattern); commit only a token-only placeholder to `.env.example`; Task 2 verify greps to prove no real `mongodb+srv://user:pass@host` string is committed |
| T-02-01 | Tampering | MongoDB network exposure | medium | accept | v1 dev scope: Atlas M0 uses IP-allowlisting / local instance binds to localhost; no public unauthenticated exposure introduced this phase (hardening is Phase 7's operational concern) |
</threat_model>

<verification>
- `git check-ignore .env` prints `.env` (real URI is not tracked)
- `.env.example` contains a placeholder-only `MONGODB_URI=` line
- The provisioned instance is reachable via the URI in `.env`
</verification>

<success_criteria>
A MongoDB instance is provisioned and reachable via MONGODB_URI stored in the gitignored .env, with a safe placeholder documented in .env.example.
</success_criteria>

<output>
Create `.planning/phases/02-product-catalog-data-model/02-02-SUMMARY.md` when done
</output>
