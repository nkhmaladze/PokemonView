---
phase: 02-product-catalog-data-model
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/catalog_data.py
  - scripts/__init__.py
autonomous: true
requirements: [CATALOG-01, CATALOG-02]
user_setup: []

must_haves:
  truths:
    - "A curated CATALOG constant holds every in-scope standard-retail sealed product for all 4 sets (Chaos Rising, Perfect Order, Ascended Heroes, Pitch Black) as ~15-16 distinct entries (D-01)"
    - "Each entry carries set_name, product_type, language='en', release_date, msrp, image_url, display_name, and required_keywords (CATALOG-02, D-04, D-07)"
    - "booster_bundle entries are distinct from booster_box and booster_pack via disambiguating required_keywords (D-06)"
    - "Pitch Black entries carry verified=False to flag their provisional pre-release data (D-02)"
    - "No Pokemon Center exclusives, Build & Battle boxes, or promo-only variants appear as entries (D-05)"
  artifacts:
    - "scripts/catalog_data.py (module-level CATALOG list of dicts)"
    - "scripts/__init__.py (empty package marker enabling `from scripts.catalog_data import CATALOG`)"
  key_links:
    - "CATALOG is imported by scripts/seed_catalog.py (seeding) and tests/test_catalog_schema.py (completeness assertions) in later plans"
---

<objective>
Curate the v1 sealed-product catalog as a pure Python data module — no MongoDB, no network at import time — covering all four in-scope sets and up to four product types each, with the canonical metadata (set, type, release info, MSRP, image URL, matching keywords) that CATALOG-02 requires and downstream matching (Phase 4) keys off of. Claude curates this directly via web research per D-03; no manual data entry from the user and no third-party TCG data API.

Purpose: This is the content half of Phase 2 — the actual product list. Keeping it as a diffable data-only module (separate from the seed script) means it can be reviewed on its own and re-run cleanly once Pitch Black's real post-release data lands (D-02) without touching seed logic.
Output: `scripts/catalog_data.py` (the `CATALOG` constant) and `scripts/__init__.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-product-catalog-data-model/02-CONTEXT.md
@.planning/phases/02-product-catalog-data-model/02-RESEARCH.md
@.planning/phases/02-product-catalog-data-model/02-PATTERNS.md
@scripts/verify_ebay_access.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve the outstanding catalog data gaps via web research</name>
  <files>scripts/catalog_data.py</files>
  <read_first>
    - .planning/phases/02-product-catalog-data-model/02-RESEARCH.md (Curated Catalog Reference Data table — the starting dataset with per-field confidence tags; Open Questions 1 and 2; Assumptions Log A1/A2/A4; the TCGplayer image_url CDN pattern)
    - .planning/phases/02-product-catalog-data-model/02-CONTEXT.md (D-01 set list, D-02 Pitch Black, D-05 exclusions, D-08 CDN-link sourcing)
  </read_first>
  <action>
    Perform bounded web research (reasonable diligence per CONTEXT.md "Claude's Discretion", not an exhaustive audit) to close the gaps 02-RESEARCH.md flagged, capturing findings as inline notes to feed Task 2:

    1. Resolve the ~12 unresolved TCGplayer numeric product IDs so every entry gets a real `image_url` of the form `https://tcgplayer-cdn.tcgplayer.com/product/{id}_in_1000x1000.jpg` (D-07/D-08). Already-known IDs from research: Chaos Rising ETB=684450, Perfect Order booster_box=672394, Perfect Order booster_pack=672398, Ascended Heroes booster_bundle=668541. Each remaining product's numeric ID is directly visible in its TCGplayer product-page URL (e.g. `tcgplayer.com/product/672394/...`) — extract it, do not guess/hallucinate an ID.
    2. Confirm the two uncertain MSRPs flagged in Open Question 1: Chaos Rising booster_box (conflicting $144 vs $161.64) and Ascended Heroes standard booster_box (~$161.64, pattern-inferred). While confirming Ascended Heroes, resolve whether a STANDARD (non-Pokemon-Center-exclusive) booster box SKU even exists (Assumption A2) — if it does not, that entry is dropped per D-05 rather than seeded with a guessed price.
    3. Confirm per-product release dates where they differ from the set date (research noted Ascended Heroes ETB released 2026-02-20 and its booster_bundle 2026-04-24, versus the set's 2026-01-30).

    If any TCGplayer ID or MSRP cannot be confidently resolved, set that field to `None` (the schema permits null for image_url/msrp/release_date) rather than fabricating a value; note the gap for the Pitch Black-style follow-up.
  </action>
  <verify>
    <automated>MISSING — research task; findings verified through Task 2's CATALOG assertions and the Plan 06 test suite</automated>
  </verify>
  <acceptance_criteria>
    - A concrete numeric TCGplayer product ID (or an explicit `None`) is determined for every catalog entry's image_url
    - The Chaos Rising and Ascended Heroes booster_box MSRP questions are resolved to a single value each, or the Ascended Heroes standard booster_box entry is dropped if no standard SKU exists
    - Findings are captured (inline notes / draft dict values) ready to be written in Task 2
    - No fabricated TCGplayer IDs — any unresolved ID is recorded as `None`, not invented
  </acceptance_criteria>
  <done>All catalog data gaps from 02-RESEARCH.md Open Questions are resolved to concrete values or explicit nulls, ready to encode in the module.</done>
</task>

<task type="auto">
  <name>Task 2: Author scripts/catalog_data.py with the curated CATALOG constant</name>
  <files>scripts/catalog_data.py, scripts/__init__.py</files>
  <read_first>
    - .planning/phases/02-product-catalog-data-model/02-PATTERNS.md (scripts/catalog_data.py section — CATALOG constant shape, ALL_CAPS constant-naming convention matching Phase 1's CATALOG_QUERIES; "No top-level side effects on import" shared pattern)
    - .planning/phases/02-product-catalog-data-model/02-RESEARCH.md (Code Examples: full catalog document shape; Pitfall 4: booster_bundle keyword disambiguation)
    - scripts/verify_ebay_access.py (Phase 1 module-docstring + ALL_CAPS constant conventions to mirror)
  </read_first>
  <action>
    Create `scripts/__init__.py` as an empty file (package marker so `from scripts.catalog_data import CATALOG` resolves; this does not change how Phase 1 scripts are run directly). Then create `scripts/catalog_data.py` as a pure data module with NO top-level side effects and NO imports of pymongo/network libraries.

    Open the module with a docstring in the Phase 1 style (what it holds, and the CONTEXT.md decision IDs it satisfies: D-01 four sets, D-02 Pitch Black provisional, D-04 msrp, D-06 booster_bundle, D-07 image_url). Define one module-level constant `CATALOG` — a `list[dict]`, one dict per standard-retail sealed product across the four sets (Chaos Rising, Perfect Order, Ascended Heroes, Pitch Black), giving ~15-16 entries (16 minus any dropped Ascended Heroes standard booster_box per Task 1). Each dict has these keys:
      - `set_name` (str, e.g. "Perfect Order"),
      - `product_type` (str, exactly one of: `booster_pack`, `booster_box`, `etb`, `booster_bundle` — the four values D-06 requires),
      - `language` (str, always `"en"`),
      - `release_date` (str ISO date `"YYYY-MM-DD"`, or `None` if genuinely unknown — the seed script converts this to a datetime),
      - `msrp` (float or `None`),
      - `image_url` (str TCGplayer CDN URL from Task 1, or `None`),
      - `display_name` (str, human-readable full product name),
      - `required_keywords` (list[str], lowercase),
      - `verified` (bool — `False` for all Pitch Black entries per D-02, `True` for the three released sets),
      - `verified_at` (`None` for now; a later post-release pass sets Pitch Black's).

    Design `required_keywords` with explicit disambiguating terms so the four product types for the same set never share an identical keyword list (Pitfall 4): booster_box keywords include `"booster box"` and `"display"`; booster_bundle keywords include `"booster bundle"` and `"bundle"`; booster_pack keywords include `"booster pack"` (and must NOT include "box"/"bundle"); etb keywords include `"elite trainer box"` and `"etb"`. Every entry's keyword list also includes the lowercased set name.

    Do NOT add Pokemon Center-exclusive ETB variants, Build & Battle boxes, or promo-only products (D-05). Do NOT write any DB/seed logic here — this module is data only.
  </action>
  <verify>
    <automated>python -c "from scripts.catalog_data import CATALOG; sets={p['set_name'] for p in CATALOG}; types={p['product_type'] for p in CATALOG}; keys=[(p['set_name'],p['product_type']) for p in CATALOG]; assert 15<=len(CATALOG)<=16, len(CATALOG); assert len(sets)==4, sets; assert types<={'booster_pack','booster_box','etb','booster_bundle'}, types; assert len(keys)==len(set(keys)), 'duplicate (set,type)'; assert all(set(p)>= {'set_name','product_type','language','msrp','image_url','display_name','required_keywords','release_date','verified'} for p in CATALOG); assert all(p['language']=='en' for p in CATALOG); assert all(p['verified'] is False for p in CATALOG if p['set_name']=='Pitch Black'); print('CATALOG ok:', len(CATALOG), 'entries')"</automated>
  </verify>
  <acceptance_criteria>
    - `from scripts.catalog_data import CATALOG` succeeds (scripts/__init__.py present, no import side effects)
    - `len(CATALOG)` is 15 or 16, spanning exactly 4 distinct `set_name` values
    - Every `product_type` is one of the four enum values; no two entries share the same `(set_name, product_type)` pair
    - Every entry has all required keys; every `language` is `"en"`
    - Every Pitch Black entry has `verified is False`
    - No two product types for the same set have identical `required_keywords` (booster_bundle carries `"bundle"`, booster_box carries `"display"`/`"box"`, per Pitfall 4)
    - No Pokemon Center-exclusive / Build & Battle / promo entries present (D-05)
  </acceptance_criteria>
  <done>scripts/catalog_data.py defines a curated, disambiguated, D-01…D-08-compliant CATALOG of ~15-16 entries importable as a side-effect-free package module.</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Type | Notes |
|----------|------|-------|
| `scripts/catalog_data.py` | data module | Module-level `CATALOG: list[dict]` — the curated product list |
| `CATALOG` | constant (list[dict]) | Fields per entry: set_name, product_type, language, release_date, msrp, image_url, display_name, required_keywords, verified, verified_at |
| `scripts/__init__.py` | package marker | Empty; enables `from scripts.catalog_data import CATALOG` |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Web research sources → catalog data | Web-sourced MSRP / product-lineup / image-ID facts (some provisional, especially pre-release Pitch Black) cross into the persisted catalog |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Tampering (data integrity) | `CATALOG` provisional entries (Pitch Black, pattern-inferred MSRPs) | low | mitigate | Every entry carries a `verified` flag (False for Pitch Black per D-02); unresolved image/MSRP fields are set to `None` rather than fabricated; the D-02 post-release re-verification follow-up is recorded (see Plan 06) so provisional data cannot silently become "trusted" |
| T-02-02 | Information Disclosure | scripts/catalog_data.py | low | accept | This module is pure public product data — no credentials, no MONGODB_URI, no secrets; nothing to leak |
| T-02-01 | Tampering | image_url references | low | accept | image_url stores only external TCGplayer/Pokemon Center CDN reference strings (D-08); this project hosts no image bytes and constructs no queries from these strings this phase |
</threat_model>

<verification>
- `python -c "from scripts.catalog_data import CATALOG; print(len(CATALOG))"` prints 15 or 16
- CATALOG spans all 4 sets, uses only the 4 product_type enum values, has no duplicate (set,type) pairs
- Pitch Black entries are flagged verified=False
</verification>

<success_criteria>
A curated, importable CATALOG of ~15-16 distinct standard-retail sealed products across all 4 sets exists with full canonical metadata and disambiguated matching keywords, provisional Pitch Black data explicitly flagged.
</success_criteria>

<output>
Create `.planning/phases/02-product-catalog-data-model/02-03-SUMMARY.md` when done
</output>
