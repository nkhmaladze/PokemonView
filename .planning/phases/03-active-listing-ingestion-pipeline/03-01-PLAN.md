---
phase: 03-active-listing-ingestion-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
autonomous: false
requirements: [INGEST-01]
user_setup: []

must_haves:
  truths:
    - "apscheduler 3.11.3 is importable in the project's Python environment"
    - "A human has confirmed apscheduler's legitimacy (documented false-positive [SUS] verdict) before install"
  artifacts:
    - "requirements.txt updated with apscheduler==3.11.3 appended after the four existing Phase 1/2 pins"
  key_links:
    - "requirements.txt's apscheduler pin is consumed by scripts/ingest_worker.py's main()/BlockingScheduler entrypoint (Plan 03-04), the only module that imports apscheduler"
---

<objective>
Add and install the one new Python dependency this phase needs — `apscheduler==3.11.3` (the in-process job scheduler mandated by CLAUDE.md over Celery/task-broker infrastructure) — gated by a mandatory human legitimacy checkpoint, because 03-RESEARCH.md's automated supply-chain check returned a false-positive `[SUS]` verdict (`too-new`, `unknown-downloads`, `no-repository`) for the package (threat T-03-SC).

Purpose: The ingestion worker's scheduled-run entrypoint (Plan 03-04) cannot be built until APScheduler is installed. This plan establishes it once, cleanly, behind the required package-legitimacy gate rather than silently overriding the `[SUS]` verdict — mirroring Phase 1's `requests`/`python-dotenv` gate and Phase 2's `pymongo` gate (02-01) exactly.
Output: An updated `requirements.txt` and an installed environment containing apscheduler 3.11.3.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-active-listing-ingestion-pipeline/03-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Human legitimacy verification for apscheduler before install</name>
  <read_first>
    - .planning/phases/03-active-listing-ingestion-pipeline/03-RESEARCH.md (Package Legitimacy Audit section + Assumptions Log A4 — the [SUS] verdict and why it is a false positive)
  </read_first>
  <action>
    STOP before running any `pip install`. Present the apscheduler legitimacy evidence to the human and wait for explicit approval. Do NOT proceed to Task 2's install until the human approves. This gate exists because 03-RESEARCH.md's automated legitimacy check returned a `[SUS]` verdict for apscheduler (threat T-03-SC) driven by `too-new` (keyed off the latest release date 2026-06-28, not the project's real age), `unknown-downloads` (a telemetry gap in this environment), and `no-repository` — none of which is a real red flag. The canonical source repo is `github.com/agronholm/apscheduler` (maintained since 2011) and apscheduler is the exact package CLAUDE.md's own stack research recommends. Protocol still requires this human gate before install (never auto-approvable, `workflow.auto_advance` ignored), identical to Phase 2's pymongo gate (02-01).
  </action>
  <what-built>
    Nothing is installed yet. This gate presents the automated legitimacy result for apscheduler so a human can approve before the install runs. apscheduler was flagged `[SUS]` only for the false-positive reasons above, not for any behavioral red flag: it is a pure-Python package with no setuptools postinstall hook (03-RESEARCH.md Postinstall script check).
  </what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/APScheduler/ and confirm the package page shows APScheduler, latest version 3.11.3, and links to `github.com/agronholm/apscheduler`.
    2. Confirm the package name to be installed is exactly `apscheduler` (no typosquat variants such as `apschedulers` or `apeduler`).
    3. Confirm the pinned version to install is exactly `apscheduler==3.11.3`.
  </how-to-verify>
  <acceptance_criteria>
    - Human has reviewed the pypi.org/project/APScheduler page and the official agronholm/apscheduler source repo link
    - Human types "approved" (or equivalent) to unblock, OR reports a concern that halts the install
    - The [SUS] verdict is explicitly acknowledged as a false-positive-from-missing-telemetry-plus-recent-release-date, not silently overridden
  </acceptance_criteria>
  <verify><human-check>Human has reviewed pypi.org/project/APScheduler and the official agronholm/apscheduler repo link and typed "approved" (or halted with a concern). No install runs before this approval.</human-check></verify>
  <done>A human has explicitly approved the apscheduler install after reviewing its legitimacy evidence, or halted the plan with a concern.</done>
  <resume-signal>Type "approved" to proceed with install, or describe the concern to halt</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Pin and install apscheduler==3.11.3</name>
  <files>requirements.txt</files>
  <read_first>
    - requirements.txt (current state — contains exactly four lines: requests==2.34.2, python-dotenv==1.2.2, pymongo==4.17.0, pytest==8.4.2; all four must be preserved unchanged)
    - .planning/phases/03-active-listing-ingestion-pipeline/03-RESEARCH.md (Standard Stack: apscheduler==3.11.3 verified via `pip index versions apscheduler` this session as latest; matches CLAUDE.md's documented 3.11.x recommendation)
  </read_first>
  <action>
    Append one new pinned line `apscheduler==3.11.3` to requirements.txt after the four existing entries, preserving `requests==2.34.2`, `python-dotenv==1.2.2`, `pymongo==4.17.0`, and `pytest==8.4.2` unchanged and in order. Use this exact version pin — 3.11.3 is the registry-verified latest, not a training-data guess. Then install into the active environment with `pip install -r requirements.txt`. Do not add any package beyond apscheduler; the other four are already installed. Note the distribution name is `APScheduler` (PyPI) but the import module is lowercase `apscheduler`.
  </action>
  <verify>
    <automated>python -c "import apscheduler; from apscheduler.schedulers.blocking import BlockingScheduler; from apscheduler.triggers.interval import IntervalTrigger; print('apscheduler', apscheduler.version)"</automated>
  </verify>
  <acceptance_criteria>
    - requirements.txt contains exactly five lines in order: requests==2.34.2, python-dotenv==1.2.2, pymongo==4.17.0, pytest==8.4.2, apscheduler==3.11.3
    - `python -c "import apscheduler"` exits 0
    - `from apscheduler.schedulers.blocking import BlockingScheduler` and `from apscheduler.triggers.interval import IntervalTrigger` both import without error
    - `apscheduler.version` reports 3.11.3
  </acceptance_criteria>
  <done>apscheduler==3.11.3 is pinned in requirements.txt and importable in the environment (including BlockingScheduler and IntervalTrigger); the four prior dependency lines are unchanged.</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Type | Notes |
|----------|------|-------|
| `requirements.txt` (updated) | config | Appends `apscheduler==3.11.3`; preserves the four Phase 1/2 pins |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI registry → local Python environment | An installed package executes arbitrary code at import/build time; a malicious or typosquatted package would cross into the developer's trusted environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-SC | Tampering | `pip install apscheduler` (supply chain) | high | mitigate | Blocking human-verify checkpoint (Task 1) against pypi.org/project/APScheduler + official `agronholm/apscheduler` repo before install; exact-pin `apscheduler==3.11.3` to prevent silent version drift; `[SUS]` verdict is a documented false-positive (recent-release-date + missing download telemetry), never auto-approved; package is pure-Python with no postinstall hook |
| T-03-CFG | Information Disclosure | requirements.txt | low | accept | requirements.txt contains no secrets — only public package names/versions; no credential surface here |
</threat_model>

<verification>
- `python -c "import apscheduler"` succeeds and `apscheduler.version` is 3.11.3
- BlockingScheduler and IntervalTrigger are importable
- The four prior dependency lines (requests, python-dotenv, pymongo, pytest) remain intact in requirements.txt
</verification>

<success_criteria>
apscheduler==3.11.3 is pinned and installed, gated by a human legitimacy checkpoint that explicitly handled apscheduler's false-positive [SUS] verdict.
</success_criteria>

<output>
Create `.planning/phases/03-active-listing-ingestion-pipeline/03-01-SUMMARY.md` when done
</output>
