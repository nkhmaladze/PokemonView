---
phase: quick-260718-0a4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ebay_client.py
  - tests/test_ebay_client.py
autonomous: true
requirements: [INGEST-03]
must_haves:
  truths:
    - "total_cost() returns item_price + 0.0 (never raises) when shippingOptions[0] exists but has no shippingCost key"
    - "total_cost() still returns item_price + shipping_cost for well-formed listings and item_price + 0.0 when shippingOptions is absent/empty"
    - "total_cost()'s signature and 'item_price + shipping_cost' contract are unchanged (ingest_worker.py depends on it)"
  artifacts:
    - "scripts/ebay_client.py (patched total_cost)"
    - "tests/test_ebay_client.py (new regression test)"
  key_links:
    - "scripts/ingest_worker.py::upsert_listings calls total_cost(item) at line 152 as the single source of truth for item_price/shipping_cost/total_price — contract must not change"
---

<objective>
Fix a live KeyError crash in `scripts/ebay_client.py::total_cost()` when a real eBay Browse API listing returns a non-empty `shippingOptions` array whose first entry lacks a `shippingCost` key (calculated/freight shipping without a buyer postal code, or local-pickup-only options). Add one regression test.

Purpose: `verify_ebay_access.py` — Phase 1's feasibility gate, exercised live against real eBay Production data for the first time — crashed on this real response shape. `total_cost()` is the single source of truth for `item_price`/`shipping_cost`/`total_price` in the production ingestion worker, so this defensive-parsing gap blocks live ingestion.

Output: A hardened `total_cost()` that defaults shipping to 0.0 whenever the shipping value is absent/None (matching how it already handles absent/empty `shippingOptions`), plus a regression test proving the missing-`shippingCost` case returns `item_price + 0.0` instead of raising.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@scripts/ebay_client.py
@scripts/ingest_worker.py
@tests/test_ingest_worker.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED regression test for missing shippingCost key</name>
  <files>tests/test_ebay_client.py</files>
  <behavior>
    - total_cost() with an item whose shippingOptions[0] has no "shippingCost" key returns item_price + 0.0 (does NOT raise KeyError).
    - total_cost() with a well-formed item (shippingOptions[0].shippingCost.value present) returns item_price + shipping_cost.
    - total_cost() with no shippingOptions key returns item_price + 0.0 (guards against regressing the already-working path).
  </behavior>
  <action>Create a new file tests/test_ebay_client.py as a pure unit test module (no fixture) mirroring the plain-pytest-assert, deferred-in-body-import style of tests/test_ingest_worker.py::test_build_query — import total_cost from scripts.ebay_client inside each test body, not at module top level. Add one primary regression test asserting that total_cost() on an item like {"price": {"value": "50.00"}, "shippingOptions": [{"deliveryCost": {"value": "0.00"}}]} (a non-empty shippingOptions whose first entry lacks the shippingCost key) returns 50.0, not a raise. Use exactly-representable float values (50.00, 4.50, 54.50) as tests/test_ingest_worker.py::test_price_and_shipping_captured does, so == assertions stay safe. Also cover the two already-working paths in the same test or sibling tests: a well-formed item with shippingOptions[0].shippingCost.value present returns item_price + shipping_cost, and an item with no shippingOptions key returns item_price + 0.0. This test file is authored RED first — it will fail (KeyError) against the current unpatched total_cost until Task 2 lands. Do not add a conftest fixture; total_cost does no I/O.</action>
  <verify>
    <automated>cd /Users/nkhmal/Desktop/PokemonView && python -m pytest tests/test_ebay_client.py -x 2>&1 | grep -qE "KeyError|1 failed|error" && echo "RED confirmed (fails pre-fix)"</automated>
  </verify>
  <done>tests/test_ebay_client.py exists, contains a regression test for the missing-shippingCost-key case plus the two already-working paths, and fails with a KeyError against the current unpatched total_cost().</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden total_cost() with defensive .get() chaining</name>
  <files>scripts/ebay_client.py</files>
  <behavior>
    - When shipping_options is non-empty but the first option has no shippingCost (or shippingCost is None, or has no value, or value is None), shipping_cost defaults to 0.0.
    - When a real shipping value is present, it is converted to float exactly as before.
    - item_price extraction and the returned "item_price + shipping_cost" sum are unchanged.
  </behavior>
  <action>In scripts/ebay_client.py::total_cost() (lines 98-116), replace the single-expression shipping_cost computation that indexes shipping_options[0]["shippingCost"]["value"] directly with defensive .get() chaining. Resolve the shipping value from shipping_options[0] via .get("shippingCost") coalesced to an empty dict when absent/None, then .get("value") — yielding None when shippingOptions is empty OR the first option lacks shippingCost OR shippingCost lacks value OR the value is None. Convert to float only when that resolved value is not None; otherwise default shipping_cost to 0.0. Keep item_price = float(item["price"]["value"]) and the final return item_price + shipping_cost exactly as-is — do NOT change the function signature, the return contract, or anything else in the file. scripts/ingest_worker.py::upsert_listings (line 152) depends on this exact contract as the single source of truth for item_price/shipping_cost/total_price. Update the total_cost docstring's shipping-default sentence so it states shipping defaults to 0.0 whenever the shipping value is absent/None (not only when shippingOptions is empty/missing).</action>
  <verify>
    <automated>cd /Users/nkhmal/Desktop/PokemonView && python -m pytest tests/test_ebay_client.py tests/test_ingest_worker.py -x</automated>
  </verify>
  <done>total_cost() returns item_price + 0.0 for the missing-shippingCost case; tests/test_ebay_client.py passes GREEN; tests/test_ingest_worker.py (which reuses total_cost via upsert_listings) still passes, confirming the contract is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eBay Browse API → total_cost() | Untrusted external JSON: shippingOptions entry shapes vary (calculated/freight/local-pickup) and cannot be assumed to contain shippingCost. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-Q0a4-01 | Denial of Service | scripts/ebay_client.py::total_cost | medium | mitigate | Defensive .get() chaining prevents an unexpected-but-valid eBay response shape from raising and aborting the live ingestion/verify run (the exact crash observed). Regression test locks the behavior. |

No new dependencies are installed; no package-legitimacy gate applies.
</threat_model>

<verification>
- `python -m pytest tests/test_ebay_client.py -x` passes (regression covered).
- `python -m pytest tests/test_ingest_worker.py -x` still passes (total_cost contract preserved for the ingestion worker).
- total_cost() signature is unchanged (`def total_cost(item: dict) -> float`).
- No files other than scripts/ebay_client.py and tests/test_ebay_client.py are modified; ingest_worker.py is untouched.
</verification>

<success_criteria>
- Calling total_cost() with a non-empty shippingOptions whose first entry lacks "shippingCost" returns item_price + 0.0 instead of raising KeyError.
- All existing behavior (well-formed shipping, absent shippingOptions) is unchanged.
- The full test suite for the two touched surfaces (tests/test_ebay_client.py, tests/test_ingest_worker.py) is green.
</success_criteria>

<output>
Create `.planning/quick/260718-0a4-fix-ebay-client-py-total-cost-keyerror-o/260718-0a4-SUMMARY.md` when done
</output>
