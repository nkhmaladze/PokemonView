---
phase: 02-product-catalog-data-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
autonomous: false
requirements: [CATALOG-01, CATALOG-02]
user_setup: []

must_haves:
  truths:
    - "pymongo 4.17.0 and pytest 8.4.2 are importable in the project's Python environment"
    - "A human has confirmed pymongo's legitimacy (false-positive [SUS] verdict) before install"
  artifacts:
    - "requirements.txt updated with pymongo==4.17.0 and pytest==8.4.2"
  key_links:
    - "requirements.txt pins are consumed by db/init_collections.py, scripts/seed_catalog.py, and the tests/ suite in later Phase 2 plans"
---

<objective>
Add and install the two new Python dependencies this phase needs — pymongo==4.17.0 (the MongoDB driver) and pytest==8.4.2 (the test framework) — gated by a mandatory human legitimacy checkpoint for pymongo, whose automated supply-chain check returned a false-positive [SUS] verdict (see 02-RESEARCH.md Package Legitimacy Audit).

Purpose: Every other Phase 2 plan (schema creation, seeding, testing) needs these two packages present. This plan establishes them once, cleanly, behind the required package-legitimacy gate rather than letting the [SUS] verdict be silently overridden.
Output: An updated requirements.txt and an installed environment containing pymongo and pytest.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-product-catalog-data-model/02-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Human legitimacy verification for pymongo before install</name>
  <read_first>
    - .planning/phases/02-product-catalog-data-model/02-RESEARCH.md (Package Legitimacy Audit section + Assumptions Log A5 — the [SUS] verdict and why it is a false positive)
  </read_first>
  <action>
    STOP before running any `pip install`. Present the pymongo legitimacy evidence (below) to the human and wait for explicit approval. Do NOT proceed to Task 2's install until the human approves. This gate exists because 02-RESEARCH.md's automated legitimacy check returned a [SUS] verdict for pymongo (threat T-02-SC) that must not be silently auto-overridden, even though it is a documented false positive.
  </action>
  <what-built>
    Nothing is installed yet. This gate presents the automated legitimacy result for pymongo so a human can approve before the install runs. Per 02-RESEARCH.md, pymongo was flagged [SUS] ONLY because the legitimacy-check tool could not resolve a download-count signal (`unknown-downloads`), not because of any real red flag: the source repo resolves to MongoDB's own official GitHub org (`github.com/mongodb/mongo-python-driver`), and pymongo is the project's already-decided driver per .claude/CLAUDE.md. Protocol still requires this human gate before install (threat T-02-SC).
  </what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/pymongo/ and confirm the package page shows the official MongoDB driver, latest version 4.17.0, and links to `github.com/mongodb/mongo-python-driver`.
    2. Confirm the package name to be installed is exactly `pymongo` (no typosquat variants such as `pymongo-` or `py-mongo`).
    3. Confirm the pinned version to install is exactly `pymongo==4.17.0`.
  </how-to-verify>
  <acceptance_criteria>
    - Human has reviewed the pypi.org/project/pymongo page and the official source repo link
    - Human types "approved" (or equivalent) to unblock, OR reports a concern that halts the install
    - The [SUS] verdict is explicitly acknowledged as a false-positive-from-missing-telemetry, not silently overridden
  </acceptance_criteria>
  <verify><human-check>Human has reviewed pypi.org/project/pymongo and the official mongodb/mongo-python-driver repo link and typed "approved" (or halted with a concern). No install runs before this approval.</human-check></verify>
  <done>A human has explicitly approved the pymongo install after reviewing its legitimacy evidence, or halted the plan with a concern.</done>
  <resume-signal>Type "approved" to proceed with install, or describe the concern to halt</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Pin and install pymongo and pytest</name>
  <files>requirements.txt</files>
  <read_first>
    - requirements.txt (current state — contains requests==2.34.2 and python-dotenv==1.2.2 from Phase 1; preserve both lines exactly)
    - .planning/phases/02-product-catalog-data-model/02-RESEARCH.md (Standard Stack: pymongo==4.17.0 verified via `pip3 index versions pymongo` on 2026-07-13; pytest==8.4.2 for the Wave 0 test gap)
  </read_first>
  <action>
    Append two new pinned lines to requirements.txt after the existing Phase 1 entries, preserving `requests==2.34.2` and `python-dotenv==1.2.2` unchanged: add `pymongo==4.17.0` and `pytest==8.4.2`. Use these exact version pins — pymongo==4.17.0 is the registry-verified latest (not a training-data guess), and pytest==8.4.2 is the 8.x line specified by the Validation Architecture. Then install into the active environment with `pip install -r requirements.txt`. Do not add any package beyond these two; python-dotenv was already installed in Phase 1 and requests is unrelated to this phase.
  </action>
  <verify>
    <automated>python -c "import pymongo, pytest; assert pymongo.version.startswith('4.17'), pymongo.version; print('pymongo', pymongo.version, 'pytest', pytest.__version__)"</automated>
  </verify>
  <acceptance_criteria>
    - requirements.txt contains exactly four lines: requests==2.34.2, python-dotenv==1.2.2, pymongo==4.17.0, pytest==8.4.2
    - `python -c "import pymongo, pytest"` exits 0
    - `pymongo.version` starts with `4.17`
    - `pytest --version` reports 8.4.2
  </acceptance_criteria>
  <done>pymongo==4.17.0 and pytest==8.4.2 are pinned in requirements.txt and importable in the environment; the two Phase 1 dependency lines are unchanged.</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Type | Notes |
|----------|------|-------|
| `requirements.txt` (updated) | config | Adds `pymongo==4.17.0` and `pytest==8.4.2` lines; preserves Phase 1 pins |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI registry → local Python environment | An installed package executes arbitrary code at import/build time; a malicious or typosquatted package crosses into the developer's trusted environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-SC | Tampering | `pip install pymongo` (supply chain) | high | mitigate | Blocking human-verify checkpoint (Task 1) against pypi.org/project/pymongo + official `mongodb/mongo-python-driver` repo before install; exact-pin `pymongo==4.17.0` to prevent silent version drift; [SUS] verdict is a documented false-positive-from-missing-telemetry, never auto-approved |
| T-02-02 | Information Disclosure | requirements.txt | low | accept | requirements.txt contains no secrets — only public package names/versions; no credential surface here |
</threat_model>

<verification>
- `python -c "import pymongo, pytest"` succeeds
- pymongo version is 4.17.x, pytest version is 8.4.2
- Phase 1 dependency lines (requests, python-dotenv) remain intact in requirements.txt
</verification>

<success_criteria>
pymongo==4.17.0 and pytest==8.4.2 are pinned and installed, gated by a human legitimacy checkpoint that explicitly handled pymongo's false-positive [SUS] verdict.
</success_criteria>

<output>
Create `.planning/phases/02-product-catalog-data-model/02-01-SUMMARY.md` when done
</output>
