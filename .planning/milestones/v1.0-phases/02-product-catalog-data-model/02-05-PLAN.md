---
phase: 02-product-catalog-data-model
plan: 05
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - tests/conftest.py
  - tests/test_catalog_schema.py
  - pytest.ini
autonomous: true
requirements: [CATALOG-01, CATALOG-02]
user_setup: []

must_haves:
  truths:
    - "A pytest suite defines the four catalog contract tests: completeness, seed idempotency, schema-validator rejection, and query-by-set-and-type"
    - "The suite collects cleanly (4 tests discovered) even before the implementation modules exist or the seed has run — proving the scaffold is authored first (Nyquist Wave 0)"
    - "The shared fixture targets a dedicated throwaway test database, never production catalog data"
  artifacts:
    - "tests/conftest.py (catalog_db fixture)"
    - "tests/test_catalog_schema.py (4 test functions)"
    - "pytest.ini (pythonpath = . so tests resolve the scripts/ and db/ packages)"
  key_links:
    - "conftest lazily wires db.init_collections + scripts.seed_catalog + scripts.catalog_data against a dedicated test database; the tests are turned green by Plan 06's seed run"
---

<objective>
Author the phase's test scaffold FIRST (per the Nyquist test-first rule and the 02-VALIDATION.md Wave 0 requirements): a pytest suite that encodes the four CATALOG-01/CATALOG-02 contract tests plus a shared MongoDB test fixture, wired so the suite can be discovered/collected before the implementation modules and the seed exist. The tests intentionally remain RED (fixture setup fails on the not-yet-existing seed/init modules) until Plan 06 turns them green.

Purpose: The integration tests genuinely require the real init_collections + seed_catalog + a live MongoDB (mongomock cannot enforce `$jsonSchema` validation or create time-series collections — the two things this phase must prove), so the fixture uses a real, dedicated test database. Authoring the tests now defines the contract the implementation must satisfy.
Output: `tests/conftest.py`, `tests/test_catalog_schema.py`, and `pytest.ini`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-product-catalog-data-model/02-RESEARCH.md
@.planning/phases/02-product-catalog-data-model/02-VALIDATION.md
@.planning/phases/02-product-catalog-data-model/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pytest.ini and the shared test-database fixture (tests/conftest.py)</name>
  <files>pytest.ini, tests/conftest.py</files>
  <read_first>
    - .planning/phases/02-product-catalog-data-model/02-RESEARCH.md (Validation Architecture — pytest 8.x, quick/full run commands; the conftest test-DB fixture decision; Environment Availability — MONGODB_URI from .env)
    - .planning/phases/02-product-catalog-data-model/02-PATTERNS.md (tests/test_catalog_schema.py section — first test file in the repo, plain-pytest assert style, no unittest.TestCase; ".env / credential loading" and "No top-level side effects on import" shared patterns)
  </read_first>
  <action>
    Create `pytest.ini` at the repo root containing a `[pytest]` section that sets `pythonpath = .` so the `scripts` and `db` packages resolve from the repo root during test collection and execution (pytest 8.x supports the `pythonpath` ini option). Optionally set `testpaths = tests`.

    Create `tests/conftest.py` importing only `pytest`, `os`, and `pymongo.MongoClient` at module top — do NOT import the project's `scripts.*` / `db.*` modules at top level (lazy-import them INSIDE the fixture so `pytest --collect-only` succeeds before those modules exist, satisfying the Nyquist scaffold-first requirement). Define a fixture named `catalog_db` (function scope is fine for a tiny catalog) that:
      1. `load_dotenv()` and read `MONGODB_URI` from `os.environ`; if it is unset, `pytest.skip` with a message pointing at Plan 02 (MongoDB provisioning), so the scaffold degrades gracefully rather than erroring when no DB is configured.
      2. Connect a `MongoClient(MONGODB_URI)` and select a DEDICATED throwaway test database whose name ends in `_test` (for example `pokemonview_test`) — NEVER the real `pokemonview` database. Drop that test database's `products` and `price_points` collections up front so each run starts clean.
      3. Lazily import and call `from db.init_collections import init_collections; init_collections(test_db)` then `from scripts.seed_catalog import seed_catalog; from scripts.catalog_data import CATALOG; seed_catalog(test_db, CATALOG)`.
      4. `yield test_db`.
      5. Teardown: drop the test database (or its two collections) and close the client.
    Never hardcode or print the full MONGODB_URI (mirror Phase 1's secrets-hygiene pattern — print only db name / counts if anything).
  </action>
  <verify>
    <automated>python -m py_compile tests/conftest.py && grep -q 'pythonpath' pytest.ini && grep -q 'def catalog_db' tests/conftest.py && grep -q '_test' tests/conftest.py && echo "conftest + pytest.ini scaffold ok"</automated>
  </verify>
  <acceptance_criteria>
    - `pytest.ini` exists with a `[pytest]` section setting `pythonpath = .`
    - `tests/conftest.py` compiles and defines a `catalog_db` fixture
    - The fixture selects a database name ending in `_test` (never `pokemonview` alone) — proven by the `_test` token in source
    - The fixture `pytest.skip`s when `MONGODB_URI` is unset (graceful when no DB provisioned)
    - No top-level import of `scripts.*` / `db.*` in conftest (lazy imports inside the fixture) — so collection works before those modules exist
  </acceptance_criteria>
  <done>pytest.ini and a lazy-importing, test-database-scoped catalog_db fixture exist, discoverable before the implementation modules land.</done>
</task>

<task type="auto">
  <name>Task 2: Author the four catalog contract tests (tests/test_catalog_schema.py)</name>
  <files>tests/test_catalog_schema.py</files>
  <read_first>
    - .planning/phases/02-product-catalog-data-model/02-VALIDATION.md (Per-Task Verification Map — the four required test function names and their automated commands; update its Task ID / Plan / Wave columns to reference this plan)
    - .planning/phases/02-product-catalog-data-model/02-RESEARCH.md (Phase Requirements → Test Map — the behavior each test asserts; Pitfall 5 duplication signal)
    - tests/conftest.py (the `catalog_db` fixture authored in Task 1 that these tests consume)
  </read_first>
  <action>
    Create `tests/test_catalog_schema.py` with plain pytest `assert` style (no unittest.TestCase), each test consuming the `catalog_db` fixture. Define exactly these four functions with these names (they map 1:1 to 02-VALIDATION.md):
      1. `test_catalog_completeness(catalog_db)` — assert the `products` collection's `count_documents({})` equals the number of entries in `scripts.catalog_data.CATALOG` (15 or 16); assert all four `set_name` values are present via `distinct("set_name")`; assert `distinct("product_type")` is a subset of the four allowed values; assert there are no duplicate `(set_name, product_type)` pairs (aggregate a group count, or compare distinct-pair count to total). (CATALOG-01)
      2. `test_seed_idempotent(catalog_db)` — capture `count_documents({})`, then lazily import and re-run `seed_catalog(catalog_db, CATALOG)` a second time, then assert the count is unchanged (upsert-by-slug produces no duplicates; Pitfall 5). (CATALOG-01)
      3. `test_schema_validator_rejects_malformed(catalog_db)` — attempt to `insert_one` a document with an invalid `product_type` (for example `"lot_bundle"`) and assert pymongo raises a write/operation error (`pymongo.errors.WriteError` or `OperationFailure`); this proves the `$jsonSchema` validator (V5 control, threat T-02-01) actually rejects bad shape at the DB layer. (CATALOG-02)
      4. `test_query_by_set_and_type(catalog_db)` — query `find({"set_name": <a real set>})` and assert it returns that set's entries; query `find({"set_name": <set>, "product_type": "booster_box"})` and assert it returns the single matching box; confirm the compound index is used by checking the index exists via `index_information()`. (CATALOG-02)
    Import `scripts.catalog_data.CATALOG`, `scripts.seed_catalog.seed_catalog`, and `pymongo.errors` at the TOP of the test module is acceptable here (this module is only collected/run, and lazy fixture imports already keep `--collect-only` working via conftest); if top-level imports would break `--collect-only` before the modules exist, defer them into each test body. Use plain asserts with clear messages.
  </action>
  <verify>
    <automated>python -m py_compile tests/test_catalog_schema.py && grep -c 'def test_' tests/test_catalog_schema.py && pytest tests/test_catalog_schema.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_catalog_schema.py` compiles and defines exactly the four functions: `test_catalog_completeness`, `test_seed_idempotent`, `test_schema_validator_rejects_malformed`, `test_query_by_set_and_type`
    - `pytest tests/test_catalog_schema.py --collect-only -q` collects 4 tests without a collection error (lazy conftest imports permit this before implementation exists)
    - Each test consumes the `catalog_db` fixture
    - `test_schema_validator_rejects_malformed` asserts a pymongo write/operation error on an invalid `product_type` (the V5 control assertion)
    - 02-VALIDATION.md Per-Task Verification Map rows updated with this plan's ID/Wave for the four tests
  </acceptance_criteria>
  <done>The four CATALOG contract tests are authored and discoverable (RED until Plan 06 seeds), each mapped to a phase requirement and consuming the shared test-DB fixture.</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Type | Notes |
|----------|------|-------|
| `pytest.ini` | config | `[pytest]` with `pythonpath = .` (resolves scripts/ and db/ packages) |
| `tests/conftest.py` | test fixture | `catalog_db` fixture — dedicated `_test` database, lazy imports, skips when MONGODB_URI unset |
| `tests/test_catalog_schema.py` | test module | 4 tests: `test_catalog_completeness`, `test_seed_idempotent`, `test_schema_validator_rejects_malformed`, `test_query_by_set_and_type` |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test suite → MongoDB test database | Tests write/drop data; must target an isolated `_test` database so they never corrupt real catalog data |
| Malformed document → `products` validator | The rejection test deliberately crosses the validator boundary to prove V5 enforcement |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | `$jsonSchema` validator enforcement | high | mitigate | `test_schema_validator_rejects_malformed` is the automated proof that the V5 input-validation control rejects an invalid `product_type` at the DB layer — converts the mitigation from asserted to verified |
| T-02-05 | Tampering (test isolation) | test database selection | medium | mitigate | Fixture targets a dedicated `_test` database and drops it on setup/teardown, so tests cannot clobber the real `pokemonview` catalog |
| T-02-02 | Information Disclosure | MONGODB_URI in fixture | low | mitigate | Fixture loads MONGODB_URI via python-dotenv from `.env`, never hardcodes or prints the full connection string (Phase 1 secrets-hygiene pattern) |
</threat_model>

<verification>
- `python -m py_compile tests/conftest.py tests/test_catalog_schema.py` succeeds
- `pytest tests/test_catalog_schema.py --collect-only -q` reports 4 tests collected
- `grep -c 'def test_' tests/test_catalog_schema.py` returns 4
- `pytest.ini` sets `pythonpath = .`
</verification>

<success_criteria>
A discoverable four-test pytest scaffold plus a dedicated-test-database fixture exist (RED until seeded), encoding the CATALOG-01/CATALOG-02 contract and the V5 validator-rejection proof.
</success_criteria>

<output>
Create `.planning/phases/02-product-catalog-data-model/02-05-SUMMARY.md` when done
</output>
