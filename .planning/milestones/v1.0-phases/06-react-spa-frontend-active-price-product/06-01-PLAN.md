---
phase: 06-react-spa-frontend-active-price-product
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/index.html
  - frontend/vite.config.js
  - frontend/.gitignore
  - frontend/src/main.jsx
  - frontend/src/setupTests.js
  - frontend/src/styles/tokens.css
  - frontend/src/api/client.js
  - frontend/src/api/client.test.js
autonomous: false
requirements: [SEARCH-01, SEARCH-02]
user_setup: []

must_haves:
  truths:
    - "A Vitest run in frontend/ executes at least one passing test (test harness is real, not stubbed)."
    - "getProducts() calls GET /products and returns the parsed JSON array; getProductDetail(id) calls GET /products/<id> and returns the parsed detail object."
    - "A non-2xx API response causes the client to throw an Error whose message is the API's `error` string (invalid_product_type / not_found) when present."
  artifacts:
    - frontend/package.json
    - frontend/vite.config.js
    - frontend/src/setupTests.js
    - frontend/src/styles/tokens.css
    - frontend/src/api/client.js
    - frontend/src/api/client.test.js
  key_links:
    - "vite.config.js server.proxy '/products' → the local Flask API port (dev-time, avoids relying on CORS)."
    - "api/client.js only ever requests /products and /products/<id> (Flask-CORS is scoped to r\"/products*\" only, CR-02)."
---

<objective>
Stand up the greenfield `frontend/` Vite + React 19 + React Router 7 project, install the exact vetted dependency set (behind a package-legitimacy human gate), wire the Vitest + Testing Library harness (Wave 0 test infrastructure), publish the shared CSS design tokens from the UI-SPEC, and build the two-call REST data layer (`getProducts`, `getProductDetail`) that every later plan fetches through.

Purpose: Everything else in Phase 6 depends on this foundation existing — the build tool, the test harness, the design tokens, and the API client. Without it no component can be written or tested.
Output: A runnable, test-capable `frontend/` project with a working `src/api/client.js` proven by a green `client.test.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-react-spa-frontend-active-price-product/06-RESEARCH.md
@.planning/phases/06-react-spa-frontend-active-price-product/06-PATTERNS.md
@.planning/phases/06-react-spa-frontend-active-price-product/06-VALIDATION.md
@api/blueprints/products.py
</context>

## Artifacts this phase produces (created by this plan)

- New directory `frontend/` (greenfield — did not exist before this phase).
- `frontend/package.json` scripts: `dev` (vite), `build` (vite build), `test` (`vitest run`), `test:watch` (`vitest`).
- `frontend/vite.config.js` — `@vitejs/plugin-react`, `server.proxy` for `/products`, and a `test` block (`environment: 'jsdom'`, `globals: true`, `setupFiles`).
- `frontend/src/setupTests.js` — imports `@testing-library/jest-dom`.
- `frontend/src/styles/tokens.css` — CSS custom properties for the UI-SPEC color / spacing / typography tokens (consumed by every later component's CSS module).
- `frontend/src/api/client.js` exports: `getProducts(filters?)`, `getProductDetail(productId)`.
- `frontend/src/main.jsx` — minimal render root placeholder (rewired to `RouterProvider` in 06-07).

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate for the four [SUS] "too-new" npm packages</name>
  <what-built>Nothing yet — this checkpoint gates the install in Task 2. 06-RESEARCH.md's Package Legitimacy Audit flagged `vite` (8.1.4), `@vitejs/plugin-react` (6.0.3), `react-router` (7.18.1), and `vitest` (4.1.10) as `[SUS]` purely on the `too-new` signal (latest patch published within days of research), each with tens-to-hundreds of millions of weekly downloads from an established GitHub org. Per protocol this verdict is kept and must be human-verified before install; it is the same recent-release false-positive class already approved for pymongo/apscheduler/rapidfuzz/flask/flask-cors in prior phases (see STATE.md decisions).</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/vite — confirm it is the official Vite build tool (repo github.com/vitejs/vite), current latest ~8.1.x.
    2. Open https://www.npmjs.com/package/@vitejs/plugin-react — confirm official Vite React plugin (github.com/vitejs/vite-plugin-react), latest ~6.0.x, peer-requires `vite ^8.0.0`.
    3. Open https://www.npmjs.com/package/react-router — confirm official React Router (github.com/remix-run/react-router); install target is the `version-7` line at 7.18.1, NOT `react-router-dom`.
    4. Open https://www.npmjs.com/package/vitest — confirm official Vitest (github.com/vitest-dev/vitest), latest ~4.1.x.
    Re-verify exact latest versions with `npm view <pkg> version` before installing (06-RESEARCH.md "Valid until 2026-07-22" — the JS ecosystem moves fast; pin whatever is current on the v7 React Router line and current Vite 8 line).
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to install, or name any package to hold.</resume-signal>
  <files>(none — verification-only human gate; no files written by this task)</files>
  <action>STOP: this is a blocking human checkpoint. Do NOT automate or auto-approve (legitimacy gates ignore auto_advance). Present the four package pages from &lt;how-to-verify&gt; to the human, re-run `npm view &lt;pkg&gt; version` for each, and wait for the resume-signal before Task 2 installs anything.</action>
  <verify>
    <human-check>Human has opened each npmjs.com page, confirmed the official repo/owner, and typed "approved".</human-check>
  </verify>
  <done>Human has explicitly approved installing vite, @vitejs/plugin-react, react-router (v7 line), and vitest at the registry-verified current versions.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold frontend/, install vetted deps, configure Vitest harness + design tokens</name>
  <files>frontend/package.json, frontend/index.html, frontend/vite.config.js, frontend/.gitignore, frontend/src/main.jsx, frontend/src/setupTests.js, frontend/src/styles/tokens.css</files>
  <read_first>
    - .planning/phases/06-react-spa-frontend-active-price-product/06-RESEARCH.md (Standard Stack table with exact versions; "Vite dev server proxy" and installation code examples; Pitfall 1 macOS port 5000 → run Flask on 5001 and proxy there; Pitfall 4 VITE_ env-var scoping)
    - .planning/phases/06-react-spa-frontend-active-price-product/06-VALIDATION.md (Test Infrastructure table + Wave 0 Requirements checklist)
    - .planning/phases/06-react-spa-frontend-active-price-product/06-UI-SPEC.md (Design System, Spacing Scale, Typography, Color sections — the exact token values to encode as CSS custom properties)
  </read_first>
  <action>
    Scaffold with `npm create vite@latest frontend -- --template react` (JS, not TS — the codebase and RESEARCH use `.jsx`). Install runtime dep `react-router` pinned to the current v7 line (7.18.1 per RESEARCH, re-verified in Task 1) — install the unified `react-router` package, NOT `react-router-dom` (legacy on v7, removed in v8). Install dev deps: `vitest`, `@testing-library/react`, `@testing-library/jest-dom`, `@testing-library/user-event`, `jsdom` at the RESEARCH-pinned versions. Delete the Vite template boilerplate that this project does not use: `src/App.jsx`, `src/App.css`, `src/assets/`, and template default styling in `src/index.css`. Set `package.json` scripts to `dev`=`vite`, `build`=`vite build`, `test`=`vitest run`, `test:watch`=`vitest` (the `test` script is `vitest run` so no watch mode ever leaks into verify commands). Write `vite.config.js` with `@vitejs/plugin-react` in `plugins`, a `server.proxy` entry mapping `/products` to `http://localhost:5001` with `changeOrigin: true` (Flask runs on 5001 per Pitfall 1), and a `test` block: `environment: 'jsdom'`, `globals: true`, `setupFiles: './src/setupTests.js'`. Write `src/setupTests.js` importing `@testing-library/jest-dom`. Write `src/styles/tokens.css` declaring CSS custom properties on `:root` for every UI-SPEC token: colors (`--color-bg #0B0D12`, `--color-surface #171A21`, `--color-divider #232833`, `--color-accent #E8A33D`, `--color-destructive #D92D20`, `--text-primary #F5F6F8`, `--text-secondary #9AA3B2`, `--text-disabled #6B7280`, trend `--trend-up #34C759`, `--trend-down #FF5C5C`, `--trend-flat #8A94A6`, `--trend-insufficient #5B6472`), spacing (`--space-xs 4px` … `--space-3xl 64px`), and typography (system font stack, `font-variant-numeric: tabular-nums` helper). Reduce `src/main.jsx` to a minimal placeholder that imports `./styles/tokens.css` and renders an empty root div marker (RouterProvider wiring is 06-07's job — do not import react-router here yet). Ensure `.gitignore` ignores `node_modules/` and `dist/`.
  </action>
  <acceptance_criteria>
    - `frontend/vite.config.js` exists and contains `environment: 'jsdom'`, `globals: true`, and a `server.proxy` key referencing `/products` — verify: `grep -q "jsdom" frontend/vite.config.js && grep -q "proxy" frontend/vite.config.js`.
    - `react-router` (not `react-router-dom`) is a dependency — verify: `grep -q '"react-router"' frontend/package.json` and `grep -qv '"react-router-dom"' frontend/package.json` (react-router-dom must be absent).
    - `frontend/src/styles/tokens.css` declares the accent and trend tokens — verify: `grep -q -- '--color-accent' frontend/src/styles/tokens.css && grep -q -- '--trend-up' frontend/src/styles/tokens.css`.
    - The `test` script is exactly `vitest run` (no watch) — verify: `grep -q '"test": "vitest run"' frontend/package.json`.
    - Deleted template files are gone — verify: `test ! -f frontend/src/App.jsx`.
  </acceptance_criteria>
  <verify>
    <automated>test -f frontend/vite.config.js && grep -q "jsdom" frontend/vite.config.js && test ! -f frontend/src/App.jsx && npm --prefix ./frontend ls react-router >/dev/null 2>&1</automated>
  </verify>
  <done>frontend/ is scaffolded with the exact vetted dependency versions installed, boilerplate removed, Vitest/jsdom harness configured via vite.config.js + setupTests.js, and the UI-SPEC design tokens available as CSS custom properties in tokens.css.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: REST data layer — api/client.js (getProducts, getProductDetail)</name>
  <files>frontend/src/api/client.js, frontend/src/api/client.test.js</files>
  <behavior>
    - getProducts() with no args requests exactly `/products` and resolves to the parsed JSON array the fetch returns.
    - getProducts({ product_type: "booster_box" }) requests `/products?product_type=booster_box` (params built via URLSearchParams; supports set / product_type / q for API-shape completeness, though the catalog loader in 06-05 calls it with no args per D-14).
    - getProductDetail("some-id") requests exactly `/products/some-id` and resolves to the parsed detail object.
    - When fetch resolves with `ok: false` and a JSON body `{ "error": "not_found" }`, the returned promise rejects with an Error whose message is `not_found`; when the error body is unparseable, the message falls back to `Request failed: <status>`.
  </behavior>
  <read_first>
    - .planning/phases/06-react-spa-frontend-active-price-product/06-RESEARCH.md ("Native fetch wrapper (no axios needed)" code example — copy its structure)
    - .planning/phases/06-react-spa-frontend-active-price-product/06-PATTERNS.md (frontend/src/api/client.js section: exact error shapes 400 invalid_product_type / 404 not_found, and the r"/products*" CORS constraint)
    - api/blueprints/products.py (the two routes and their exact JSON envelopes this client consumes)
  </read_first>
  <action>
    Implement `src/api/client.js` per the RESEARCH "Native fetch wrapper" example: a private `request(path)` helper using native `fetch` (no axios) against `const BASE = ""` (relative — the Vite dev proxy / same-origin prod handles routing); on `!res.ok` it parses the JSON body (guarded with `.catch(() => ({}))`) and throws `new Error(body.error || \`Request failed: ${res.status}\`)`. Export `getProducts(filters = {})` that builds an optional query string from `set` / `product_type` / `q` via `URLSearchParams` and calls `request(\`/products${qs ? \`?${qs}\` : ""}\`)`, and `getProductDetail(productId)` calling `request(\`/products/${productId}\`)`. Only these two path prefixes may be requested (CORS is scoped to r"/products*" — CR-02). Write `client.test.js` covering the four behaviors above using `vi.spyOn(global, 'fetch')` returning a mock `Response`-like object (`{ ok, status, json: () => Promise.resolve(...) }`) — no MSW (over-infrastructure for 2 endpoints, per VALIDATION Wave 0). Search text and filter values must only ever be encoded through URLSearchParams, never concatenated raw into the path (V5 input handling).
  </action>
  <acceptance_criteria>
    - `getProducts` and `getProductDetail` are named exports — verify: `grep -q "export const getProducts" frontend/src/api/client.js && grep -q "export const getProductDetail" frontend/src/api/client.js`.
    - The client uses native fetch, not axios — verify: `grep -q "fetch(" frontend/src/api/client.js && grep -qv "axios" frontend/src/api/client.js`.
    - Test proves the not_found error string surfaces as the thrown message and the URL-building behavior — the test file asserts on `not_found` and on `/products/` and `product_type=`.
    - `npm --prefix ./frontend run test -- src/api/client.test.js` exits 0 with all client tests passing.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix ./frontend run test -- src/api/client.test.js</automated>
  </verify>
  <done>api/client.js exposes getProducts/getProductDetail as thin native-fetch wrappers over the exact two Flask routes, surfaces the API's error strings on failure, and client.test.js passes green — proving both the data layer and the Vitest harness work end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build | Third-party packages installed into the project (supply-chain surface) |
| React SPA → Flask API | Cross-origin (dev) / same-origin (prod) HTTP; the SPA sends only path + querystring, receives JSON |
| build config → client bundle | Vite inlines `VITE_`-prefixed env vars into client-visible code |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-SC | Tampering | npm installs (vite, @vitejs/plugin-react, react-router, vitest) | high | mitigate | Blocking-human package-legitimacy checkpoint (Task 1) before install; pin exact registry-verified versions; the four flag only on `too-new`, matching the already-approved false-positive class in STATE.md |
| T-06-02 | Information Disclosure | vite.config.js / env handling | low | mitigate | No secret is placed in any `VITE_`-prefixed var; the dev-proxy pattern avoids needing an API-base env var at all (Pitfall 4). This read-only SPA handles no credentials |
| T-06-03 | Spoofing | dev proxy / CORS | low | accept | CORS is already correctly configured server-side in Phase 5 (CR-02, scoped to r"/products*"); this plan's dev proxy is dev-only and introduces no production CORS-bypass |
</threat_model>

<verification>
- `npm --prefix ./frontend run test` runs the Vitest suite and client.test.js passes (proves harness + data layer).
- `frontend/vite.config.js` contains a jsdom `test` block and a `/products` proxy.
- `react-router` present, `react-router-dom` absent, in package.json.
- `frontend/src/styles/tokens.css` declares the UI-SPEC color + spacing tokens.
</verification>

<success_criteria>
The frontend project builds and tests run; the API client fetches both endpoints and surfaces API error strings; design tokens and Vitest harness are in place for all downstream plans.
</success_criteria>

<output>
Create `.planning/phases/06-react-spa-frontend-active-price-product/06-01-SUMMARY.md` when done.
</output>
