---
phase: 06-react-spa-frontend-active-price-product
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - frontend/src/utils/relativeTime.js
  - frontend/src/utils/relativeTime.test.js
  - frontend/src/components/FreshnessIndicator.jsx
  - frontend/src/components/FreshnessIndicator.module.css
  - frontend/src/components/FreshnessIndicator.test.jsx
autonomous: true
requirements: [PRICE-02]
user_setup: []

must_haves:
  truths:
    - "formatRelativeTime(iso) turns an ISO-8601 timestamp into a human 'X ago' string using native Intl.RelativeTimeFormat (PRICE-02, D-09)."
    - "FreshnessIndicator renders the copy 'Data as of {relative time}' from a current_price.as_of value."
  artifacts:
    - frontend/src/utils/relativeTime.js
    - frontend/src/components/FreshnessIndicator.jsx
  key_links:
    - "FreshnessIndicator consumes current_price.as_of (ISO string from ts.isoformat() server-side) directly, no reparsing."
---

<objective>
Build the data-freshness surface (PRICE-02): a dependency-free `formatRelativeTime` utility over the native `Intl.RelativeTimeFormat` API, and a `FreshnessIndicator` component that renders "Data as of {relative time}" from the API's `current_price.as_of` timestamp. Relative time only, never an absolute timestamp (D-09); detail-page only (D-10).

Purpose: Freshness is a locked PRICE-02 requirement and one of the project's core-value guards ("always show whether data is current"). Isolating the formatter keeps the off-by-one/pluralization risk in one tested place instead of hand-rolled per component.
Output: A tested `relativeTime.js` util and a tested `FreshnessIndicator` component.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-react-spa-frontend-active-price-product/06-RESEARCH.md
@.planning/phases/06-react-spa-frontend-active-price-product/06-PATTERNS.md
@.planning/phases/06-react-spa-frontend-active-price-product/06-UI-SPEC.md
</context>

## Artifacts this phase produces (created by this plan)

- `frontend/src/utils/relativeTime.js` — named export `formatRelativeTime(isoString)`.
- `frontend/src/components/FreshnessIndicator.jsx` — default export `FreshnessIndicator({ asOf })`.
- CSS module class `.freshness` (Label-size, secondary text color).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: formatRelativeTime util over Intl.RelativeTimeFormat (Don't Hand-Roll)</name>
  <files>frontend/src/utils/relativeTime.js, frontend/src/utils/relativeTime.test.js</files>
  <behavior>
    - An ISO timestamp ~2 hours before now returns a string containing "hour" (e.g. "2 hours ago") via Intl.RelativeTimeFormat with numeric: "auto".
    - An ISO timestamp ~3 days before now returns a "day"-based string ("3 days ago").
    - An ISO timestamp within the last minute returns a seconds-based relative string (falls through to the "second" unit).
    - The function picks the largest unit whose absolute seconds threshold is met, walking year→month→week→day→hour→minute→second.
  </behavior>
  <read_first>
    - .planning/phases/06-react-spa-frontend-active-price-product/06-RESEARCH.md ("Relative time formatting (Don't Hand-Roll)" code example — near-complete implementation given; Don't Hand-Roll table)
    - .planning/phases/06-react-spa-frontend-active-price-product/06-PATTERNS.md (relativeTime.js section: consumes as_of ISO string directly, no reparsing)
  </read_first>
  <action>
    Implement `export function formatRelativeTime(isoString)` exactly per the RESEARCH "Relative time formatting" example: a module-level `const rtf = new Intl.RelativeTimeFormat("en", { numeric: "auto" })`, a `UNITS` array of `[unit, secondsInUnit]` pairs from year (31536000) down to second (1), compute `diffSeconds = (new Date(isoString).getTime() - Date.now()) / 1000`, then loop and return `rtf.format(Math.round(diffSeconds / secondsInUnit), unit)` for the first unit where `Math.abs(diffSeconds) >= secondsInUnit || unit === "second"`. Do NOT pull in date-fns/dayjs (unnecessary weight for one string) and do NOT hand-roll a manual diff ladder of if/else string concatenation. In `relativeTime.test.js`, construct ISO inputs relative to `Date.now()` (e.g. `new Date(Date.now() - 2*3600*1000).toISOString()`) and assert the output string contains the expected unit word; keep tests deterministic by deriving inputs from the current time rather than hardcoding an absolute date.
  </action>
  <acceptance_criteria>
    - Uses the native Intl API — verify: `grep -q "Intl.RelativeTimeFormat" frontend/src/utils/relativeTime.js`.
    - No date library dependency introduced — verify: `grep -qvi "date-fns\|dayjs\|moment" frontend/src/utils/relativeTime.js`.
    - `formatRelativeTime` is a named export — verify: `grep -q "export function formatRelativeTime" frontend/src/utils/relativeTime.js`.
    - `npm --prefix ./frontend run test -- src/utils/relativeTime.test.js` passes with the hour/day/second cases.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix ./frontend run test -- src/utils/relativeTime.test.js</automated>
  </verify>
  <done>formatRelativeTime converts ISO timestamps to human relative-time strings via the native browser API with no extra dependency, and its test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FreshnessIndicator component ("Data as of {relative time}") — PRICE-02, D-09/D-10</name>
  <files>frontend/src/components/FreshnessIndicator.jsx, frontend/src/components/FreshnessIndicator.module.css, frontend/src/components/FreshnessIndicator.test.jsx</files>
  <behavior>
    - Given an `asOf` ISO string ~2 hours old, renders text matching "Data as of" followed by the relative time (e.g. "Data as of 2 hours ago").
    - Renders using Label typography (12/400) in secondary text color per the UI-SPEC.
    - It delegates all time math to formatRelativeTime — it does not compute durations itself.
  </behavior>
  <read_first>
    - .planning/phases/06-react-spa-frontend-active-price-product/06-UI-SPEC.md (Copywriting Contract: "Data as of {relative time}"; Layout notes Freshness bullet — detail page only, Label role, secondary text)
    - frontend/src/utils/relativeTime.js (the formatter this component calls — created in Task 1 of this plan)
    - .planning/phases/06-react-spa-frontend-active-price-product/06-PATTERNS.md (FreshnessIndicator section: D-09/D-10 detail-page-only rule)
  </read_first>
  <action>
    Implement `FreshnessIndicator({ asOf })` as a default export that renders a single element with class `.freshness` containing the literal copy `Data as of ` concatenated with `formatRelativeTime(asOf)` (imported from `../utils/relativeTime`). Never render an absolute timestamp (D-09). This component is only mounted on the product detail page (D-10) — do not add it to list rows. In `FreshnessIndicator.module.css`, style `.freshness` with the Label size (12px/400), `var(--text-secondary)` color. In `FreshnessIndicator.test.jsx`, render with an `asOf` derived from `Date.now() - 2 hours` and assert the output contains both "Data as of" and "hour".
  </action>
  <acceptance_criteria>
    - Renders the exact caption prefix "Data as of" — asserted in the test file.
    - Imports and uses formatRelativeTime rather than doing its own date math — verify: `grep -q "formatRelativeTime" frontend/src/components/FreshnessIndicator.jsx` and `grep -qv "Intl.RelativeTimeFormat" frontend/src/components/FreshnessIndicator.jsx`.
    - `npm --prefix ./frontend run test -- src/components/FreshnessIndicator.test.jsx` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix ./frontend run test -- src/components/FreshnessIndicator.test.jsx</automated>
  </verify>
  <done>FreshnessIndicator renders "Data as of {relative time}" from an as_of timestamp using the shared formatter, styled per the UI-SPEC, and its test passes — satisfying PRICE-02 / D-09.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API timestamp → rendered DOM | `current_price.as_of` (ISO string) parsed by `new Date()` and rendered as text |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering / Information Disclosure | FreshnessIndicator | low | mitigate | The relative-time string is rendered via JSX text interpolation (auto-escaped); `asOf` is only ever passed to `new Date()` / Intl formatting, never to any raw-HTML sink (V5) |
</threat_model>

<verification>
- `npm --prefix ./frontend run test -- src/utils/relativeTime.test.js` green.
- `npm --prefix ./frontend run test -- src/components/FreshnessIndicator.test.jsx` green.
</verification>

<success_criteria>
The freshness formatter and indicator render relative "X ago" copy from the API's as_of timestamp with no extra dependency and pass their tests, delivering PRICE-02.
</success_criteria>

<output>
Create `.planning/phases/06-react-spa-frontend-active-price-product/06-03-SUMMARY.md` when done.
</output>
