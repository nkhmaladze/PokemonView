---
phase: 04-listing-matching-price-normalization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
autonomous: false
requirements: [MATCH-01]
must_haves:
  truths:
    - "`import rapidfuzz` succeeds in the project environment and reports version 3.14.5"
    - "requirements.txt pins rapidfuzz==3.14.5 using the project's exact-pin convention"
    - "The [SUS] verdict on rapidfuzz was reviewed and approved by a human before install (T-04-SC)"
  artifacts:
    - "requirements.txt (rapidfuzz==3.14.5 appended after apscheduler==3.11.3)"
  key_links:
    - "rapidfuzz becomes importable by scripts/matching.py's Tier-2 fuzzy fallback (Plan 04-03) and transitively by scripts/ingest_worker.py once Plan 04-05 wires matching in"
---

<objective>
Install `rapidfuzz==3.14.5` — the C++-backed fuzzy-string library ARCHITECTURE.md specifies for this phase's Tier-2 fuzzy fallback matching (MATCH-01) — gated behind a blocking human legitimacy checkpoint, exactly as `apscheduler`/`pymongo` were handled in prior phases.

Purpose: rapidfuzz is a hard dependency of `scripts/matching.py` (Plan 04-03's Tier-2 `match_listing` fuzzy fallback). It carries a `[SUS]` legitimacy verdict caused solely by the legitimacy tool failing to resolve a download-count telemetry signal in this environment — the same known false-positive class already reviewed and approved for `apscheduler` (03-RESEARCH.md) and `pymongo` (02-RESEARCH.md). Protocol still requires a blocking human verification before install.

Output: `rapidfuzz==3.14.5` pinned in `requirements.txt` and installed/importable.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-listing-matching-price-normalization/04-RESEARCH.md
@requirements.txt
</context>

## Artifacts this phase produces (this plan's slice)

- `requirements.txt` — new pinned dependency line: `rapidfuzz==3.14.5`.

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate — rapidfuzz==3.14.5</name>
  <read_first>
    - .planning/phases/04-listing-matching-price-normalization/04-RESEARCH.md (the `## Package Legitimacy Audit` section — the rapidfuzz row, the `[SUS]` note, and the `Postinstall script check: N/A` line)
    - .planning/STATE.md (Accumulated Context — the prior [Phase 02]/[Phase 03] entries approving pymongo and apscheduler after human review confirmed the same [SUS] false positive)
  </read_first>
  <action>
    Do NOT install anything yet. Present the `rapidfuzz==3.14.5` legitimacy assessment to the human and BLOCK until an explicit approve/reject decision is given (T-04-SC). Per the Package Legitimacy Audit: registry = PyPI; canonical source repo = `github.com/rapidfuzz/RapidFuzz` (actively maintained, 100+ historical releases, the exact package ARCHITECTURE.md specified); verdict = `[SUS]` for reason `unknown-downloads` only (the legitimacy tool could not retrieve download stats in this environment — a telemetry gap, not a supply-chain signal); Python wheels ship prebuilt with no npm-style postinstall hook / no arbitrary install-time code execution. This gate is never auto-approvable — `workflow.auto_advance` is ignored for package-legitimacy checkpoints. Record the approve/reject decision in the plan SUMMARY (mirroring the apscheduler/pymongo approval notes in STATE.md). Only on approval does Task 2 proceed.
  </action>
  <what-built>
    Nothing is installed yet. This checkpoint presents the `rapidfuzz==3.14.5` legitimacy assessment for human sign-off before any `pip install` runs.
  </what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/rapidfuzz/3.14.5/ and confirm the release exists, is published by the RapidFuzz maintainers, and links to `github.com/rapidfuzz/RapidFuzz`.
    2. Confirm this is the same `[SUS]`/`unknown-downloads` false-positive class already approved in this project for `apscheduler` and `pymongo` (see STATE.md entries in the read_first).
    3. Approve to proceed with `pip install rapidfuzz==3.14.5`, or reject and stop the phase.
  </how-to-verify>
  <acceptance_criteria>
    - A human has explicitly approved the rapidfuzz install (or rejected it). This checkpoint is NOT auto-approvable — `workflow.auto_advance` is ignored for package-legitimacy gates.
    - The approval decision is recorded in the plan SUMMARY (mirroring the apscheduler/pymongo approval notes in STATE.md).
  </acceptance_criteria>
  <verify>Human types "approved" (or rejects); the decision is recorded in 04-01-SUMMARY.md before Task 2 runs.</verify>
  <resume-signal>Type "approved" to proceed with the install, or describe the concern to halt.</resume-signal>
  <done>A human has explicitly approved (or rejected) the rapidfuzz==3.14.5 install, and the decision is recorded in the SUMMARY. No package was installed during this task.</done>
</task>

<task type="auto">
  <name>Task 2: Pin and install rapidfuzz==3.14.5</name>
  <files>requirements.txt</files>
  <read_first>
    - requirements.txt (current 5 pinned lines — append, do not reorder or re-pin existing entries)
    - .planning/phases/04-listing-matching-price-normalization/04-RESEARCH.md (`## Standard Stack` → Installation block confirming `rapidfuzz==3.14.5` is the registry-latest as of 2026-07-14)
  </read_first>
  <action>
    Append the exact line `rapidfuzz==3.14.5` to `requirements.txt` as a new final line, using the same exact-version-pin style (`==`) as the existing five lines (`requests==2.34.2`, `python-dotenv==1.2.2`, `pymongo==4.17.0`, `pytest==8.4.2`, `apscheduler==3.11.3`). Do not modify or reorder any existing pin. Then run `pip install rapidfuzz==3.14.5` (equivalently `pip install -r requirements.txt`) in the same interpreter environment the existing dependencies are installed into (the one `pytest` runs under). This proceeds ONLY after Task 1's human approval of the T-04-SC supply-chain gate.
  </action>
  <verify>
    <automated>python -c "import rapidfuzz; print(rapidfuzz.__version__)"</automated>
  </verify>
  <acceptance_criteria>
    - `requirements.txt` contains a line reading exactly `rapidfuzz==3.14.5`.
    - `python -c "import rapidfuzz, rapidfuzz.fuzz, rapidfuzz.process; print(rapidfuzz.__version__)"` prints `3.14.5` and exits 0.
    - `python -c "from rapidfuzz import fuzz, process; print(hasattr(fuzz, 'token_sort_ratio'), hasattr(process, 'extract'))"` prints `True True` (the two symbols Plan 04-03's Tier-2 fallback depends on).
  </acceptance_criteria>
  <done>rapidfuzz==3.14.5 is pinned in requirements.txt and importable, exposing fuzz.token_sort_ratio and process.extract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI registry → local environment | An external package is downloaded and installed into the environment that runs ingestion/matching |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-SC | Tampering | `pip install rapidfuzz==3.14.5` | high | mitigate | Blocking human legitimacy checkpoint (Task 1, `gate="blocking-human"`, never auto-approvable) before install; verified against pypi.org + github.com/rapidfuzz/RapidFuzz; prebuilt wheels have no postinstall code-execution hook |

Security note: `security_block_on: high` — T-04-SC is high and is mitigated by the blocking checkpoint, so this plan clears the gate only after human approval.
</threat_model>

<verification>
- `requirements.txt` diff adds exactly one line (`rapidfuzz==3.14.5`), no other lines changed.
- `python -c "import rapidfuzz; print(rapidfuzz.__version__)"` → `3.14.5`.
- Task 1 human approval recorded in SUMMARY.
</verification>

<success_criteria>
rapidfuzz==3.14.5 is pinned and importable; the [SUS] verdict was human-reviewed and approved before install.
</success_criteria>

<output>
Create `.planning/phases/04-listing-matching-price-normalization/04-01-SUMMARY.md` when done.
</output>
