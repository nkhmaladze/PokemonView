---
phase: 04-listing-matching-price-normalization
plan: 04
type: execute
wave: 3
depends_on: [04-03]
files_modified:
  - scripts/matching.py
autonomous: true
requirements: [MATCH-03]
must_haves:
  truths:
    - "filter_outliers() excludes listings more than 2 population std devs from the median of total_price (D-13), returns everything included when the group has fewer than 3 listings (D-14), and computes stats from only the passed (current-run) group (D-15)"
    - "aggregate_and_write() writes exactly ONE price_points document per product per run with independent medians for item_price and total_price (D-09/D-10/D-12), keyed ts=passed timestamp and product_id=matched slug, and returns False writing nothing when the included set is empty (D-11)"
    - "run_matching_once() reads active_listings by run_id, matches + excludes each listing in place via bulk_write, flags outlier-excluded listings with exclusion_reason='outlier' (D-16), writes one aggregate per product, and returns {listings_matched, listings_unmatched, listings_excluded} (D-04)"
    - "A listing with a missing/malformed title is isolated by a per-item try/except and does not abort the run (T-04-01)"
  artifacts:
    - "scripts/matching.py (filter_outliers, aggregate_and_write, run_matching_once + OUTLIER_N_STD=2, OUTLIER_MIN_COUNT=3)"
  key_links:
    - "run_matching_once queries db.active_listings.find({'run_id': run_id}) — the run-scoped batch Phase 3 stamps"
    - "price_points.product_id == matched_product_id == products._id slug, enabling Phase 5's price/trend joins"
    - "the returned count dict is what Plan 04-05 merges into the ingestion_runs document (D-04)"
---

<objective>
Implement the statistical and orchestration layer of `scripts/matching.py` — `filter_outliers()`, `aggregate_and_write()`, and `run_matching_once()` — turning the MATCH-03 and end-to-end matching tests GREEN.

Purpose: Satisfies MATCH-03 (statistical outliers excluded from the aggregate) and ROADMAP SC-4 (current price derived only from matched, non-excluded, non-outlier listings), aggregating included listings into one median-based `price_points` document per product per run (D-09…D-16) and returning the per-run match/exclude counts (D-04). This composes Plan 04-03's pure functions into the run-scoped pipeline the ingestion worker will invoke (Plan 04-05).

Output: `scripts/matching.py` completed with the outlier filter, the gap-on-empty median aggregation, and the `run_matching_once` orchestrator. Writes to `active_listings` (in place) and `price_points`. No manual-override tooling (CLI script or admin UI) is built for correcting a match/exclusion false positive — per D-07, the documented fix path is a direct MongoDB document edit to `match_status`/`exclusion_reason`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-listing-matching-price-normalization/04-CONTEXT.md
@.planning/phases/04-listing-matching-price-normalization/04-RESEARCH.md
@.planning/phases/04-listing-matching-price-normalization/04-PATTERNS.md
@scripts/matching.py
@scripts/ingest_worker.py
@db/init_collections.py
@tests/test_matching.py
</context>

## Artifacts this phase produces (this plan's slice)

- `scripts/matching.py` functions: `filter_outliers(listings, price_field="total_price") -> (included, excluded)`, `aggregate_and_write(db, product_id, included, ts) -> bool`, `run_matching_once(db, run_id, ts) -> dict`.
- Module-level constants: `OUTLIER_N_STD = 2` (D-13), `OUTLIER_MIN_COUNT = 3` (D-14 interpretation).
- New `active_listings` field value: `exclusion_reason="outlier"` (D-16) plus `match_status`/`matched_product_id`/`match_method`/`match_score`/`exclusion_reason`/`matched_at` written in place.
- New `price_points` documents: `{ts, product_id, item_price, total_price}` (one per product per run with ≥1 included listing).
- Return contract for D-04: `{"listings_matched": int, "listings_unmatched": int, "listings_excluded": int}`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: filter_outliers() and aggregate_and_write() (MATCH-03, D-09..D-16)</name>
  <files>scripts/matching.py</files>
  <behavior>
    - filter_outliers([5 listings: 4 tight + 1 far], "total_price") → (included=the 4 tight, excluded=[the far one]); far means |price - median| > 2*pstdev.
    - filter_outliers([2 listings], ...) → (both included, [] excluded) — skipped entirely below OUTLIER_MIN_COUNT (D-14).
    - filter_outliers([listings all identical price], ...) → (all included, []) — pstdev 0, nothing is an outlier.
    - aggregate_and_write(db, "perfect-order_booster_box", <odd included set>, ts) → writes exactly one price_points doc {ts, product_id, item_price=median(item_prices), total_price=median(total_prices)} (independent medians), returns True.
    - aggregate_and_write(db, product_id, [], ts) → returns False, writes nothing (D-11 gap).
  </behavior>
  <read_first>
    - scripts/matching.py (current state after Plan 04-03 — append to it; reuse its import block and add `import statistics`)
    - .planning/phases/04-listing-matching-price-normalization/04-RESEARCH.md (Pattern 3 filter_outliers code example with OUTLIER_N_STD=2 / OUTLIER_MIN_COUNT=3 / pstdev / sd==0 guard; Pattern 4 aggregate_and_write code example and the locked `{ts, product_id, item_price, total_price}` shape; Pitfall 5 — filter on total_price, never item_price; the Don't-Hand-Roll note on using stdlib statistics.median/pstdev)
    - .planning/phases/04-listing-matching-price-normalization/04-CONTEXT.md (D-09 one point per product per run, D-10 median, D-11 skip-write on empty, D-12 independent medians, D-13 two std dev, D-14 skip-below-3, D-15 current-run-only)
    - db/init_collections.py (price_points is a time-series collection, timeField "ts", metaField "product_id", granularity hours, no validator — insert_one with the locked doc shape)
    - tests/test_matching.py (assertions of test_outlier_filter_excludes_far_listings, test_outlier_filter_skipped_when_too_few, test_price_points_median_aggregation, test_price_points_skipped_when_zero_included)
  </read_first>
  <action>
    Append to `scripts/matching.py` (add `import statistics` to the top import block). Define `OUTLIER_N_STD = 2` (D-13) and `OUTLIER_MIN_COUNT = 3` (D-14) as named constants.

    Implement `filter_outliers(listings, price_field="total_price") -> tuple[list, list]` per RESEARCH Pattern 3: if `len(listings) < OUTLIER_MIN_COUNT`, return `(listings, [])` (skip filtering entirely — D-14); compute `med = statistics.median(prices)` and `sd = statistics.pstdev(prices)` over `[l[price_field] for l in listings]`; if `sd == 0`, return `(listings, [])` (identical prices — nothing is an outlier); otherwise partition into `included`/`excluded` where a listing is excluded iff `abs(listing[price_field] - med) > OUTLIER_N_STD * sd`. The default `price_field` MUST be `"total_price"` (the canonical landed-cost field — never `item_price`, RESEARCH Pitfall 5). Statistics are computed only over the passed `listings` (the current run's group — D-15), never a rolling historical window.

    Implement `aggregate_and_write(db, product_id, included, ts) -> bool` per RESEARCH Pattern 4: if `included` is empty return `False` and write nothing (D-11 — leave a gap, never carry forward); otherwise `db.price_points.insert_one({"ts": ts, "product_id": product_id, "item_price": statistics.median(l["item_price"] for l in included), "total_price": statistics.median(l["total_price"] for l in included)})` — the two medians computed INDEPENDENTLY over item_price and total_price (D-12), and return `True`. Exactly one document per product per run (D-09).
  </action>
  <verify>
    <automated>pytest tests/test_matching.py::test_outlier_filter_excludes_far_listings tests/test_matching.py::test_outlier_filter_skipped_when_too_few tests/test_matching.py::test_price_points_median_aggregation tests/test_matching.py::test_price_points_skipped_when_zero_included -x</automated>
  </verify>
  <acceptance_criteria>
    - The four named MATCH-03 tests pass (exit 0).
    - `python -c "from scripts.matching import filter_outliers; print(filter_outliers([{'total_price':1.0},{'total_price':1.0}]))"` returns both listings included and an empty excluded list (below-min-count skip).
    - `python -c "import inspect,scripts.matching as m; print('total_price' in inspect.signature(m.filter_outliers).parameters['price_field'].default)"` confirms the default price_field is total_price (or equivalent proof the default is 'total_price').
    - test_price_points_median_aggregation proves item_price and total_price are independent medians (distinct values), and test_price_points_skipped_when_zero_included proves the empty-set write is skipped.
  </acceptance_criteria>
  <done>filter_outliers (2-pstdev on total_price, skip-below-3, current-run-only) and aggregate_and_write (independent medians, gap-on-empty) are implemented; the four MATCH-03 tests are GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: run_matching_once() orchestration + D-16 outlier flagging + D-04 counts</name>
  <files>scripts/matching.py</files>
  <behavior>
    - run_matching_once(db, "RUN-E2E", ts) over a seeded batch: each active_listings doc is updated in place with match_status/matched_product_id/match_method/match_score/exclusion_reason/matched_at; matched-and-not-keyword-excluded listings are grouped by matched_product_id; each group is outlier-filtered; outlier-excluded listings get exclusion_reason="outlier"; one price_points doc per product with ≥1 included listing; returns {listings_matched, listings_unmatched, listings_excluded}.
    - A listing whose title is missing/None does not abort the batch (per-item try/except); it is counted as unmatched and still updated with match_status="unmatched".
  </behavior>
  <read_first>
    - scripts/matching.py (current state after Task 1 — normalize/match_listing/check_exclusion/filter_outliers/aggregate_and_write all present)
    - scripts/ingest_worker.py (upsert_listings lines ~120-168: the exact `bulk_write([UpdateOne({"_id": ...}, {"$set": {...}}, ...)])` idiom + per-item `try/except (KeyError, ValueError, TypeError): continue` isolation to mirror; run_ingestion_once's counter-accumulate-then-return shape)
    - .planning/phases/04-listing-matching-price-normalization/04-RESEARCH.md (the `run_matching_once` orchestration code example in `## Code Examples`, including the two-bulk_write structure (match/exclusion fields first, then outlier exclusions) and the by_product grouping of match_status=="matched" AND exclusion_reason is None; the count semantics)
    - .planning/phases/04-listing-matching-price-normalization/04-PATTERNS.md (Shared Patterns: idempotent bulk_write-by-_id, per-item try/except isolation, flat run-observability counters)
    - .planning/phases/04-listing-matching-price-normalization/04-CONTEXT.md (D-02 store unmatched with match_status, D-05 keep excluded flagged, D-16 outlier reason, D-04 return counts)
    - tests/test_matching.py (assertions of test_run_matching_once_end_to_end — the expected per-listing flags, the single price_points doc + its median, and the returned count dict)
  </read_first>
  <action>
    Append `run_matching_once(db, run_id, ts) -> dict` to `scripts/matching.py`, following RESEARCH's Code-Examples orchestration exactly, with these locked semantics:

    1. Read the run-scoped batch: `listings = list(db.active_listings.find({"run_id": run_id}))` (D-15 / A5 — only this run's touched listings).
    2. Per-listing loop, each iteration wrapped in a `try/except (KeyError, ValueError, TypeError)` that isolates a malformed/missing-title document (mirror upsert_listings; a bad doc must not abort the run — T-04-01): `normalized = normalize(listing["title"])`; `result = match_listing(normalized)`; `exclusion_reason = check_exclusion(normalized)`; build the `$set` update `{**result, "exclusion_reason": exclusion_reason, "matched_at": datetime.now(timezone.utc)}` and append `UpdateOne({"_id": listing["_id"]}, {"$set": update})` to a `match_ops` list. Accumulate counts: `listings_matched` counts docs with match_status=="matched"; `listings_unmatched` counts match_status=="unmatched"; a matched listing whose keyword `exclusion_reason` is not None is added to `listings_excluded`; only matched-AND-not-excluded listings are appended to `by_product[matched_product_id]` (carry the merged doc so the group has item_price/total_price).
    3. `if match_ops: db.active_listings.bulk_write(match_ops)` (single batched write).
    4. Per product group: `included, excluded = filter_outliers(group)`; add `len(excluded)` to `listings_excluded`; for each outlier-excluded listing append `UpdateOne({"_id": listing["_id"]}, {"$set": {"exclusion_reason": "outlier"}})` (D-16) to an `outlier_ops` list; call `aggregate_and_write(db, product_id, included, ts)`.
    5. `if outlier_ops: db.active_listings.bulk_write(outlier_ops)`.
    6. Return `{"listings_matched": ..., "listings_unmatched": ..., "listings_excluded": ...}` (D-04).

    Import `from datetime import datetime, timezone` and `from pymongo import UpdateOne` at module top level (extend the existing import block). Never log full listing titles at info level (data hygiene).
  </action>
  <verify>
    <automated>pytest tests/test_matching.py::test_run_matching_once_end_to_end -x</automated>
  </verify>
  <acceptance_criteria>
    - test_run_matching_once_end_to_end passes (exit 0): the seeded clean listings become matched with the box slug and no exclusion; lot/damaged/counterfeit listings carry their reason; the far listing carries exclusion_reason="outlier"; ambiguous and nonsense listings are match_status="unmatched"; exactly one price_points doc exists for "perfect-order_booster_box" whose total_price equals the median of the clean included listings; the returned dict has integer counts.
    - `pytest tests/test_matching.py -x` (the full matching file) passes GREEN — every test authored in Plan 04-02 except test_run_ingestion_once_records_match_counts (which turns green in Plan 04-05).
    - A malformed listing (title missing) does not raise out of run_matching_once — covered by the end-to-end test including a defensive case, or add an assertion that a None/absent-title doc is still updated to match_status="unmatched".
  </acceptance_criteria>
  <done>run_matching_once composes match/exclusion/outlier/aggregation over the run batch, flags outliers (D-16), returns D-04 counts, and isolates malformed documents; all matching tests are GREEN except the ingest_worker integration test (Plan 04-05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eBay-originated active_listings documents → run_matching_once batch loop | Untrusted stored documents (possibly missing/malformed `title`) are iterated and written back |
| matching module → price_points time-series collection | This phase is the first writer of price_points; a wrong product_id would corrupt the metaField contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Denial of Service | run_matching_once per-listing loop on a malformed/missing `title` | medium | mitigate | Per-item `try/except (KeyError, ValueError, TypeError)` isolates one bad document (mirrors upsert_listings) so it does not abort the whole run's matching stage (V5 Input Validation) |
| T-04-04 | Tampering | Wrong product_id written to price_points metaField | medium | mitigate | matched_product_id is the `_product_slug` == products._id (Plan 04-03); aggregate_and_write only ever writes that slug as product_id, keeping the time-series metaField contract intact |
| T-04-03 | Tampering | Manual MongoDB edit of exclusion_reason/match_status (no validator on active_listings) | low | accept | Intentional per D-07 (direct-edit fix path); no validator added this phase |

Security note: `security_block_on: high` — no high/critical threats open; the two medium threats are mitigated.
</threat_model>

<verification>
- `pytest tests/test_matching.py -x` passes for all MATCH-01/02/03 + run_matching_once tests (test_run_ingestion_once_records_match_counts remains RED until Plan 04-05 — expected).
- `pytest` (full suite) — Phase 2/3 tests remain green; ingest_worker is NOT yet modified, so its tests are unaffected at this wave.
- price_points doc count for the aggregated product is exactly 1 per run; independent item/total medians proven by test.
</verification>

<success_criteria>
filter_outliers, aggregate_and_write, and run_matching_once are implemented per D-09…D-16 and D-04; MATCH-03 and the run_matching_once end-to-end tests are GREEN; malformed documents are isolated; price_points.product_id equals the products._id slug.
</success_criteria>

<output>
Create `.planning/phases/04-listing-matching-price-normalization/04-04-SUMMARY.md` when done.
</output>
