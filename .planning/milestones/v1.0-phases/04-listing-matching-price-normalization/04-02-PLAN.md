---
phase: 04-listing-matching-price-normalization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/conftest.py
  - tests/test_matching.py
autonomous: true
requirements: [MATCH-01, MATCH-02, MATCH-03]
must_haves:
  truths:
    - "`pytest --collect-only tests/test_matching.py` exits 0 before scripts/matching.py exists (every module-under-test import is deferred into each test body)"
    - "The matching_db fixture resets active_listings, price_points, ingestion_runs, and ingestion_locks before and after each test, and skips (not errors) when MONGODB_URI is unset"
    - "Running tests/test_matching.py fails RED only because scripts/matching.py and its functions do not yet exist — not from fixture-not-found, syntax, or collection errors"
  artifacts:
    - "tests/test_matching.py (RED unit + integration tests for MATCH-01/02/03 and the run_matching_once / run_ingestion_once end-to-end cases)"
    - "tests/conftest.py (new matching_db fixture)"
  key_links:
    - "matching_db fixture is consumed by the outlier/aggregation/end-to-end tests"
    - "test node IDs match RESEARCH.md's Phase Requirements → Test Map and 04-VALIDATION.md's Per-Task Verification Map"
    - "Plans 04-03/04-04/04-05 turn these RED tests GREEN wave by wave"
---

<objective>
Author the Nyquist RED test scaffold for Phase 4 — a new `matching_db` fixture plus failing contract tests in `tests/test_matching.py` covering matching (MATCH-01), exclusion (MATCH-02), outlier filtering + price aggregation (MATCH-03), the `run_matching_once` end-to-end pass, and the `run_ingestion_once` match-count integration (D-04) — before any implementation exists.

Purpose: This project's established convention (Phase 3's 03-03-PLAN.md) authors failing tests against a dedicated test-db fixture first, then turns them GREEN in later waves. Matching/exclusion/outlier correctness is exactly the price-accuracy-critical logic that pattern exists for. Writing the tests first pins the exact behavioral contract (function signatures, return shapes, field names, D-01…D-16 semantics) that Plans 04-03/04-04/04-05 must satisfy.

Output: `tests/test_matching.py` (RED) + `matching_db` fixture in `tests/conftest.py`. Tests are collectible now and fail only because `scripts/matching.py` does not yet exist.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-listing-matching-price-normalization/04-CONTEXT.md
@.planning/phases/04-listing-matching-price-normalization/04-RESEARCH.md
@.planning/phases/04-listing-matching-price-normalization/04-PATTERNS.md
@.planning/phases/04-listing-matching-price-normalization/04-VALIDATION.md
@tests/conftest.py
@tests/test_ingest_worker.py
@scripts/catalog_data.py
@scripts/ingest_worker.py
</context>

## Artifacts this phase produces (this plan's slice)

- `matching_db` pytest fixture (tests/conftest.py) — a live `pokemonview_test` DB handle resetting `active_listings`, `price_points`, `ingestion_runs`, `ingestion_locks`.
- `tests/test_matching.py` — RED contract tests. Test node IDs (exact, from RESEARCH.md's Test Map):
  - MATCH-01: `test_match_listing_keyword_exact`, `test_match_listing_fuzzy_fallback`, `test_match_listing_ambiguous_unmatched`
  - MATCH-02: `test_exclusion_lot`, `test_exclusion_damaged_severe_only`, `test_exclusion_counterfeit`, `test_exclusion_does_not_flag_legitimate_bundle`
  - MATCH-03: `test_outlier_filter_excludes_far_listings`, `test_outlier_filter_skipped_when_too_few`, `test_price_points_median_aggregation`, `test_price_points_skipped_when_zero_included`
  - End-to-end: `test_run_matching_once_end_to_end`, `test_run_ingestion_once_records_match_counts`
  - Plus a `normalize` unit test: `test_normalize_word_boundary_preserves_resealed`

<tasks>

<task type="auto">
  <name>Task 1: Add the matching_db fixture to conftest.py</name>
  <files>tests/conftest.py</files>
  <read_first>
    - tests/conftest.py (the existing `ingest_db` fixture, lines ~89-153 — the exact skeleton to copy: `pytest.skip` when MONGODB_URI unset, `try/except: client.close(); raise` on setup failure, lazy `from db.init_collections import init_collections`, before-and-after `drop_collection` teardown, the module-level `TEST_DB_NAME = "pokemonview_test"` constant)
    - db/init_collections.py (confirms init_collections creates products, price_points, active_listings, ingestion_locks, ingestion_runs — so dropping + re-init is safe)
    - .planning/phases/04-listing-matching-price-normalization/04-PATTERNS.md (the `tests/conftest.py (extend — new matching_db fixture)` section)
  </read_first>
  <action>
    Add a new `matching_db` fixture to `tests/conftest.py` by copying the `ingest_db` fixture's skeleton verbatim and swapping the collection set. Reuse the existing module-level `TEST_DB_NAME` constant — do NOT redefine it. The fixture MUST drop these four collections both before `yield` and in teardown: `active_listings`, `price_points`, `ingestion_runs`, and `ingestion_locks` (the extra `price_points` is needed for aggregation-write tests, and `ingestion_locks` is needed because `test_run_ingestion_once_records_match_counts` drives `run_ingestion_once`, which acquires the ingestion lock). Keep the exact `pytest.skip("MONGODB_URI not set — ...")` guard, the `try/except: client.close(); raise` setup-failure safety, and the lazy `from db.init_collections import init_collections` inside the fixture body. Call `init_collections(test_db)` after the pre-drop so `price_points` is (re)created as a fresh time-series collection each test. Do NOT seed catalog products — matching reads `scripts.catalog_data.CATALOG` directly, not the products collection.
  </action>
  <verify>
    <automated>python -c "import ast,sys; ast.parse(open('tests/conftest.py').read()); print('conftest OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/conftest.py` defines a `matching_db` pytest fixture (a `@pytest.fixture`-decorated generator).
    - The fixture drops `active_listings`, `price_points`, `ingestion_runs`, and `ingestion_locks` both before yield and in teardown, and calls `init_collections(test_db)` between the pre-drop and yield.
    - `TEST_DB_NAME` is referenced, not redefined.
    - `pytest --collect-only -q` exits 0 (conftest imports cleanly and does not break collection of the existing suite).
  </acceptance_criteria>
  <done>A matching_db fixture exists mirroring ingest_db, targeting active_listings/price_points/ingestion_runs/ingestion_locks, skipping gracefully when MONGODB_URI is unset.</done>
</task>

<task type="auto">
  <name>Task 2: RED unit tests — normalize, match_listing, check_exclusion (MATCH-01, MATCH-02)</name>
  <files>tests/test_matching.py</files>
  <read_first>
    - tests/test_ingest_worker.py (the deferred-import convention — every `from scripts.ingest_worker import ...` lives INSIDE each test body, never at module top level, so `pytest --collect-only` succeeds before the module exists; and the pure-unit `test_build_query` pattern, no fixture)
    - scripts/catalog_data.py (the CATALOG entries and their `required_keywords` per product type — e.g. Chaos Rising etb keywords `["chaos rising", "elite trainer box", "etb"]`, Chaos Rising booster_box `["chaos rising", "booster box", "display"]`, Chaos Rising booster_bundle `["chaos rising", "booster bundle", "bundle"]`; NOTE: CATALOG entries have NO `_id` field — the product id is the slug `{set_name}_{product_type}` lowercased with spaces→hyphens)
    - scripts/seed_catalog.py (line ~70: the slug `_id` derivation `f"{doc['set_name']}_{doc['product_type']}".lower().replace(" ", "-")` — the exact value `matched_product_id` must equal)
    - .planning/phases/04-listing-matching-price-normalization/04-RESEARCH.md (Pattern 1 `match_listing` return-dict shape `{match_status, matched_product_id, match_method, match_score}`; Pattern 2 `check_exclusion` return `"lot" | "damaged" | "counterfeit" | None`; the "never exclude the bare word 'bundle'" and D-08 cosmetic-not-excluded rules)
    - .planning/phases/04-listing-matching-price-normalization/04-CONTEXT.md (D-01, D-03, D-06, D-08)
  </read_first>
  <action>
    Create `tests/test_matching.py` with a module docstring stating this is the RED scaffold (authored here, turned GREEN by Plans 04-03/04-04/04-05), plain pytest `assert` style, deferred `from scripts.matching import ...` inside every test body. Write these fixture-free unit tests:

    `test_normalize_word_boundary_preserves_resealed`: `normalize("Resealed Chaos Rising ETB")` must still contain the token `resealed` (asserting filler-stripping uses word-boundary regex, not substring `.replace`, so "sealed" inside "resealed" is not destroyed — RESEARCH Pitfall 2).

    `test_match_listing_keyword_exact` (MATCH-01, D-01): a title containing all of a single product's `required_keywords` (e.g. a normalized Chaos Rising ETB title) returns `match_status="matched"`, `match_method="keyword"`, and `matched_product_id` equal to the slug `"chaos-rising_etb"` (the exact `products._id` value seed_catalog produces). Assert `matched_product_id == "chaos-rising_etb"` literally.

    `test_match_listing_fuzzy_fallback` (MATCH-01): a near-miss title with zero exact keyword candidates but clearly closest to one product resolves via `match_method="fuzzy"` with `match_status="matched"` and a numeric `match_score`. (Choose a title that misses an exact `required_keywords` token yet fuzzy-matches one product's canonical phrase above threshold; keep it a single clear winner so the margin rule passes.)

    `test_match_listing_ambiguous_unmatched` (MATCH-01, D-03): a title that satisfies the `required_keywords` of TWO products (e.g. contains both "elite trainer box"/"etb" and "booster box"/"display" for the same set) returns `match_status="unmatched"` and is NOT auto-resolved to either candidate (`matched_product_id is None`).

    `test_exclusion_lot` (MATCH-02): titles with quantity/lot language ("lot of 4", "job lot", "set of 6", "x3"/"3x", "wholesale") return `check_exclusion(...) == "lot"`.

    `test_exclusion_damaged_severe_only` (MATCH-02, D-08): titles with severe damage signals ("dented", "resealed", "opened", "empty box", "no cards") return `"damaged"`, WHILE titles with only minor-cosmetic language ("shelf wear", "corner ding", "minor wear") return `None` (not excluded).

    `test_exclusion_counterfeit` (MATCH-02): titles with counterfeit/replica signals ("replica", "proxy", "reproduction", "bootleg", "custom") return `"counterfeit"`.

    `test_exclusion_does_not_flag_legitimate_bundle` (MATCH-02, D-06, RESEARCH Pitfall 1): a legitimate `booster_bundle` title (e.g. "Chaos Rising Booster Bundle 6 Packs") returns `check_exclusion(...) is None` — the bare word "bundle" must NOT trigger the lot rule, because it is a first-class product-type keyword.

    All titles fed to `match_listing`/`check_exclusion` should be pre-normalized in the test (call `normalize` first, as the real pipeline does) or written already-normalized (lowercase, no punctuation) — match the real call order.
  </action>
  <verify>
    <automated>pytest tests/test_matching.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_matching.py` exists and `pytest tests/test_matching.py --collect-only -q` exits 0 (all test functions collectible without importing scripts.matching at module level).
    - The file contains the eight test functions named above.
    - Running `pytest tests/test_matching.py::test_match_listing_keyword_exact -x` fails with a ModuleNotFoundError/ImportError for `scripts.matching` (RED for the right reason — the module does not exist yet), not a fixture-not-found or syntax error.
    - No top-level `from scripts.matching import` statement exists (grep for it at column 0 returns nothing; imports are indented inside test bodies).
  </acceptance_criteria>
  <done>MATCH-01/02 unit tests are authored, collectible, and RED because scripts/matching.py does not yet exist.</done>
</task>

<task type="auto">
  <name>Task 3: RED tests — outlier filter, price_points aggregation, run_matching_once and run_ingestion_once end-to-end (MATCH-03, D-04)</name>
  <files>tests/test_matching.py</files>
  <read_first>
    - tests/test_ingest_worker.py (the fixture-driven integration pattern `test_price_and_shipping_captured(ingest_db)` — seed docs via `insert_many`/`upsert`, call the function under test, assert on `find_one`; and the monkeypatch pattern `test_run_ingestion_once_happy_path(ingest_db, monkeypatch)` for stubbing get_app_token/search_sealed_listings; use exactly-representable floats like 50.00/4.50/54.50 to keep `==` assertions safe)
    - .planning/phases/04-listing-matching-price-normalization/04-RESEARCH.md (Pattern 3 `filter_outliers(listings, price_field="total_price") -> (included, excluded)` with 2-pstdev cutoff and skip-below-3; Pattern 4 `aggregate_and_write(db, product_id, included, ts) -> bool`; the `run_matching_once(db, run_id, ts) -> {"listings_matched","listings_unmatched","listings_excluded"}` orchestration code example; the price_points doc shape `{ts, product_id, item_price, total_price}`)
    - .planning/phases/04-listing-matching-price-normalization/04-CONTEXT.md (D-09 one point per product per run, D-10 median, D-11 skip-write on empty, D-12 independent medians, D-13 2 std dev, D-14 skip-below-3, D-16 exclusion_reason="outlier", D-04 ingestion_runs counts)
    - db/init_collections.py (price_points time-series shape; note it has no $jsonSchema validator so extra fields are permitted but the locked shape is `{ts, product_id, item_price, total_price}`)
    - scripts/ingest_worker.py (run_ingestion_once's final `update_doc` / ingestion_runs shape that D-04's three new count fields extend)
  </read_first>
  <action>
    Append these tests to `tests/test_matching.py` (still deferred imports inside each body):

    `test_outlier_filter_excludes_far_listings` (MATCH-03, D-13): build a list of ≥5 synthetic listing dicts (each `{"total_price": <float>, "item_price": <float>, "_id": ...}`) where four cluster tightly and one sits far above (>2 population std devs from the median). Assert `filter_outliers(listings)` returns `(included, excluded)` where the far listing is in `excluded` and the tight cluster is in `included`. Default `price_field` must be `total_price` (RESEARCH Pitfall 5 — never `item_price`).

    `test_outlier_filter_skipped_when_too_few` (MATCH-03, D-14): a list of exactly 2 listings (below the min-count threshold) returns them ALL in `included` and an empty `excluded`, even if the two prices differ wildly (filtering is skipped entirely below 3).

    `test_price_points_median_aggregation` (MATCH-03, D-09/D-10/D-12, uses matching_db): call `aggregate_and_write(matching_db, "perfect-order_booster_box", included, ts)` with an ODD-count included set whose `item_price` and `total_price` medians are distinct, exactly-representable values (so the two medians are independent, provable numbers). Assert exactly ONE `price_points` document is written for that `product_id`, with `item_price` == the median of the included item prices and `total_price` == the median of the included total prices (independent medians), and `ts` == the passed timestamp. Assert the function returned `True`.

    `test_price_points_skipped_when_zero_included` (MATCH-03, D-11): `aggregate_and_write(matching_db, "chaos-rising_etb", [], ts)` returns `False` and writes ZERO `price_points` documents for that product (gap, not carry-forward).

    `test_run_matching_once_end_to_end` (MATCH-01/02/03 + D-16, uses matching_db): insert into `active_listings` (all with `run_id="RUN-E2E"`, `fetched_at=ts`) a realistic batch of synthetic listings targeting a single product (e.g. Perfect Order booster_box): an ODD number (≥3) of clean matchable listings whose total_prices are tight, ONE clean-matchable-but-far outlier listing, one lot listing ("... booster box lot of 4"), one damaged listing ("... booster box dented"), one counterfeit listing ("... booster box replica"), one ambiguous listing (satisfies two products' keywords), and one nonsense unmatched listing. Call `run_matching_once(matching_db, "RUN-E2E", ts)`. Assert: (a) each active_listings doc now carries the expected `match_status`/`matched_product_id`/`exclusion_reason` (clean→matched with the box slug and no exclusion; lot/damaged/counterfeit→ their reason; the far listing→ `exclusion_reason="outlier"` per D-16; ambiguous & nonsense→ `match_status="unmatched"`); (b) exactly ONE price_points doc exists for `"perfect-order_booster_box"` whose `total_price` equals the median of the clean included listings (outlier and keyword-excluded listings NOT in the median); (c) the returned dict has integer `listings_matched`, `listings_unmatched`, `listings_excluded` consistent with the seeded batch. Use exactly-representable floats and an odd clean-included count so the expected median is a single listing's value.

    `test_run_ingestion_once_records_match_counts` (D-04, uses matching_db + monkeypatch): monkeypatch `scripts.ingest_worker.get_app_token` and `scripts.ingest_worker.search_sealed_listings` (as `test_run_ingestion_once_happy_path` does) so each CATALOG product returns a small synthetic item list including at least one cleanly-matchable title for one product. Call `scripts.ingest_worker.run_ingestion_once(matching_db)`. Assert the resulting `ingestion_runs` document contains integer fields `listings_matched`, `listings_unmatched`, and `listings_excluded` (present as top-level flat fields, not nested), proving run_matching_once's counts were merged into the run doc.
  </action>
  <verify>
    <automated>pytest tests/test_matching.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - All six test functions above are present and collectible (`pytest tests/test_matching.py --collect-only -q` exits 0).
    - `test_run_matching_once_end_to_end` and `test_run_ingestion_once_records_match_counts` request the `matching_db` fixture by name.
    - Running `pytest tests/test_matching.py::test_price_points_median_aggregation -x` fails RED due to missing `scripts.matching` (ImportError), not a fixture-not-found error — proving Task 1's fixture resolves.
    - Every synthetic price value used in `==` assertions is exactly representable in binary float (e.g. 150.00, 155.00, 160.00), following test_ingest_worker.py's precedent.
  </acceptance_criteria>
  <done>MATCH-03 and end-to-end / D-04 tests are authored, collectible, consuming matching_db, and RED pending implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → live pokemonview_test MongoDB | Test fixture drops/recreates collections on a real Atlas/local instance (never the production `pokemonview` DB) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-T1 | Tampering | matching_db fixture targeting the wrong database | low | mitigate | Fixture uses the existing `TEST_DB_NAME = "pokemonview_test"` constant (a `_test`-suffixed DB distinct from real `pokemonview`), copied verbatim from `ingest_db`; drops only the four test collections |
| T-04-I1 | Information Disclosure | MONGODB_URI logged by the fixture | low | mitigate | Fixture reads MONGODB_URI via dotenv+os.environ only, never prints it — mirrors `ingest_db`'s existing credential-hygiene discipline |

No V5 input-validation surface is introduced by this test-only plan; the untrusted-`title` handling and ReDoS threats (T-04-01/T-04-02) are carried by the implementation plans (04-03/04-04) and are exercised by these tests.
</threat_model>

<verification>
- `pytest tests/test_matching.py --collect-only -q` exits 0.
- `pytest --collect-only -q` (full suite) exits 0 — the new conftest fixture does not break existing collection.
- Running `pytest tests/test_matching.py -x` fails RED with a missing-`scripts.matching` import (expected until Plans 04-03/04-04/04-05 land). This is the intended state at end of this plan.
</verification>

<success_criteria>
A collectible RED test scaffold (tests/test_matching.py) and a matching_db fixture exist, pinning the full MATCH-01/02/03 + D-04 behavioral contract, failing only because the implementation does not yet exist.
</success_criteria>

<output>
Create `.planning/phases/04-listing-matching-price-normalization/04-02-SUMMARY.md` when done.
</output>
