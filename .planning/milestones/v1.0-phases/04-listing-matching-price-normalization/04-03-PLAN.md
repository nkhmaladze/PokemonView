---
phase: 04-listing-matching-price-normalization
plan: 03
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - scripts/matching.py
autonomous: true
requirements: [MATCH-01, MATCH-02]
must_haves:
  truths:
    - "normalize() lowercases, strips filler marketing words by word-boundary regex (does NOT corrupt 'resealed'→'re'), strips punctuation, and collapses whitespace"
    - "match_listing() is strict/precision-first (D-01): a single keyword candidate → matched/keyword; >1 keyword candidate → unmatched (D-03, never auto-resolved); zero candidates → bounded fuzzy fallback that only accepts a clear winner above the cutoff-and-margin"
    - "matched_product_id equals the products._id slug: {set_name}_{product_type} lowercased with spaces→hyphens"
    - "check_exclusion() flags lot/damaged/counterfeit and returns None for a legitimate booster_bundle listing (D-06) and for minor-cosmetic-wear language (D-08)"
    - "A missing/malformed title is handled defensively, and all exclusion regexes use simple non-nested bounded quantifiers (no ReDoS)"
  artifacts:
    - "scripts/matching.py (normalize, _product_slug, _canonical_phrase, match_listing, check_exclusion + FILLER_WORDS, FUZZY_SCORE_CUTOFF, FUZZY_MARGIN, LOT_PATTERNS, DAMAGED_PATTERNS, COUNTERFEIT_PATTERNS)"
  key_links:
    - "match_listing consumes scripts.catalog_data.CATALOG required_keywords as the matching input"
    - "matched_product_id slug is byte-identical to seed_catalog.py's _id and ingest_worker.py's product_ref derivation"
    - "PRODUCT_TYPE_SEARCH_TERMS is imported lazily inside _canonical_phrase to avoid a circular import once Plan 04-05 adds `from scripts.matching import run_matching_once` to ingest_worker.py"
---

<objective>
Implement the deterministic, precision-first core of `scripts/matching.py` — `normalize()`, `match_listing()` (two-tier: exact keyword rule, then bounded RapidFuzz fallback), and `check_exclusion()` (lot/damaged/counterfeit keyword pass) — turning the MATCH-01 and MATCH-02 unit tests GREEN.

Purpose: Satisfies MATCH-01 (titles matched to catalog products via keyword rules, with fuzzy fallback) and MATCH-02 (lot/bundle/damaged/counterfeit signalling flagged), under the locked precision-first stance (D-01, D-03, D-06, D-08). These are the pure, DB-free functions the orchestration layer (Plan 04-04) composes.

Output: `scripts/matching.py` with normalize/match_listing/check_exclusion and their module-level constants. No DB writes yet (that is Plan 04-04).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-listing-matching-price-normalization/04-CONTEXT.md
@.planning/phases/04-listing-matching-price-normalization/04-RESEARCH.md
@.planning/phases/04-listing-matching-price-normalization/04-PATTERNS.md
@scripts/catalog_data.py
@scripts/ingest_worker.py
@scripts/seed_catalog.py
@tests/test_matching.py
</context>

## Artifacts this phase produces (this plan's slice)

- `scripts/matching.py` functions: `normalize(title) -> str`, `_product_slug(product) -> str`, `_canonical_phrase(product) -> str`, `match_listing(normalized_title) -> dict`, `check_exclusion(normalized_title) -> str | None`.
- Module-level constants: `FILLER_WORDS` (list), `FUZZY_SCORE_CUTOFF = 90`, `FUZZY_MARGIN = 5`, `LOT_PATTERNS`, `DAMAGED_PATTERNS`, `COUNTERFEIT_PATTERNS` (lists of compiled `re.Pattern`).
- New `active_listings` field VALUES produced (written by Plan 04-04): `match_status` ("matched"|"unmatched"), `matched_product_id` (slug|None), `match_method` ("keyword"|"fuzzy"|"ambiguous"|None), `match_score` (float|None), `exclusion_reason` ("lot"|"damaged"|"counterfeit"|None; "outlier" added in 04-04).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: normalize() and the two-tier match_listing() (MATCH-01, D-01/D-03)</name>
  <files>scripts/matching.py</files>
  <behavior>
    - normalize("Resealed Chaos Rising ETB Factory Sealed") → a lowercased token string still containing "resealed" (word-boundary filler strip, not substring replace) and with "factory sealed" filler removed.
    - A normalized title containing all of one product's required_keywords → {match_status:"matched", matched_product_id:"chaos-rising_etb", match_method:"keyword", match_score:None}.
    - A normalized title satisfying two products' required_keywords → {match_status:"unmatched", matched_product_id:None, match_method:"ambiguous", match_score:None} (D-03: never pick a winner).
    - A near-miss title (zero exact keyword candidates) closest to one product → {match_status:"matched", match_method:"fuzzy", match_score:<float>=cutoff}, only when the top fuzzy score ≥ FUZZY_SCORE_CUTOFF and beats the runner-up by ≥ FUZZY_MARGIN; otherwise {match_status:"unmatched", match_method:None}.
  </behavior>
  <read_first>
    - scripts/ingest_worker.py (lines ~44-56: the `PRODUCT_TYPE_SEARCH_TERMS` dict this module reuses for the fuzzy canonical phrase; lines ~239-241: the product-slug derivation `f"{product['set_name']}_{product['product_type']}".lower().replace(" ", "-")` that `_product_slug` must reproduce exactly; the module docstring's no-top-level-side-effects + credential-hygiene conventions)
    - scripts/seed_catalog.py (line ~70: the identical slug used as `products._id` — matched_product_id MUST equal this so Phase 5 can join price_points.product_id → products._id)
    - scripts/catalog_data.py (CATALOG entries; each has `set_name`, `product_type`, `required_keywords`, but NO `_id` field — the slug must be computed, not read)
    - .planning/phases/04-listing-matching-price-normalization/04-RESEARCH.md (Pattern 1 full code example for normalize/_canonical_phrase/match_listing including FILLER_WORDS, FUZZY_SCORE_CUTOFF=90, FUZZY_MARGIN=5, and the `process.extract(..., scorer=fuzz.token_sort_ratio, limit=2)` fallback; the anti-pattern notes on `.replace()` corruption and display_name dilution)
    - .planning/phases/04-listing-matching-price-normalization/04-PATTERNS.md (the `scripts/matching.py` analog section — imports pattern, no-side-effects-on-import)
    - tests/test_matching.py (the exact assertions of test_normalize_word_boundary_preserves_resealed, test_match_listing_keyword_exact, test_match_listing_fuzzy_fallback, test_match_listing_ambiguous_unmatched)
  </read_first>
  <action>
    Create `scripts/matching.py` with a module docstring that states: no top-level side effects on import (no MongoClient, no network, no rapidfuzz model loading beyond the plain `from rapidfuzz import fuzz, process`), and never log full listing `title` strings at info level (credential/data hygiene, mirroring ingest_worker.py). Top-level imports: stdlib `re`; `from rapidfuzz import fuzz, process`; `from scripts.catalog_data import CATALOG`. Do NOT import `PRODUCT_TYPE_SEARCH_TERMS` at module top level.

    Implement `normalize(title)` per RESEARCH Pattern 1: lowercase; for each word in `FILLER_WORDS` remove it via `re.sub(rf"\b{re.escape(word)}\b", " ", t)` (word-boundary anchored — NOT `str.replace`, which corrupts "resealed"); replace remaining non-word/non-space characters with a space; collapse whitespace and strip. Define `FILLER_WORDS` as the RESEARCH list ("new", "sealed", "factory sealed", "fast ship", "fast shipping", "free shipping", "brand new", "in hand", "ready to ship", "same day ship").

    Implement `_product_slug(product)` returning `f"{product['set_name']}_{product['product_type']}".lower().replace(" ", "-")` — byte-identical to seed_catalog.py's `_id` and ingest_worker.py's `product_ref` formula.

    Implement `_canonical_phrase(product)` returning `f"{product['set_name']} {PRODUCT_TYPE_SEARCH_TERMS[product['product_type']]}".lower()`, where `PRODUCT_TYPE_SEARCH_TERMS` is obtained via a DEFERRED import inside the function body (`from scripts.ingest_worker import PRODUCT_TYPE_SEARCH_TERMS`). This deferral is mandatory: a module-top-level import would create a circular import once Plan 04-05 adds `from scripts.matching import run_matching_once` to ingest_worker.py. Do NOT use catalog `display_name` for the fuzzy phrase (its shared "pokemon tcg: mega evolution-" boilerplate dilutes discrimination — RESEARCH Pitfall 3).

    Implement `match_listing(normalized_title)` returning the dict `{match_status, matched_product_id, match_method, match_score}` per RESEARCH Pattern 1: Tier 1 collects `candidates = [p for p in CATALOG if all(kw in normalized_title for kw in p["required_keywords"])]`; exactly 1 candidate → matched/keyword with `matched_product_id=_product_slug(candidate)` and `match_score=None`; >1 candidate → unmatched/ambiguous/None (D-03). Zero candidates → Tier 2: build `phrases = {_canonical_phrase(p): _product_slug(p) for p in CATALOG}`, call `process.extract(normalized_title, list(phrases), scorer=fuzz.token_sort_ratio, limit=2)`; accept the top result only if `top_score >= FUZZY_SCORE_CUTOFF` AND `(top_score - second_score) >= FUZZY_MARGIN` → matched/fuzzy with the winning slug and `match_score=top_score`; else unmatched/None with `match_score=top_score` (or None if no results). Define `FUZZY_SCORE_CUTOFF = 90` and `FUZZY_MARGIN = 5` as named module constants.
  </action>
  <verify>
    <automated>pytest tests/test_matching.py::test_normalize_word_boundary_preserves_resealed tests/test_matching.py::test_match_listing_keyword_exact tests/test_matching.py::test_match_listing_fuzzy_fallback tests/test_matching.py::test_match_listing_ambiguous_unmatched -x</automated>
  </verify>
  <acceptance_criteria>
    - The four named tests pass (exit 0).
    - `python -c "import scripts.matching"` succeeds with no network/DB side effects (imports cleanly; rapidfuzz importable per Plan 04-01).
    - `python -c "from scripts.matching import _product_slug; print(_product_slug({'set_name':'Chaos Rising','product_type':'etb'}))"` prints exactly `chaos-rising_etb` (matches products._id).
    - `python -c "import scripts.matching, ast,inspect; src=inspect.getsource(scripts.matching); print('PRODUCT_TYPE_SEARCH_TERMS' in src)"` is True AND no module-top-level import of PRODUCT_TYPE_SEARCH_TERMS exists — verified by `python -c "import scripts.ingest_worker, scripts.matching; print('no circular import')"` succeeding in both import orders.
    - normalize preserves the token "resealed" (test_normalize_word_boundary_preserves_resealed passes).
  </acceptance_criteria>
  <done>normalize + match_listing implement the strict two-tier match; matched_product_id equals the products._id slug; the four MATCH-01/normalize tests are GREEN; no circular import.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: check_exclusion() — lot/damaged/counterfeit pass (MATCH-02, D-06/D-08)</name>
  <files>scripts/matching.py</files>
  <behavior>
    - check_exclusion("... booster box lot of 4") → "lot"; "job lot", "set of 6", "x3"/"3x", "wholesale" → "lot".
    - check_exclusion("... booster box dented") → "damaged"; "resealed", "opened", "empty box", "no cards" → "damaged".
    - check_exclusion("... shelf wear ... corner ding ... minor wear") → None (D-08: cosmetic never excluded).
    - check_exclusion("... booster box replica") → "counterfeit"; "proxy", "reproduction", "bootleg", "custom" → "counterfeit".
    - check_exclusion("chaos rising booster bundle 6 packs") → None (D-06: bare "bundle" is a legitimate product-type keyword, never a lot signal).
  </behavior>
  <read_first>
    - .planning/phases/04-listing-matching-price-normalization/04-RESEARCH.md (Pattern 2 full code example: LOT_PATTERNS, DAMAGED_PATTERNS, COUNTERFEIT_PATTERNS and the check_exclusion precedence lot→damaged→counterfeit; the explicit "deliberately NOT the bare word 'bundle'" and "deliberately does NOT include 'shelf wear'/'corner ding'" comments; the Security Domain note requiring simple non-nested bounded quantifiers to avoid ReDoS)
    - .planning/phases/04-listing-matching-price-normalization/04-CONTEXT.md (D-06 three categories, D-08 severe-only condition signals)
    - scripts/catalog_data.py (confirm booster_bundle required_keywords include "bundle" — the exact reason the lot rule must never key on the bare word)
    - tests/test_matching.py (the exact assertions of test_exclusion_lot, test_exclusion_damaged_severe_only, test_exclusion_counterfeit, test_exclusion_does_not_flag_legitimate_bundle)
  </read_first>
  <action>
    Add `check_exclusion(normalized_title)` and the three compiled-pattern lists to `scripts/matching.py`, per RESEARCH Pattern 2. Define `LOT_PATTERNS` as compiled regexes targeting QUANTITY language only — `\blot\s+of\b`, `\bjob\s*lot\b`, `\bwholesale\b`, `\bset\s+of\s+\d+\b`, `\bx\s?[2-9]\d*\b`, `\b[2-9]\d*\s?x\b`, `\b[2-9]\d*\s*(boxes|packs|etbs|bundles)\b` — and NEVER a bare-word "bundle" pattern (that word is a first-class booster_bundle required_keyword; keying on it would wrongly exclude every legitimate bundle listing — RESEARCH Pitfall 1). Define `DAMAGED_PATTERNS` covering the severe/explicit signals only (`\bdented\b`, `\bresealed\b`, `\bopened\b`, `\bempty\s+box\b`, `\bno\s+cards?\b`, `\bdamaged\b`, `\bcrushed\b`, `\btorn\s+seal\b`, `\bbroken\s+seal\b`, `\bseal\s+broken\b`) and deliberately excluding cosmetic language such as shelf-wear/corner-ding/minor-wear/light-wear so genuinely sealed product is not over-excluded (D-08). Define `COUNTERFEIT_PATTERNS` (`\breplica\b`, `\bcustom\b`, `\bfan\s?made\b`, `\bproxy\b`, `\breproduction\b`, `\brepro\b`, `\bbootleg\b`, `\bnot\s+authentic\b`, `\bunofficial\b`). `check_exclusion` returns `"lot"` if any LOT pattern matches, else `"damaged"` if any DAMAGED matches, else `"counterfeit"` if any COUNTERFEIT matches, else `None` (precedence order lot→damaged→counterfeit). Every pattern must use simple, non-nested, bounded quantifiers only — no `(a+)+`-style nesting — so an adversarially long title cannot trigger catastrophic backtracking (T-04-02, ReDoS).
  </action>
  <verify>
    <automated>pytest tests/test_matching.py::test_exclusion_lot tests/test_matching.py::test_exclusion_damaged_severe_only tests/test_matching.py::test_exclusion_counterfeit tests/test_matching.py::test_exclusion_does_not_flag_legitimate_bundle -x</automated>
  </verify>
  <acceptance_criteria>
    - The four named exclusion tests pass (exit 0).
    - test_exclusion_does_not_flag_legitimate_bundle passes — a "booster bundle" title returns None (behavioral proof the lot rule never keys on the bare word "bundle").
    - test_exclusion_damaged_severe_only passes — cosmetic-wear titles return None while severe-damage titles return "damaged".
    - `python -c "import scripts.matching, re; s='booster box '+('a b '*4000)+'lot of 4'; print(scripts.matching.check_exclusion(s))"` returns `lot` quickly (< 1s), demonstrating no catastrophic backtracking on a long title.
  </acceptance_criteria>
  <done>check_exclusion flags the three categories with correct precedence, never flags a legitimate bundle or cosmetic-wear listing, and is ReDoS-safe; all MATCH-02 unit tests are GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eBay-originated `active_listings.title` → matching functions | Untrusted external strings (stored by Phase 3) enter normalize()/match_listing()/check_exclusion() and are fed to regex |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-02 | Denial of Service | Regex exclusion patterns (LOT/DAMAGED/COUNTERFEIT) | medium | mitigate | All patterns use simple, non-nested, bounded quantifiers (`\b...\b`, `[2-9]\d*`); no nested `(a+)+` quantifiers — verified by the long-title timing acceptance check (V5 Input Validation, ReDoS) |
| T-04-01 | Denial of Service | normalize()/match_listing() on a missing/malformed title | medium | mitigate | These functions operate on a string arg; the per-listing try/except that isolates a malformed document is added by the caller `run_matching_once` in Plan 04-04 (RESEARCH Security Domain V5 row) — noted here as the boundary owner |
| T-04-03 | Tampering | Manual MongoDB edit writing an invalid `exclusion_reason`/`match_status` (no $jsonSchema validator on active_listings) | low | accept | Intentional per D-07 (direct MongoDB edit is the documented fix path); no validator added this phase — known characteristic for future schema-hardening |

Security note: `security_block_on: high`. This plan's highest threat (T-04-02) is medium and mitigated; no high/critical threats remain open.
</threat_model>

<verification>
- `pytest tests/test_matching.py::test_normalize_word_boundary_preserves_resealed tests/test_matching.py::test_match_listing_keyword_exact tests/test_matching.py::test_match_listing_fuzzy_fallback tests/test_matching.py::test_match_listing_ambiguous_unmatched tests/test_matching.py::test_exclusion_lot tests/test_matching.py::test_exclusion_damaged_severe_only tests/test_matching.py::test_exclusion_counterfeit tests/test_matching.py::test_exclusion_does_not_flag_legitimate_bundle -x` all pass.
- Both import orders `python -c "import scripts.ingest_worker, scripts.matching"` and `python -c "import scripts.matching, scripts.ingest_worker"` succeed (no circular import).
- The outlier/aggregation/end-to-end tests remain RED (implemented in Plan 04-04) — expected at this wave.
</verification>

<success_criteria>
normalize, match_listing (strict two-tier), and check_exclusion are implemented; matched_product_id equals products._id; the eight MATCH-01/MATCH-02/normalize unit tests are GREEN; no circular import; exclusion regexes are ReDoS-safe.
</success_criteria>

<output>
Create `.planning/phases/04-listing-matching-price-normalization/04-03-SUMMARY.md` when done.
</output>
