---
phase: 03-active-listing-ingestion-pipeline
plan: 05
type: execute
wave: 4
depends_on: [03-04]
files_modified: []
autonomous: false
requirements: [INGEST-01, INGEST-02, INGEST-03]
user_setup:
  - service: ebay
    why: "Live Production Browse API run to prove INGEST-01/02/03 against real listing data (price + shippingOptions field shapes were never verified live — Phase 1 Plan 01-04 never completed)"
    env_vars:
      - name: EBAY_CLIENT_ID
        source: "eBay Developer Program -> your app keyset (Production). Lost in a prior .env incident; must be re-obtained. Add via blind append to .env only."
      - name: EBAY_CLIENT_SECRET
        source: "eBay Developer Program -> your app keyset (Production). Add via blind append to .env only."
      - name: EBAY_ENV
        source: "Set EBAY_ENV=production in .env (blind append)."

must_haves:
  truths:
    - "Either: a real Production run of `python -m scripts.ingest_worker --once` wrote real listings to active_listings (each with item_price AND total_price) and a completed ingestion_runs document — OR the developer explicitly deferred this proof because eBay credentials remain unavailable"
  artifacts:
    - "A human confirmation (approved with observed results, or an explicit deferral) recorded in the plan SUMMARY"
  key_links:
    - "This is the ONLY task in the phase that exercises the real Browse API end-to-end; the automated unit/integration tests (Plan 03-04) already prove build_query/lock/upsert/skip correctness without credentials, so a deferral here leaves the phase's code proven and only the live field-shape assumption (A1/A3) unconfirmed"
---

<objective>
Provide the live, end-to-end proof of INGEST-01/02/03 against the real eBay Production Browse API — the one thing the automated tests cannot cover because they use synthetic fixtures. This mirrors Phase 1's own live-verification pattern (Plan 01-04) and its documented risk: `EBAY_CLIENT_ID`/`EBAY_CLIENT_SECRET` were lost in a prior `.env` incident (STATE.md Blockers) and had not been re-obtained as of planning. The Browse API `price`/`shippingOptions`/`itemId` field shapes assumed by `scripts/ebay_client.py` and this worker have therefore NEVER been observed live (03-RESEARCH.md Assumptions A1/A3, Pitfall 3).

Purpose: Confirm that a real run captures real price + shipping and writes correct documents, so INGEST-03 is proven by observation, not code review alone. Because this depends on credentials outside Claude's control, it is a human-gated checkpoint that can be legitimately DEFERRED (recorded as a known-incomplete verification item) if credentials are still unavailable — it MUST NOT hard-block the rest of the phase, whose code is already fully proven by Plan 03-04's automated tests.
Output: A human confirmation of a successful live run (with observed counts and a sample document), OR an explicit deferral noting credentials remain unavailable.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-active-listing-ingestion-pipeline/03-RESEARCH.md
@scripts/ingest_worker.py
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live Production Browse API ingestion run (or explicit deferral)</name>
  <read_first>
    - .planning/STATE.md (Blockers/Concerns — the lost-credentials incident and the .env safety rule)
    - .planning/phases/03-active-listing-ingestion-pipeline/03-RESEARCH.md (Assumptions A1/A3, Pitfall 3, and Validation Architecture "Manual-Only Verifications" — why this must be a live run, not a mock; Open Question 1 — spot-check per-product result counts)
    - scripts/ingest_worker.py (the worker being run; --once path)
  </read_first>
  <action>
    Do NOT attempt to fabricate or mock this verification. This checkpoint requires a real Production Browse API run, which requires live eBay credentials Claude does not have. Automate nothing that touches `.env`; instead present the developer with the exact steps below and PAUSE for their response.

    First determine credential availability without ever reading `.env`: attempt the run and let it fail loudly if credentials are missing (`get_app_token` raises a KeyError on `EBAY_CLIENT_ID` or eBay returns a 401). If the developer has NOT re-obtained credentials, they respond "deferred" and this checkpoint is recorded as a known-incomplete verification item (INGEST-03's live field-shape proof outstanding) — the phase still completes because Plan 03-04's automated tests already prove the worker's logic.

    If credentials ARE available: the developer re-adds them to `.env` using BLIND APPEND ONLY (`>>`, never read/cat/grep/overwrite the file, per the STATE.md incident rule) — `EBAY_CLIENT_ID`, `EBAY_CLIENT_SECRET`, and `EBAY_ENV=production` — then runs `python -m scripts.ingest_worker --once` from the repo root and inspects the results in MongoDB.
  </action>
  <what-built>
    Plan 03-04 built the complete ingestion worker (`scripts/ingest_worker.py`) and Plan 03-03's automated tests already prove — without any eBay credentials — that: build_query produces correct per-product queries, the MongoDB lock blocks concurrent runs, upsert-by-itemId is idempotent, item+total price (with shipping defaulting to 0.0) is captured, and an overlapping run skips with zero eBay calls. What is NOT yet proven is that the REAL Browse API returns the `price`/`shippingOptions`/`itemId` shapes the code assumes. This checkpoint closes that gap (or explicitly defers it).
  </what-built>
  <how-to-verify>
    IF eBay credentials are unavailable:
      1. Respond "deferred — eBay credentials unavailable". The phase completes with this live proof recorded as an outstanding verification item.

    IF eBay credentials are available:
      1. Blind-append EBAY_CLIENT_ID, EBAY_CLIENT_SECRET, and EBAY_ENV=production to `.env` (use `>>` only; do NOT read/cat/grep/overwrite `.env`).
      2. From the repo root run: `python -m scripts.ingest_worker --once`
      3. Confirm the console output shows the run completed (no traceback) and reports per-product query activity and non-zero counts for at least the older/high-volume sets (Chaos Rising, Perfect Order, Ascended Heroes). Note any product returning 0 results (expected possible for pre-release Pitch Black) for a Phase 4 query-tuning follow-up (Open Question 1) — 0 results for a few products is NOT a failure.
      4. In MongoDB (`pokemonview` DB), inspect `db.active_listings.find_one()` and confirm a real document has both `item_price` and `total_price` populated (and `shipping_cost`), a real eBay `_id` (itemId), a `product_ref` slug, and a `fetched_at` timestamp (proves INGEST-03).
      5. Inspect `db.ingestion_runs.find_one(sort=[("started_at", -1)])` and confirm one document with `status` "success" (or "partial"), a `finished_at`, and `listings_fetched`/`listings_written` counts (proves INGEST-01 + SC-4).
      6. Optionally re-run `python -m scripts.ingest_worker --once` and confirm `db.active_listings.count_documents({})` does not balloon (idempotent upsert holds against real data — INGEST-02).
      7. Confirm the console output never printed the raw access token or the MONGODB_URI (threat T-03-01).
  </how-to-verify>
  <acceptance_criteria>
    - EITHER: the developer reports a successful live run with a sampled active_listings document showing populated item_price + total_price and a completed ingestion_runs document with counts — AND confirms no secret was printed to the console
    - OR: the developer responds "deferred" and the outstanding live-proof item is recorded in the SUMMARY (credentials unavailable) — this is an acceptable terminal state for this plan and does not block phase completion
  </acceptance_criteria>
  <verify><human-check>Developer has either (a) run `python -m scripts.ingest_worker --once` against Production and confirmed real active_listings documents carry both item_price and total_price plus a completed ingestion_runs doc with no secret leaked to the console, or (b) explicitly deferred with "credentials unavailable".</human-check></verify>
  <done>A human has confirmed a successful live Production ingestion run (with observed item_price + total_price and run metadata), OR explicitly deferred the live proof because eBay credentials remain unavailable.</done>
  <resume-signal>Type "approved" with a brief note of the observed counts / sample document, or "deferred — credentials unavailable"</resume-signal>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Type | Notes |
|----------|------|-------|
| Live-verification confirmation | verification record | Recorded in 03-05-SUMMARY.md: approved-with-results OR deferred-credentials-unavailable. No source files modified by this plan. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer terminal → eBay Production + MongoDB | A live run uses real Production credentials and writes to the real `pokemonview` DB; secrets must not leak to console/logs |
| `.env` file writes | Re-adding credentials must not read or overwrite `.env` (prior incident wiped it) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information Disclosure | live run console output | medium | mitigate | Verification step 7 explicitly confirms the run never printed the raw access token, Authorization header, or MONGODB_URI; the worker (Plan 03-04) logs only counts and query strings |
| T-03-08 | Tampering | `.env` overwrite during credential re-entry | medium | mitigate | Credentials are re-added by BLIND APPEND (`>>`) only; the file is never read/cat/grep/overwritten, per the STATE.md incident rule — preventing a repeat of the prior credential-loss event |
| T-03-04 | Tampering / resource exhaustion | Production eBay rate budget | low | accept | A single `--once` run costs ~16 Browse API calls, far under the 5,000/day budget; the lock still prevents an accidental overlapping manual run from doubling that |
</threat_model>

<verification>
- Human confirmation recorded: successful live run (item_price + total_price observed in active_listings, completed ingestion_runs doc, no secret leaked) OR explicit deferral (credentials unavailable)
- If deferred, the outstanding live-proof item is documented in the SUMMARY so `/gsd-verify-work` treats it as a known-incomplete verification rather than a silent pass
</verification>

<success_criteria>
The live INGEST-01/02/03 proof is either confirmed by a real Production `--once` run (real documents with both price fields + run metadata, no secret leakage) or explicitly and visibly deferred pending eBay credentials — never silently skipped.
</success_criteria>

<output>
Create `.planning/phases/03-active-listing-ingestion-pipeline/03-05-SUMMARY.md` when done
</output>
