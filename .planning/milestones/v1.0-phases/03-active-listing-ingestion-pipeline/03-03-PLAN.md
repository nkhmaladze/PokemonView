---
phase: 03-active-listing-ingestion-pipeline
plan: 03
type: execute
wave: 2
depends_on: [03-02]
files_modified:
  - tests/conftest.py
  - tests/test_ingest_worker.py
autonomous: true
requirements: [INGEST-01, INGEST-02, INGEST-03]

must_haves:
  truths:
    - "pytest --collect-only succeeds with the new tests collected, even though scripts/ingest_worker.py does not exist yet (lazy imports inside test/fixture bodies)"
    - "tests/conftest.py exposes an ingest_db fixture that skips when MONGODB_URI is unset and otherwise yields a pokemonview_test DB with active_listings/ingestion_locks/ingestion_runs bootstrapped and dropped before/after"
    - "tests/test_ingest_worker.py defines the five REQ-tagged tests (build_query, lock, upsert idempotency, price+shipping, run-skips-when-locked) — RED until Plan 03-04 implements the worker"
  artifacts:
    - "tests/conftest.py — ingest_db fixture added (existing catalog_db fixture preserved)"
    - "tests/test_ingest_worker.py — five tests covering INGEST-01/02/03"
  key_links:
    - "The five tests import scripts.ingest_worker lazily inside their bodies; Plan 03-04's implementation is what turns them GREEN. The ingest_db fixture calls db.init_collections.init_collections (Plan 03-02) to create the three collections the integration tests need."
---

<objective>
Author the Nyquist Wave-0 test scaffold for this phase BEFORE the worker exists: a new `ingest_db` fixture in `tests/conftest.py` (mirroring the existing `catalog_db` fixture but targeting the three ingestion collections) and `tests/test_ingest_worker.py` with the five REQ-tagged tests. The tests are intentionally RED now (they lazily import `scripts.ingest_worker`, which lands in Plan 03-04) and go GREEN once the worker is implemented — exactly the scaffold-first workflow Phase 2 used (02-05 authored the tests RED, 02-06 turned them green).

Purpose: Establishes the automated feedback contract for INGEST-01/02/03 so Plan 03-04's implementation has a green/red signal per commit. Four of the five tests need no live eBay access (they exercise pure functions and the MongoDB lock/upsert), so the phase's core correctness is provable WITHOUT the currently-lost eBay credentials.
Output: `tests/conftest.py` with an added `ingest_db` fixture, and `tests/test_ingest_worker.py` collecting five tests, all RED, `pytest --collect-only` clean.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-active-listing-ingestion-pipeline/03-RESEARCH.md
@.planning/phases/03-active-listing-ingestion-pipeline/03-PATTERNS.md
@.planning/phases/03-active-listing-ingestion-pipeline/03-VALIDATION.md
@tests/conftest.py
@tests/test_catalog_schema.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the ingest_db fixture to tests/conftest.py</name>
  <files>tests/conftest.py</files>
  <read_first>
    - tests/conftest.py (current state — read the whole file; the existing catalog_db fixture, the TEST_DB_NAME = "pokemonview_test" constant, the module-top imports of os/pytest/MongoClient, and the MONGODB_URI skip guard are the exact shapes to mirror; PRESERVE catalog_db unchanged)
    - .planning/phases/03-active-listing-ingestion-pipeline/03-PATTERNS.md (`tests/test_ingest_worker.py` section — the ingest_db fixture shape, lines 124-152)
    - db/init_collections.py (init_collections signature — the fixture calls init_collections(test_db) after dropping the three collections)
  </read_first>
  <action>
    Add a new pytest fixture `ingest_db` to `tests/conftest.py` alongside (not replacing) the existing `catalog_db` fixture. Mirror catalog_db's structure exactly, swapping the collection set: (1) `from dotenv import load_dotenv; load_dotenv()` then read `os.environ.get("MONGODB_URI")` and `pytest.skip(...)` with a message pointing at Plan 02-02 if unset; (2) build a `MongoClient(mongodb_uri)`; (3) target `client[TEST_DB_NAME]` (reuse the existing `pokemonview_test` constant — never the real `pokemonview` DB); (4) before yield, drop `active_listings`, `ingestion_locks`, and `ingestion_runs`, then LAZILY `from db.init_collections import init_collections` inside the fixture body (NOT at module top level, so `pytest --collect-only` succeeds before scripts/ingest_worker.py exists) and call `init_collections(test_db)`; (5) wrap the setup in `try/except Exception: client.close(); raise` mirroring catalog_db; (6) `yield test_db`; (7) teardown: drop the same three collections and `client.close()`. Do NOT seed the catalog (this fixture needs no products data). Do NOT import scripts.ingest_worker anywhere in conftest.py.
  </action>
  <verify>
    <automated>python -c "import ast; src=open('tests/conftest.py').read(); t=ast.parse(src); names=[n.name for n in ast.walk(t) if isinstance(n, ast.FunctionDef)]; assert 'ingest_db' in names and 'catalog_db' in names, names; assert 'active_listings' in src and 'ingestion_locks' in src and 'ingestion_runs' in src; print('ingest_db fixture present, catalog_db preserved')"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/conftest.py` defines both `catalog_db` (unchanged) and a new `ingest_db` fixture
    - `ingest_db` references active_listings, ingestion_locks, and ingestion_runs (drop before + after)
    - `ingest_db` targets TEST_DB_NAME (pokemonview_test), never the real pokemonview DB
    - `ingest_db` skips (not errors) when MONGODB_URI is unset
    - `init_collections` is imported lazily inside the fixture body, not at module top level
    - `python -m pytest --collect-only` exits 0
  </acceptance_criteria>
  <done>tests/conftest.py exposes an ingest_db fixture that yields a throwaway pokemonview_test DB with the three ingestion collections bootstrapped and cleaned up before/after, skipping gracefully when MONGODB_URI is unset, without disturbing the existing catalog_db fixture.</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/test_ingest_worker.py with the five RED REQ-tagged tests</name>
  <files>tests/test_ingest_worker.py</files>
  <read_first>
    - tests/test_catalog_schema.py (current state — the module-docstring-naming-each-test style, plain `assert` (no unittest.TestCase), and the deferred-import pattern of test_seed_idempotent lines 64-76 where `from scripts.X import Y` lives INSIDE the test body so --collect-only works before the module exists — replicate this exactly)
    - .planning/phases/03-active-listing-ingestion-pipeline/03-RESEARCH.md (Pattern 3 upsert_listings signature + listing doc shape; Pattern 4 build_query + PRODUCT_TYPE_SEARCH_TERMS; Pattern 2 acquire_lock/release_lock/ensure_lock_index semantics; ingestion_runs doc shape with status "skipped_locked")
    - .planning/phases/03-active-listing-ingestion-pipeline/03-VALIDATION.md (Per-Task Verification Map — the automated commands each test must satisfy)
  </read_first>
  <action>
    Create `tests/test_ingest_worker.py` with a module docstring naming each test and the REQ ID it satisfies (mirror test_catalog_schema.py's docstring style). Use plain `assert` style, no unittest.TestCase. Every import of `scripts.ingest_worker` MUST be deferred into the individual test bodies (not at module top level) so `pytest --collect-only` passes before the worker exists. Define exactly these five tests:

    1. `test_build_query()` (INGEST-01, pure unit, NO fixture): inside the body, `from scripts.ingest_worker import build_query`. Assert `build_query({"set_name": "Chaos Rising", "product_type": "etb"}) == "Pokemon Chaos Rising Elite Trainer Box"`, `build_query({"set_name": "Chaos Rising", "product_type": "booster_box"}) == "Pokemon Chaos Rising Booster Box"`, and `build_query({"set_name": "Perfect Order", "product_type": "booster_bundle"}) == "Pokemon Perfect Order Booster Bundle"`. These pin Pattern 4's exact output shape and prove build_query does NOT join `required_keywords`.

    2. `test_price_and_shipping_captured(ingest_db)` (INGEST-03, integration): inside the body, `from scripts.ingest_worker import upsert_listings`. Build two synthetic Browse-API-shaped item dicts using EXACTLY-representable float values to avoid binary-float equality flakiness: item A = `{"itemId": "v1|A|0", "title": "Chaos Rising ETB", "price": {"value": "50.00"}, "shippingOptions": [{"shippingCost": {"value": "4.50"}}]}`, and item B = `{"itemId": "v1|B|0", "title": "Chaos Rising ETB free ship", "price": {"value": "50.00"}}` (a price but NO shippingOptions key). Call `upsert_listings(ingest_db, "chaos-rising_etb", [item_a, item_b], run_id="r1", fetched_at=<a timezone-aware datetime>)`. Then read both docs back from `ingest_db.active_listings` by `_id`. Assert item A's doc has `item_price == 50.0`, `shipping_cost == 4.5`, `total_price == 54.5`; assert item B's doc has `item_price == 50.0`, `shipping_cost == 0.0`, and `total_price == 50.0` (shipping defaults to 0.0 when shippingOptions is absent — INGEST-03). (50.0, 4.5, and 54.5 are all exactly representable in IEEE-754 double, so `==` is safe here.)

    3. `test_upsert_idempotent(ingest_db)` (INGEST-02, integration): inside the body, `from scripts.ingest_worker import upsert_listings`. Call it twice with the SAME list of synthetic items (each with a distinct `itemId`). Assert `ingest_db.active_listings.count_documents({})` after the second call equals the count after the first (no duplicates — upsert by `_id`=itemId is idempotent). Mirror test_catalog_schema.py::test_seed_idempotent's count-before/count-after shape.

    4. `test_lock_prevents_concurrent_acquire(ingest_db)` (INGEST-02, integration): inside the body, `from scripts.ingest_worker import ensure_lock_index, acquire_lock, release_lock`. Call `ensure_lock_index(ingest_db)`. Assert `acquire_lock(ingest_db, "holder-A") is True`; assert a second `acquire_lock(ingest_db, "holder-B") is False` while A holds it; call `release_lock(ingest_db, "holder-A")`; assert `acquire_lock(ingest_db, "holder-B") is True` after release. This proves SC-2's "concurrent runs are prevented by locking".

    5. `test_run_ingestion_once_skips_when_locked(ingest_db, monkeypatch)` (INGEST-02, integration, credential-free): inside the body, `from scripts import ingest_worker`. Monkeypatch `ingest_worker.get_app_token` and `ingest_worker.search_sealed_listings` to raise AssertionError if ever called (proving no eBay call happens on the skip path). Call `ingest_worker.ensure_lock_index(ingest_db)` then `ingest_worker.acquire_lock(ingest_db, "other-run")` to pre-hold the lock. Call `ingest_worker.run_ingestion_once(ingest_db)`. Assert `ingest_db.active_listings.count_documents({}) == 0` (no listings written) and that `ingest_db.ingestion_runs` contains a document with `status == "skipped_locked"`. This proves an overlapping run makes zero eBay calls and logs the skip (SC-2 + SC-4).

    Because scripts/ingest_worker.py does not exist yet, all five tests are expected to ERROR/FAIL on collection-time import inside their bodies — that is the intended RED state. `pytest --collect-only` must still succeed because no top-level import of scripts.ingest_worker exists.
  </action>
  <verify>
    <automated>python -m pytest tests/test_ingest_worker.py --collect-only -q 2>&1 | grep -E "test_build_query|test_price_and_shipping_captured|test_upsert_idempotent|test_lock_prevents_concurrent_acquire|test_run_ingestion_once_skips_when_locked" | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_ingest_worker.py --collect-only` exits 0 and collects exactly five tests named test_build_query, test_price_and_shipping_captured, test_upsert_idempotent, test_lock_prevents_concurrent_acquire, test_run_ingestion_once_skips_when_locked (the verify command prints 5)
    - No `import scripts.ingest_worker` or `from scripts.ingest_worker import ...` appears at module top level (all deferred into test bodies)
    - The module docstring names all five tests with their REQ IDs (INGEST-01/02/03)
    - Running the tests now yields RED (import errors / failures) because scripts/ingest_worker.py does not exist yet — this is expected and correct at scaffold stage
  </acceptance_criteria>
  <done>tests/test_ingest_worker.py defines the five REQ-tagged tests in plain assert style with all scripts.ingest_worker imports deferred into test bodies; pytest --collect-only is clean and lists exactly five tests, all currently RED pending Plan 03-04.</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Type | Notes |
|----------|------|-------|
| `tests/conftest.py` (extended) | test fixture | Adds `ingest_db` fixture (targets active_listings/ingestion_locks/ingestion_runs on pokemonview_test); preserves `catalog_db` |
| `tests/test_ingest_worker.py` | test | Five tests: `test_build_query`, `test_price_and_shipping_captured`, `test_upsert_idempotent`, `test_lock_prevents_concurrent_acquire`, `test_run_ingestion_once_skips_when_locked` |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test suite → shared MongoDB instance | Tests connect to the same Atlas instance as production data; a test that wrote to the real `pokemonview` DB could corrupt live catalog/listing data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05 | Tampering | test DB isolation | medium | mitigate | The `ingest_db` fixture targets only `TEST_DB_NAME` (`pokemonview_test`, distinct from the real `pokemonview` DB) and drops its three collections both before and after each test, so tests never read stale state nor write to production data (mirrors Phase 2's catalog_db discipline / T-02-05) |
| T-03-CFG | Information Disclosure | MONGODB_URI in fixtures | low | mitigate | MONGODB_URI is loaded via python-dotenv + os.environ at fixture-call time, never hardcoded and never printed/logged in full; the fixture skips (does not error) when it is unset |
| T-03-07 | Denial of Service (test-time regression signal) | build_query / lock / upsert correctness | low | mitigate | The five automated tests are the regression signal that the worker's untrusted-input handling (defensive price/shipping parsing) and lock behavior stay correct across future edits — four of five run without eBay credentials |
</threat_model>

<verification>
- `python -m pytest tests/test_ingest_worker.py --collect-only` exits 0 and lists exactly five tests
- `python -m pytest --collect-only` (whole suite) exits 0 — the new file does not break collection of Phase 2's tests
- conftest.py defines both catalog_db and ingest_db; no top-level scripts.ingest_worker import anywhere
- Tests are RED now (scripts/ingest_worker.py absent) — expected scaffold state
</verification>

<success_criteria>
The Wave-0 test scaffold exists and is collectible: five RED tests covering INGEST-01/02/03 plus an ingest_db fixture, with all worker imports deferred so collection succeeds before the worker is written, and Phase 2's existing tests unaffected.
</success_criteria>

<output>
Create `.planning/phases/03-active-listing-ingestion-pipeline/03-03-SUMMARY.md` when done
</output>
