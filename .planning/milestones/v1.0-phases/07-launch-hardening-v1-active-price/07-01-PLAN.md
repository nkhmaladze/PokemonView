---
phase: 07-launch-hardening-v1-active-price
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ingest_worker.py
  - tests/test_staleness_alert.py
  - .env.example
autonomous: true
requirements: [SC-3]
must_haves:
  truths:
    - "When the most recent success/partial ingestion_runs entry is older than ~8h (or no such run exists), a Discord webhook POST fires."
    - "A recent successful run (gap < threshold) produces no Discord POST."
    - "A missing DISCORD_WEBHOOK_URL never raises and never crashes the worker."
    - "A Discord-side network failure (RequestException) is swallowed, never propagated."
    - "The scheduled path calls check_and_alert_staleness after every run; the --once path does not."
  artifacts:
    - "scripts/ingest_worker.py :: check_and_alert_staleness(db, threshold_hours)"
    - "scripts/ingest_worker.py :: STALENESS_THRESHOLD_HOURS constant"
    - "scripts/ingest_worker.py :: scheduled_job(db) wrapper wired into scheduler.add_job"
    - "tests/test_staleness_alert.py (5 tests)"
    - ".env.example :: DISCORD_WEBHOOK_URL entry"
  key_links:
    - "check_and_alert_staleness reads db.ingestion_runs (status in success|partial, sort started_at desc) — the same collection run_ingestion_once finalizes."
    - "scheduled_job runs run_ingestion_once THEN check_and_alert_staleness so the alert sees the just-written run document."
---

<objective>
Add the SC-3 staleness monitor: a single best-effort Discord alert that fires when the most recent successful/partial ingestion run is stale beyond ~2x the locked 4h polling interval (~8h), plus its five-test Nyquist suite. This is the only genuinely new code in Phase 7 (D-06/D-07).

Purpose: Silent pipeline failures (including the expected credentials-absent state, D-05/D-06) surface as a Discord alert instead of quietly showing users stale prices.
Output: check_and_alert_staleness() and scheduled_job() added to scripts/ingest_worker.py, tests/test_staleness_alert.py (5 tests), DISCORD_WEBHOOK_URL documented in .env.example.
</objective>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- **New file** `tests/test_staleness_alert.py` — 5 unit tests: test_no_successful_run_alerts, test_fresh_run_no_alert, test_stale_run_posts_discord_payload, test_missing_webhook_url_noop, test_discord_post_failure_swallowed.
- **New function** `scripts/ingest_worker.py :: check_and_alert_staleness(db, threshold_hours=STALENESS_THRESHOLD_HOURS) -> None`.
- **New function** `scripts/ingest_worker.py :: scheduled_job(db)` — wrapper calling run_ingestion_once then check_and_alert_staleness.
- **New constant** `scripts/ingest_worker.py :: STALENESS_THRESHOLD_HOURS = 8`.
- **New module-level import** `scripts/ingest_worker.py :: import requests`.
- **New env-var docs** `.env.example :: DISCORD_WEBHOOK_URL`.
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-launch-hardening-v1-active-price/07-RESEARCH.md
@.planning/phases/07-launch-hardening-v1-active-price/07-PATTERNS.md
@scripts/ingest_worker.py
@tests/test_ingest_worker.py
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — create tests/test_staleness_alert.py (5 failing tests)</name>
  <files>tests/test_staleness_alert.py</files>
  <read_first>
    - tests/test_ingest_worker.py — copy its plain-pytest-assert style, inline ingestion_runs seeding, and deferred (in-function-body) import of scripts.ingest_worker.
    - tests/conftest.py — the ingest_db fixture (lines ~98-161): provisions/tears-down a clean ingestion_runs collection in pokemonview_test; reuse it as-is, add no new fixture.
    - .planning/phases/07-launch-hardening-v1-active-price/07-RESEARCH.md — "Validation Architecture" test map and the check_and_alert_staleness reference implementation (lines ~220-259) that defines the expected behavior each test asserts.
  </read_first>
  <action>
    Create tests/test_staleness_alert.py using the ingest_db fixture, unittest.mock.patch, and monkeypatch. Import check_and_alert_staleness inside each test body (deferred import, matching test_ingest_worker.py). Seed ingestion_runs documents inline via ingest_db.ingestion_runs.insert_one({...}) with fields _id, status, started_at, finished_at (timezone-aware datetimes via datetime.now(timezone.utc) +/- timedelta). Mock the outbound call with patch("scripts.ingest_worker.requests.post") — patch at point-of-use in scripts.ingest_worker, not requests.post globally. Set/unset the webhook via monkeypatch.setenv("DISCORD_WEBHOOK_URL", "https://discord.com/api/webhooks/test") / monkeypatch.delenv(..., raising=False). Write exactly these 5 tests:
      - test_no_successful_run_alerts: no ingestion_runs docs at all, webhook set → mock_post.called is True (gap treated as infinite, D-06 "no successful run yet").
      - test_fresh_run_no_alert: one status="success" run finished 1h ago, webhook set, threshold_hours=8 → mock_post.called is False.
      - test_stale_run_posts_discord_payload: one status="success" run finished 10h ago, webhook set, threshold_hours=8 → mock_post.called is True; assert the call kwargs carry json with a "content" key whose value is a str; assert the posted content does NOT contain the webhook URL substring "discord.com/api/webhooks/test" and does NOT contain any "mongodb" substring (secret hygiene, V7).
      - test_missing_webhook_url_noop: one status="success" run finished 10h ago (stale) BUT monkeypatch.delenv DISCORD_WEBHOOK_URL → check_and_alert_staleness(ingest_db, threshold_hours=8) returns without raising and mock_post.called is False.
      - test_discord_post_failure_swallowed: stale run (10h), webhook set, mock_post.side_effect = requests.RequestException("boom") → check_and_alert_staleness(ingest_db, threshold_hours=8) does not raise (wrap in a plain call; test passes if no exception escapes).
    Confirm a status="partial" run also counts as a successful baseline where relevant is NOT required for these 5 — success is sufficient. Do NOT implement check_and_alert_staleness in this task; these tests are RED until Task 2.
  </action>
  <verify>
    <automated>pytest tests/test_staleness_alert.py 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'def test_' tests/test_staleness_alert.py` returns 5.
    - `grep -c 'scripts.ingest_worker.requests.post' tests/test_staleness_alert.py` is >= 1 (mock patches point-of-use).
    - `pytest tests/test_staleness_alert.py` fails/errors (RED) because scripts.ingest_worker has no check_and_alert_staleness yet — the failure is an ImportError/AttributeError on check_and_alert_staleness, NOT a fixture or collection error.
    - The five test function names present: test_no_successful_run_alerts, test_fresh_run_no_alert, test_stale_run_posts_discord_payload, test_missing_webhook_url_noop, test_discord_post_failure_swallowed.
  </acceptance_criteria>
  <done>tests/test_staleness_alert.py exists with 5 tests that fail only because check_and_alert_staleness is not yet defined.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement check_and_alert_staleness + wire into scheduled path + document env var</name>
  <files>scripts/ingest_worker.py, .env.example</files>
  <read_first>
    - scripts/ingest_worker.py — full current file: existing imports (os, datetime/timedelta/timezone at line 35, no requests import yet), LOCK_TTL_SECONDS constant (line 60) for constant placement, run_ingestion_once docstring shape (lines 175-199) to mirror, main()'s scheduler.add_job call (lines 366-373) to rewire.
    - scripts/ebay_client.py — confirms `import requests` as a plain top-level import is the project convention (no side effects), so add it at module top of ingest_worker.py.
    - .planning/phases/07-launch-hardening-v1-active-price/07-RESEARCH.md — Pattern 3 reference implementation (lines 220-275) is the exact target behavior.
    - .env.example — current entries so DISCORD_WEBHOOK_URL is appended alongside MONGODB_URI/EBAY_CLIENT_ID/EBAY_CLIENT_SECRET/CORS_ORIGINS/INGESTION_INTERVAL_HOURS in the same style.
  </read_first>
  <behavior>
    - No successful/partial run found → gap treated as infinite → POST fires (when webhook configured).
    - Last success/partial gap <= threshold_hours → return early, no POST.
    - Last success/partial gap > threshold_hours AND webhook set → single requests.post(webhook, json={"content": message}, timeout=10).
    - Webhook env var absent → return without POST, no raise.
    - requests.RequestException from the POST → caught, swallowed, no raise.
    - Alert message contains only counts/timestamps — never the webhook URL, MONGODB_URI, or eBay credentials (V7).
  </behavior>
  <action>
    In scripts/ingest_worker.py: (1) add `import requests` at module top level alongside the existing stdlib/third-party imports (not lazy — requests has no import side effects, per ebay_client.py). (2) Add `STALENESS_THRESHOLD_HOURS = 8` near LOCK_TTL_SECONDS with a comment noting ~2x the INGESTION_INTERVAL_HOURS default of 4 (D-07). (3) Add function check_and_alert_staleness(db, threshold_hours=STALENESS_THRESHOLD_HOURS) -> None per 07-RESEARCH.md Pattern 3: query db.ingestion_runs.find_one({"status": {"$in": ["success", "partial"]}}, sort=[("started_at", -1)]); compute now = datetime.now(timezone.utc); if last_good is None set gap_hours = float("inf") and last_desc = "no successful run yet", else last_ts = last_good.get("finished_at") or last_good["started_at"] and gap_hours = (now - last_ts).total_seconds() / 3600; return early if gap_hours <= threshold_hours; read webhook_url = os.environ.get("DISCORD_WEBHOOK_URL") and return if falsy (never raise on missing secret — .get() not bracket access, mirroring CORS_ORIGINS); build message referencing the staleness and threshold_hours using only last_desc/timestamps/counts (never interpolate webhook_url or any secret); wrap requests.post(webhook_url, json={"content": message}, timeout=10) in try/except requests.RequestException: pass. Docstring must cite D-06/D-07 and state it never raises and never crashes the worker. (4) Add scheduled_job(db) that calls run_ingestion_once(db) then check_and_alert_staleness(db). (5) In main(), change scheduler.add_job's first arg from run_ingestion_once to scheduled_job (keep args=[db], max_instances=1, coalesce=True, misfire_grace_time=300). Leave the `--once` path calling run_ingestion_once directly — it must NOT call check_and_alert_staleness. (6) Append DISCORD_WEBHOOK_URL to .env.example (a Discord webhook URL, optional; when unset the staleness alert no-ops) in the same commented style as the existing entries.
  </action>
  <verify>
    <automated>pytest tests/test_staleness_alert.py -x && pytest tests/test_ingest_worker.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_staleness_alert.py` — all 5 tests pass (GREEN).
    - `pytest tests/test_ingest_worker.py` — still fully green (no regression to existing worker behavior).
    - `grep -c 'def check_and_alert_staleness' scripts/ingest_worker.py` returns 1.
    - `grep -c 'def scheduled_job' scripts/ingest_worker.py` returns 1.
    - `grep -c 'scheduler.add_job' scripts/ingest_worker.py` returns 1 and the job function is scheduled_job (verify by reading: add_job's first positional arg is scheduled_job, not run_ingestion_once).
    - `grep -c 'DISCORD_WEBHOOK_URL' .env.example` returns 1.
    - `python -c "import scripts.ingest_worker"` succeeds with no network call or MongoClient construction (no import side effects preserved).
  </acceptance_criteria>
  <done>check_and_alert_staleness and scheduled_job exist and are wired into the scheduled path only; all 5 new tests plus the existing worker suite pass; DISCORD_WEBHOOK_URL is documented in .env.example.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker → Discord webhook | Outbound POST from the ingestion worker to an external Discord endpoint carries an operational alert message. |
| worker → MongoDB Atlas | Read of ingestion_runs to determine freshness. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Information Disclosure | DISCORD_WEBHOOK_URL handling in check_and_alert_staleness | medium | mitigate | Read the webhook only via os.environ.get; never log/print it; never interpolate it (or MONGODB_URI / eBay creds) into the alert message body — message carries only counts/timestamps (V7). Test test_stale_run_posts_discord_payload asserts the posted content excludes the webhook URL and any mongodb substring. |
| T-07-02 | Denial of Service | Discord outage or missing webhook crashing the worker loop | medium | mitigate | Missing webhook → early return (no raise); requests.RequestException → swallowed via try/except. Worker never crashes over the alert path (tests test_missing_webhook_url_noop, test_discord_post_failure_swallowed). |
</threat_model>

<verification>
- `pytest tests/test_staleness_alert.py` — 5/5 green.
- `pytest tests/test_ingest_worker.py` — no regression.
- `python -c "import scripts.ingest_worker"` — no import side effects.
</verification>

<success_criteria>
check_and_alert_staleness fires a single best-effort Discord POST when the last success/partial ingestion_runs entry exceeds ~8h (or none exists), no-ops on missing webhook, swallows Discord failures, and is invoked only on the scheduled path — proven by the 5-test suite and the unchanged existing worker suite. (SC-3)
</success_criteria>

<output>
Create `.planning/phases/07-launch-hardening-v1-active-price/07-01-SUMMARY.md` when done.
</output>
