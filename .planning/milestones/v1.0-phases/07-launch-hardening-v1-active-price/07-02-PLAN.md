---
phase: 07-launch-hardening-v1-active-price
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - wsgi.py
autonomous: false
requirements: [SC-1, SC-2]
user_setup: []
must_haves:
  truths:
    - "gunicorn==26.0.0 is pinned in requirements.txt only after a human confirms its PyPI legitimacy (SUS false-positive gate)."
    - "wsgi:app resolves to a live WSGI callable produced by api.app.create_app()."
    - "wsgi.py constructs the app at import time (the one designated create_app() call site) with no other config surface."
  artifacts:
    - "requirements.txt :: gunicorn==26.0.0 line"
    - "wsgi.py :: module-level app = create_app()"
  key_links:
    - "gunicorn --bind 0.0.0.0:8080 --workers 1 --timeout 30 wsgi:app must import wsgi and find `app`."
    - "wsgi.py imports api.app.create_app which reads MONGODB_URI/CORS_ORIGINS from os.environ internally — wsgi.py wires no config itself."
---

<objective>
Add gunicorn as the production WSGI server (behind a mandatory package-legitimacy human gate) and create wsgi.py — the module-level entrypoint gunicorn needs to serve the existing create_app() application factory. No application code changes; this is the containerization-ready serving surface for the Fly.io web process.

Purpose: The Flask API is currently only runnable via the dev server; Fly.io's web process runs `gunicorn ... wsgi:app`, which requires both the dependency and a module:variable entrypoint (D-02, STACK.md's long-deferred gunicorn addition).
Output: gunicorn==26.0.0 in requirements.txt, wsgi.py at repo root.
</objective>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- **Modified file** `requirements.txt` — appended `gunicorn==26.0.0`.
- **New file** `wsgi.py` (repo root) — `app = create_app()` module-level gunicorn entrypoint.
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-launch-hardening-v1-active-price/07-RESEARCH.md
@.planning/phases/07-launch-hardening-v1-active-price/07-PATTERNS.md
@api/app.py
@requirements.txt
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package-legitimacy gate — gunicorn==26.0.0 (T-07-SC)</name>
  <read_first>
    - .planning/phases/07-launch-hardening-v1-active-price/07-RESEARCH.md — "Package Legitimacy Audit" table: gunicorn flagged SUS (`unknown-downloads`), assessed an almost-certain false positive of the same telemetry-gap class already human-approved 5x in this project (pymongo, apscheduler, rapidfuzz, flask, flask-cors).
  </read_first>
  <action>Blocking-human legitimacy gate: halt before gunicorn==26.0.0 is pinned/installed until a human verifies it against pypi.org/project/gunicorn and explicitly approves. Not auto-approvable (auto_advance is ignored for legitimacy checkpoints). On rejection, block the deploy path.</action>
  <what-built>
    gunicorn==26.0.0 is the version to be pinned into requirements.txt and installed by the Docker build during deploy. RESEARCH flagged it SUS solely on an `unknown-downloads` telemetry gap — gunicorn is the 15+-year-old canonical WSGI server (project site gunicorn.org).
  </what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/gunicorn/ and confirm version 26.0.0 is published and the project is the well-known WSGI server (maintainers/benoitc, links to gunicorn.org and github.com/benoitc/gunicorn).
    2. Confirm this matches the expected false-positive pattern (telemetry gap, not a typosquat or a new/unknown package).
    3. This checkpoint is NOT auto-approvable — a human must explicitly approve before gunicorn is pinned/installed.
  </how-to-verify>
  <resume-signal>Type "approved" to allow the gunicorn pin/install, or describe the concern.</resume-signal>
  <acceptance_criteria>
    - A human has explicitly approved (or rejected) after checking pypi.org/project/gunicorn.
    - On rejection, planning halts and the deploy path is blocked — do not pin an unapproved package.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Pin gunicorn + create wsgi.py entrypoint</name>
  <files>requirements.txt, wsgi.py</files>
  <read_first>
    - requirements.txt — current 8 pinned deps ending at flask-cors==6.0.5; append below it.
    - api/app.py — create_app(mongodb_uri=None, db_name=Config.DB_NAME) at line ~29; reads MONGODB_URI (line ~50) and CORS_ORIGINS (line ~55) from os.environ internally when args are omitted.
    - .planning/phases/07-launch-hardening-v1-active-price/07-PATTERNS.md — wsgi.py section: this is the single designated create_app() call site (the one legitimate top-level construction in the codebase).
  </read_first>
  <action>
    Append the exact line `gunicorn==26.0.0` to requirements.txt below `flask-cors==6.0.5` (exact-version pin, matching the project's pinning convention). Create wsgi.py at the repo root: a short module with a docstring stating it is the gunicorn entrypoint and reads MONGODB_URI/CORS_ORIGINS via create_app()'s existing os.environ defaults (no new config surface); `from api.app import create_app`; module-level `app = create_app()`. Do NOT pass mongodb_uri/db_name explicitly — rely on create_app()'s environment defaults. Do NOT add any config reading, logging of secrets, or side effects beyond the single create_app() call. The gunicorn invocation that consumes this (defined in the Dockerfile/fly.toml in plan 07-03) is `gunicorn --bind 0.0.0.0:8080 --workers 1 --timeout 30 wsgi:app`.
  </action>
  <verify>
    <automated>grep -c 'gunicorn==26.0.0' requirements.txt && grep -c 'app = create_app()' wsgi.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'gunicorn==26.0.0' requirements.txt` returns 1 (exact pin present).
    - `requirements.txt` still contains all 8 prior pins (requests, python-dotenv, pymongo, pytest, apscheduler, rapidfuzz, flask, flask-cors) — nothing removed.
    - `grep -c 'from api.app import create_app' wsgi.py` returns 1.
    - `grep -c 'app = create_app()' wsgi.py` returns 1.
    - With MONGODB_URI set in the environment, `python -c "import wsgi; assert wsgi.app is not None"` succeeds and produces a WSGI app object.
    - wsgi.py contains no literal secret values and does not print/log MONGODB_URI or CORS_ORIGINS.
  </acceptance_criteria>
  <done>gunicorn==26.0.0 is pinned; wsgi.py exposes a module-level `app` built from create_app() with no extra config surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → PyPI | A new third-party dependency (gunicorn) enters the build/runtime supply chain. |
| gunicorn → api.app | The WSGI server imports and serves the application factory's app object. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-SC | Tampering | gunicorn==26.0.0 PyPI install (Docker build) | high | mitigate | Blocking-human package-legitimacy checkpoint (Task 1) before the pin enters requirements.txt; exact-version pin (26.0.0) so the build never floats to an unreviewed release. Not auto-approvable. |
| T-07-06 | Information Disclosure | wsgi.py config surface | low | mitigate | wsgi.py adds no config reading and logs nothing — it only calls create_app(), which already handles secret hygiene; no secret is introduced at this layer. |
</threat_model>

<verification>
- Package-legitimacy checkpoint approved by a human against pypi.org/project/gunicorn.
- `grep -c 'gunicorn==26.0.0' requirements.txt` == 1; prior pins intact.
- `python -c "import wsgi; assert wsgi.app is not None"` succeeds with MONGODB_URI set.
</verification>

<success_criteria>
gunicorn is a reviewed, pinned dependency and wsgi:app is a resolvable WSGI callable — the serving prerequisites for the Fly.io web process (SC-1) are in place. (SC-1, SC-2)
</success_criteria>

<output>
Create `.planning/phases/07-launch-hardening-v1-active-price/07-02-SUMMARY.md` when done.
</output>
