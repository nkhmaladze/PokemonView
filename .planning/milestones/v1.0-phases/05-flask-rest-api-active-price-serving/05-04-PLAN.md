---
phase: 05-flask-rest-api-active-price-serving
plan: 04
type: execute
wave: 3
depends_on: [05-03]
files_modified:
  - api/__init__.py
  - api/services/__init__.py
  - api/services/price_service.py
autonomous: true
requirements: [PRICE-01, PRICE-02, PRICE-03]
must_haves:
  truths:
    - "get_current_price(db, product_id) returns the single most-recent price_points document by ts desc with NO time window (D-02) — never null just because the latest ingestion run had a gap — and returns None only when the product has zero points ever (D-01)"
    - "get_current_price passes through the point's literal ts as the freshness source (D-03/PRICE-02) with no computed staleness flag"
    - "get_trend_baseline(db, product_id, current_ts, days, tolerance_days=3) returns the price_points document closest to (current_ts - days) within ±tolerance_days (D-05), and None when nothing falls in the window (D-06)"
    - "compute_pct_change(current_total, baseline_total) computes percent change on total_price only (D-07) and returns None when baseline is 0 (divide-by-zero guard)"
    - "price_service is a pure, DB-taking-as-arg module with no top-level side effects on import (no MongoClient, no os.environ read, no Flask import)"
  artifacts:
    - "api/__init__.py, api/services/__init__.py (package markers)"
    - "api/services/price_service.py (get_current_price, get_trend_baseline, compute_pct_change, TREND_TOLERANCE_DAYS=3)"
  key_links:
    - "get_current_price / get_trend_baseline / compute_pct_change are composed by catalog_service.get_product_detail (Plan 05-05) into the detail response's current_price + trend_7d/trend_30d"
    - "get_current_price returns the whole price_points doc including the listing_count field (Plan 05-02), which catalog_service surfaces as the sample size"
---

<objective>
Implement `api/services/price_service.py` — the three pure price-computation functions (`get_current_price`, `get_trend_baseline`, `compute_pct_change`) over the `price_points` time-series collection — turning the RED `tests/test_price_service.py` (Plan 05-03) GREEN.

Purpose: These functions supply the current-price (PRICE-01), freshness-timestamp (PRICE-02), and 7d/30d trend (PRICE-03) numbers the detail/list endpoints assemble. The phase owns no requirement IDs directly; this is the serving-layer computation that makes those requirements servable in Phase 6. The two genuinely tricky rules of the phase live here and are deliberately kept in two distinctly-named functions per 05-RESEARCH.md Pitfall 2: current price has NO tolerance window (always the latest point, however old — D-02), while the trend baseline HAS a ±tolerance window and an explicit not-found result (D-05/D-06).

Output: `api/__init__.py`, `api/services/__init__.py`, and `api/services/price_service.py` with the three functions and the `TREND_TOLERANCE_DAYS` constant; `tests/test_price_service.py` GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-flask-rest-api-active-price-serving/05-CONTEXT.md
@.planning/phases/05-flask-rest-api-active-price-serving/05-RESEARCH.md
@.planning/phases/05-flask-rest-api-active-price-serving/05-PATTERNS.md
@scripts/matching.py
@db/init_collections.py
@tests/test_price_service.py
</context>

## Artifacts this phase produces (this plan's slice)

- `api/__init__.py`, `api/services/__init__.py` — new package markers (empty, no side effects).
- `api/services/price_service.py` functions: `get_current_price(db, product_id) -> dict | None`, `get_trend_baseline(db, product_id, current_ts, days, tolerance_days=3) -> dict | None`, `compute_pct_change(current_total, baseline_total) -> float | None`.
- Module constant: `TREND_TOLERANCE_DAYS = 3` (D-05).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: api package + get_current_price (D-01/D-02/D-03) + compute_pct_change (D-07)</name>
  <files>api/__init__.py, api/services/__init__.py, api/services/price_service.py</files>
  <behavior>
    - get_current_price(db, "perfect-order_booster_box") → the single newest price_points doc by ts desc, whatever its age (D-02); the returned dict includes ts, product_id, item_price, total_price, and listing_count when present.
    - get_current_price(db, "<product with zero points>") → None (D-01).
    - The returned ts is the literal ts of the latest inserted point (D-03) — no separate staleness computation.
    - compute_pct_change(110.0, 100.0) → 10.0; compute_pct_change(95.0, 100.0) → -5.0; compute_pct_change(100.0, 0) → None.
  </behavior>
  <read_first>
    - tests/test_price_service.py (the RED assertions this task turns green: test_current_price_gap_tolerant, test_current_price_none_when_no_points, test_freshness_is_real_ts, test_trend_uses_total_price_only)
    - .planning/phases/05-flask-rest-api-active-price-serving/05-RESEARCH.md (`## Code Examples` — the exact get_current_price find_one(sort=[("ts", -1)]) shape and compute_pct_change guard; the Anti-Pattern "do not add a within-N-hours filter to current price"; Pitfall 2)
    - .planning/phases/05-flask-rest-api-active-price-serving/05-PATTERNS.md (the "pure DB-taking-as-arg service functions" pattern from scripts/matching.py: db first, typed return, one docstring paragraph citing the D-xx it satisfies, no top-level side effects on import)
    - scripts/matching.py (the docstring + function style to mirror — module docstring stating "no top-level side effects on import", `db`-first pure functions)
    - db/init_collections.py (price_points shape: {ts, product_id, item_price, total_price} + listing_count from Plan 05-02; time-series metaField product_id / timeField ts; the auto compound index supports the find_one sort)
    - the "## API Response Contract" section of Plan 05-03 (the locked signatures)
  </read_first>
  <action>
    Create `api/__init__.py` and `api/services/__init__.py` as empty package markers (no imports, no side effects).

    Create `api/services/price_service.py` with a module docstring stating it is a pure, DB-free-on-import service module (no MongoClient, no os.environ, no Flask import at module level — mirrors scripts/matching.py). Implement:

    `get_current_price(db, product_id) -> dict | None`: return `db.price_points.find_one({"product_id": product_id}, sort=[("ts", -1)])`. Do NOT add any `ts` range/window filter — D-02 requires the single most-recent point however old, and 05-RESEARCH.md's Anti-Pattern explicitly forbids a within-N-hours filter here. Returns the whole document (so the caller gets item_price, total_price, ts, and listing_count when present) or None when the product has no points (D-01). Docstring cites D-01/D-02/D-03.

    `compute_pct_change(current_total, baseline_total) -> float | None`: if `baseline_total == 0` return None (divide-by-zero guard; caller treats as insufficient data); else return `round((current_total - baseline_total) / baseline_total * 100, 2)`. Operates on total_price values only (D-07). Docstring cites D-07.
  </action>
  <verify>
    <automated>pytest tests/test_price_service.py::test_current_price_gap_tolerant tests/test_price_service.py::test_current_price_none_when_no_points tests/test_price_service.py::test_freshness_is_real_ts tests/test_price_service.py::test_trend_uses_total_price_only -x</automated>
  </verify>
  <acceptance_criteria>
    - The four named tests pass (exit 0).
    - `python -c "import api.services.price_service as p; print(hasattr(p,'get_current_price'), hasattr(p,'compute_pct_change'))"` prints `True True`.
    - `python -c "import inspect, api.services.price_service as p; src=inspect.getsource(p.get_current_price); assert 'find_one' in src and 'ts' in src and 'gte' not in src and 'lte' not in src; print('ok')"` prints `ok` (proving no time-window filter on current price).
    - `python -c "import api.services.price_service as p; print(p.compute_pct_change(110.0,100.0), p.compute_pct_change(100.0,0))"` prints `10.0 None`.
    - Importing `api.services.price_service` performs no MongoDB connection (`grep -nE 'MongoClient|os\\.environ|from flask' api/services/price_service.py` returns nothing).
  </acceptance_criteria>
  <done>The api package exists; get_current_price returns the latest point with no window (D-02) or None (D-01), passing through the literal ts (D-03); compute_pct_change is total-price-only with a zero guard (D-07); the four named unit tests are GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: get_trend_baseline within ±tolerance (D-05/D-06) + TREND_TOLERANCE_DAYS</name>
  <files>api/services/price_service.py</files>
  <behavior>
    - get_trend_baseline(db, pid, current_ts, days=7) → the price_points doc closest to (current_ts - 7 days) when one exists within ±3 days of that target (D-05).
    - A point 7 days before current_ts is returned; a point 7±5 days off (outside tolerance) is not.
    - get_trend_baseline(...) → None when no point falls inside the tolerance window (D-06).
    - Works identically for days=30.
  </behavior>
  <read_first>
    - api/services/price_service.py (current state after Task 1 — append get_trend_baseline; reuse the module's imports, add `from datetime import timedelta`)
    - tests/test_price_service.py (test_trend_baseline_within_tolerance, test_trend_insufficient_data — the exact tolerance behavior and the None-on-miss contract)
    - .planning/phases/05-flask-rest-api-active-price-serving/05-RESEARCH.md (`## Code Examples` — the get_trend_baseline aggregation pipeline: $match bounded window → $addFields diff via $abs/$subtract → $sort diff asc → $limit 1; Pitfall 2 — trend HAS a window, unlike current price; Don't-Hand-Roll — push the bounded-window filter into MongoDB)
    - .planning/phases/05-flask-rest-api-active-price-serving/05-PATTERNS.md (Decision-ID-cited constants pattern: `TREND_TOLERANCE_DAYS = 3  # D-05`)
    - .planning/phases/05-flask-rest-api-active-price-serving/05-CONTEXT.md (D-05 ±2-3 day tolerance, D-06 explicit not-found)
  </read_first>
  <action>
    Append to `api/services/price_service.py`. Define `TREND_TOLERANCE_DAYS = 3  # D-05` as a named module constant. Implement `get_trend_baseline(db, product_id, current_ts, days, tolerance_days=TREND_TOLERANCE_DAYS) -> dict | None` per 05-RESEARCH.md's Code-Examples pipeline: compute `target = current_ts - timedelta(days=days)`, `window_start = target - timedelta(days=tolerance_days)`, `window_end = target + timedelta(days=tolerance_days)`; run an aggregation `[{"$match": {"product_id": product_id, "ts": {"$gte": window_start, "$lte": window_end}}}, {"$addFields": {"diff": {"$abs": {"$subtract": ["$ts", target]}}}}, {"$sort": {"diff": 1}}, {"$limit": 1}]`; return `results[0]` if the aggregation yields a document else `None` (D-06 — caller surfaces an explicit "insufficient_data" state, never omits the badge). This is a DIFFERENT function from get_current_price on purpose (Pitfall 2): the bounded window lives ONLY here. Docstring cites D-05/D-06.
  </action>
  <verify>
    <automated>pytest tests/test_price_service.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_price_service.py -x` passes fully GREEN (all price_service unit tests, including test_trend_baseline_within_tolerance and test_trend_insufficient_data).
    - `python -c "import api.services.price_service as p; print(p.TREND_TOLERANCE_DAYS)"` prints `3`.
    - `python -c "import inspect, api.services.price_service as p; src=inspect.getsource(p.get_trend_baseline); assert '$match' in src and '$abs' in src and '$limit' in src; print('ok')"` prints `ok` (proving the bounded-window aggregation is used).
    - Importing the module still performs no MongoDB connection at import time.
  </acceptance_criteria>
  <done>get_trend_baseline returns the closest point within ±TREND_TOLERANCE_DAYS (D-05) or None (D-06), kept distinct from the window-less get_current_price; the full price_service unit-test file is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| price_service → price_points collection | Read-only aggregation/query over the time-series collection; product_id is an internal slug, not raw user text at this layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | product_id / date args into a Mongo query | low | mitigate | product_id reaching price_service is an internal catalog slug (validated upstream at the request boundary in Plan 05-06); the aggregation `$match` uses equality on product_id and a numeric-derived date window, never a `$regex` built from raw user text |
| T-05-04 | Denial of Service | unbounded trend scan | low | accept | The `$match` bounds the window to ±tolerance days before any date-diff math; at ≤16 products / hours-granularity time-series this is trivially bounded |

Security note: `security_block_on: high` — no high/critical threats in this pure-computation plan; request-boundary input validation and the error handler are delivered in Plans 05-05/05-06.
</threat_model>

<verification>
- `pytest tests/test_price_service.py -x` fully GREEN.
- `api/services/price_service.py` has no top-level MongoClient / os.environ / flask import.
- `pytest tests/test_catalog_service.py tests/test_api_products.py -x` still RED (catalog_service and app not built yet — expected).
</verification>

<success_criteria>
price_service.py implements get_current_price (no window, D-02/D-01/D-03), get_trend_baseline (±tolerance window, D-05/D-06), and compute_pct_change (total-price-only, zero-guard, D-07) as pure DB-taking-as-arg functions with no import-time side effects; the price_service unit-test file is GREEN.
</success_criteria>

<output>
Create `.planning/phases/05-flask-rest-api-active-price-serving/05-04-SUMMARY.md` when done.
</output>
